# ТЕХНИЧЕСКОЕ ЗАДАНИЕ (черновая версия)

**Проект:** Онлайн-стратегия на гексагональной карте (2D) с элементами настольной игры.
**Формат:** Сессионная игра, 4–6 игроков (люди + AI) за одной партией.
**Ход:** Одновременный (WeGo) с таймером 2 минуты.

---

## 1. Карта и мир

### 1.1. Структура карты (гексы, 6-гранные) — актуальный алгоритм генерации

Модель — **потайловый рельеф + широтные климатические зоны**. Карта строится не «на глаз» вероятностно, а фиксированным пошаговым алгоритмом (реализован в `web/src/map/terrainGenerator.ts`, проверен на 100+ генерациях без единого нарушения жёстких правил ниже). Алгоритм подобран и много раз откалиброван по вручную собранным эталонным картам (`web/saved-maps/Первая.json`, `Вторая.json`).

- Размер карты: **24 × 18 гекса** (горизонталь × вертикаль) — карта вытянута по горизонтали. Широта идёт вдоль горизонтали (восток-запад), долгота — по вертикали (север-юг).
- Регионы: карта разбита на регионы размером **4 × 3 гекса** (4 по широте/горизонтали, 3 по долготе/вертикали). Сетка регионов — 6 × 6. Итого 36 регионов.
- Рельеф задаётся **на уровне отдельного гекса**, а не региона целиком: Море, Полярный лёд (море), Равнина, Холмы, Горы, Пустыня, Тундра. Внутри одного региона рельеф обычно неоднороден.
- **Лес — не отдельный тип рельефа**, а надстройка (оверлей) поверх Равнины или Холмов. В тропических широтных зонах он автоматически отображается и считается как **Джунгли**, в остальных — как **Лес**. В эталонной карте: **20 тайлов леса и 20 тайлов джунглей**.
- **Широтные зоны** (6 рядов регионов, симметрично север↔юг): Полярный (С) / Умеренный (С) / Тропический (С) / Тропический (Ю) / Умеренный (Ю) / Полярный (Ю) — по одному ряду регионов на зону. Регионы можно менять местами в пределах своей зоны или зеркально между северным и южным аналогом (при зеркальном обмене ряды тайлов внутри региона переворачиваются по долготе).
- **Пустыня встречается только в тропических широтных зонах**, тайлы образуют компактные пятна по 2–3 гекса (не разбросаны поодиночке), суммарно **10–20 тайлов**.
- **Полярный лёд — фиксированно 40 тайлов** (по 20 на полюс) в исходной геометрии, никогда не больше и не меньше. Верхний/нижний ряд карты закрашивается льдом целиком, 6 тайлов с каждого края возвращаются в открытое море, 2 тайла в следующем ряду (ближе к экватору) перекрашиваются в лёд.
- **Состав рельефа в полярных зонах жёстко ограничен:** суша там — только Тундра и Горы. Равнина и Холмы в полярных зонах не встречаются никогда.
- **«Тундра под льдом» (правка — по прямому запросу, было: «раньше тундровые материки выходили массивнее остальных из-за запрета ставить сушу на лёд»).** Часть из 40 ледяных тайлов, попавшая внутрь тундрового материка (шаг 3 ниже) или полярной ребалансировки квоты, теперь получает `terrain: "tundra"` + флаг `iceCover: true` вместо того, чтобы навсегда остаться нетронутой водой — реальная суша (регион с ней считается обитаемым, юниты по ней ходят, на ней может быть ресурс), но **выглядит как лёд** (рендер, `renderer.ts`, красит клетку цветом льда, пока не открыта технология её разработки — переиспользована «Горное дело», см. 7.2) и **город на ней ставить нельзя** (`canFoundCityAt`/`regionHasFoundableTile`, game/main.ts — отдельно от общей проходимости, чтобы не задеть юнитов). Итоговая «ледяная на вид» площадь карты не меняется — просто часть из неё теперь настоящая тундра, а не бесполезная закрытая клетка навсегда; ровно это и позволяет генератору обходиться меньшим количеством ОТКРЫТОЙ тундры при добивке региональных квот (`rebalanceBandMinimums`), отчего тундровые материки на глаз меньше доминируют картой.

#### Порядок генерации рельефа (пошаговый алгоритм, `terrainGenerator.ts`)

1. **Полярный лёд** — см. выше, ровно 40 тайлов, больше не трогается.
2. **Материки.** 5–6 регионов в умеренной/тропической зоне закрашиваются равниной целиком (все 12 гексов). В каждом материке может появиться 0–3 «дыры» открытого моря, но только **по краю региона** (10 из 12 гексов региона — «край», 2 центральных — никогда), чтобы не получалось озеро посреди суши.
3. **Полярные материки.** На каждом полюсе **фиксированно ровно 2 региона** (**правка** — было случайно 1–3, континенты всё равно смотрелись слишком крупными/многочисленными) получают тундру на ВСЕХ тайлах региона — включая ледяные (тундра+флаг «под льдом», см. выше). Квота обитаемых регионов по полярным зонам (`BAND_INHABITED_RANGE`) тоже зафиксирована на [2, 2] — ребалансировка по широтам (шаг «Добивка», ниже) больше не может добавить полюсу третий тундровый регион сверху этих двух континентов. **Дырки моря по краю — 0–3, как у обычных материков (шаг 2)** (**правка** — раньше их не было вовсе, полярные материки заливались тундрой сплошным прямоугольником): кандидаты на дырку берутся только из НЕ-ледяных клеток края региона, так что ледяная шапка никогда не превращается в дырку открытой воды.
4. **Полуострова.** 3–5 регионов, примыкающих к материкам (шаг 2), в умеренной/тропической зоне получают ровно **6 тайлов суши** (равнина) — либо один сплошной блок, либо два клочка по 3, растущие от края региона со стороны материка.
5. **Тундровые острова.** 2–4 отдельных полярных региона получают по **5 гексов тундры** одним связным островом, не по льду.
6. **Добивка до 20 обитаемых регионов.** Считается, сколько регионов уже достигли ≥3 тайлов суши; оставшиеся пустые моря добиваются до этого числа островами по **3 тайла суши** (равнина, либо тундра в полярной зоне). Форма острова — случайно: все 3 гекса рядом, 2+1, либо 1+1+1 полностью раздельно (примерно поровну каждый вариант).
7. **Одиночные тайлы.** Из ещё пустых морей 2/3 получают один тайл суши, всегда **на краю региона** (не в центре — но может случайно оказаться рядом с чужой сушей, тогда визуально «прирастает» к соседу, это нормально).

#### Обитаемость регионов

Регион — произвольная смесь тайлов суши и моря, не жёстко фиксированный тип. Обитаемость регион получает по числу тайлов суши, без привязки к форме:

| Категория региона | Признак | Ресурсов |
|---|---|---|
| Необитаемые морские | 0 тайлов суши | 0 |
| Необитаемые «безжизненные» | 1–2 тайла суши | 0 |
| Обитаемые | ≥3 тайлов суши | ровно 3 |

Итого **всегда ровно 20 обитаемых регионов** (гарантируется шагом 6 алгоритма) — именно они несут ресурсы (см. 1.2). Соотношение «морских» и «безжизненных» среди оставшихся 16 — не фиксированное число, а следствие шага 7 (2/3 добиваются одним тайлом, 1/3 остаётся пустым морем).

**Баланс по широтам.** 20 обитаемых регионов не распределяются по 6 широтным зонам случайно как попало — иначе полярные (тундровые) зоны разрастаются, а другим не хватает места под климатозависимые ресурсы (зерно, специи и т. д.). Правило:

- **Полярные зоны:** 2–3 обитаемых региона.
- **Остальные 4 зоны (умеренные + тропические):** 2–4 обитаемых региона.

Диапазон подбирается случайно при каждой генерации (не всегда «по минимуму +1», честный случайный выбор внутри диапазона), сумма всегда равна 20 — излишек одной зоны переносится в дефицитную (лишний регион «топится» обратно в море, а в регионе-доноре недостающая суша прирастает). Дополнительно гарантируется минимум **4 прибрежных обитаемых региона** суммарно в северной полярной + северной умеренной зоне — иначе китам (см. 1.2) физически негде появиться.

#### Перекраска (после того как форма суши зафиксирована)

1. **Пустыни** — см. выше (10–20 тайлов, только тропики, пятна по 2–3).
2. **Горы** — любая широта, группами по 1–3 тайла (не одиночные и не один гигантский массив), суммарно **10–20 тайлов**.
3. **Холмы** — **20–30 тайлов**, только умеренная/тропическая зона (в полярной зоне холмов не бывает).
4. **Лес/Джунгли (оверлей)** — **20 тайлов** в умеренной зоне (любое полушарие) + **20 тайлов** в тропической, только на равнине/холмах, полностью случайно, без кластеризации.

Равнина — то, что осталось не занятым перекраской (никакого жёсткого ориентира по числу тайлов).

### 1.2. Ресурсы на карте — актуальный алгоритм размещения

- Всего: ровно по **3 на каждый из 20 обитаемых регионов = 60 ресурсов**, без исключений — гарантируется финальным «добивочным» проходом алгоритма (см. ниже), даже если какому-то типу ресурса не хватило подходящего места раньше по списку.
- **Стратегический ресурс не бывает дважды в одном регионе** (металл, углеводороды, силикаты, уран, драгметаллы, редкоземельные) — абсолютное правило, не ослабляется никогда, даже ценой недобора этого типа. Для пищевых/торговых повтор в регионе допускается только как самая последняя мера.
- **Ни один ресурс никогда не ставится на тайл полярного льда.**
- **Широтная зона ресурса — жёсткое ограничение**, не ослабляется никогда, даже при нехватке места (лучше редчайший недобор точного числа этого типа, чем нарушение климата).

#### Порядок посева (важен — реализован в `resourceGenerator.ts`, от самых зажатых условиями к самым свободным)

1. **Киты** (3) — только северные широты (Полярный (С) + Умеренный (С)), обязательно на морском тайле, который граничит с необитаемым соседним регионом (то есть выходит к настоящему морю, а не в тупиковый водный карман внутри материка). Сеются первыми — самое зажатое условиями ограничение.
2. **Рыба** (4) + **Крабы/Моллюски** (4) — регионы умеренной/тропической зоны, обязательно на самом морском тайле; крабы дополнительно должны граничить с необитаемым регионом (как киты).
3. **Металл** (8) — по 1 на каждую из 6 широтных зон + ещё по 1 в каждой из двух полярных (2+2+1+1+1+1=8), никогда дважды в одном регионе.
4. **Углеводороды** (5) — 2 на тайле пустыни, 1 на тайле тундры, 1 в умеренной (С), 1 в умеренной (Ю), никогда на холмах.
5. **Уран** (3) — 1 на тундре, 1 на пустыне, 1 на холмах в умеренной зоне (любое полушарие).
6. **Мех** (3) — 2 на тундре в северном полушарии, 1 на тундре в южном.
7. **Фрукты** (4) — только тропики, обязательно на тайле с оверлеем Джунгли. **Специи** (3) — только тропики, любой не занятый тайл.
8. **Злаки** (4) — только умеренная зона, любой тайл.
9. **Овощи** (4) и **Хлопок** (3) — где угодно, кроме гор, пустыни и тундры.
10. **Остаток** (силикаты 3, драгметаллы 3, домашние животные 4, редкоземельные 2 = 12) — заполняют оставшиеся пустые слоты без ограничений.
11. **Финальная добивка.** Если после шагов 1–10 какой-то из 20 обитаемых регионов всё ещё не набрал 3 ресурса (редко, но случается, если более раннему по списку ресурсу физически не хватило подходящего места) — регион досеивается любым ещё подходящим по климату типом, а если и такого нет — вообще любым не-стратегическим (лёд и повтор стратегического — всё равно под запретом). Это гарантирует «ровно 3 всегда», даже ценой единичного отклонения от целевого числа какого-то типа в очень редких случаях.

| Тип | Категория | Кол-во | Правило размещения |
|---|---|---|---|
| Металлические руды | Стратегический | 8 | По 1 на широту + по 1 доп. на полюсах, никогда дважды в регионе |
| Углеводороды | Стратегический | 5 | 2 пустыня + 1 тундра + 1 умер.(С) + 1 умер.(Ю), никогда на холмах |
| Силикаты | Стратегический | 3 | Без ограничений (остаток) |
| Уран | Стратегический | 3 | 1 тундра + 1 пустыня + 1 холмы (умеренная зона) |
| Драгоценные металлы | Стратегический | 3 | Без ограничений (остаток) |
| Редкоземельные материалы | Стратегический | 2 | Без ограничений (остаток) |
| Рыба | Пищевой | 4 | Морской тайл, умеренная/тропическая зона |
| Крабы/Моллюски | Пищевой | 4 | Морской тайл, умеренная/тропическая зона, граничит с необитаемым регионом |
| Фрукты | Пищевой | 4 | Тропики, только на тайле с Джунглями |
| Злаки | Пищевой | 4 | Умеренная зона |
| Овощи | Пищевой | 4 | Где угодно, кроме гор/пустыни/тундры |
| Домашние животные | Пищевой | 4 | Без ограничений (остаток) |
| Киты | Торговый | 3 | Только север (Полярный (С) + Умеренный (С)), морской тайл, граничит с необитаемым регионом |
| Мех | Торговый | 3 | Тундра, 2 на севере + 1 на юге |
| Хлопок | Торговый | 3 | Где угодно, кроме гор/пустыни/тундры |
| Специи | Торговый | 3 | Тропики |

  (Пищевые: 24. Стратегические: 24. Торговые: 12. Итого **60** — ровно 20 обитаемых регионов × 3, без остатка.)

#### Привязка ресурсов к климату и рельефу

- **Стратегические ресурсы климату почти не подчиняются** — геологическая природа, а не климатическая (исключение — металл, см. выше). Углеводороды/уран также требуют конкретный тип рельефа (пустыня/тундра/холмы), но не широту саму по себе.
- **Пищевые и часть торговых — климатозависимы** (см. таблицу выше).
- **Пустыня** — только тропические широтные зоны, компактные пятна по 2–3 тайла.
- **Горы** — любая широта, небольшие связные скопления (1–3 тайла).
- **Лес/Джунгли** — распределены полностью случайно (без кластеризации), только на тайлах Равнина или Холмы.

### 1.3. Стартовая расстановка (розыгрыш городов)

Перед обычными ходами каждый игрок получает 3 жетона ценой 1, 2 и 3 и по очереди ставит их на гексы карты — по 1 жетону в регион, не больше — это торги за право основать город. Реализовано в `web/src/game/placement.ts` (логика разрешения) + `web/src/game/main.ts` (клики, авто-переход хода).

- Тестовая версия клиента — **без ручного выбора номинала**: при клике на гекс автоматически ставится следующий жетон игрока по порядку 3 → 2 → 1, ход сразу передаётся следующему игроку без отдельной кнопки. После третьего жетона ход игрока завершается автоматически.
- Клик засчитывается по конкретному кликнутому гексу, если это суша; если клик пришёлся на воду/лёд внутри обитаемого региона — жетон ставится на ближайший гекс суши в этом регионе.
- Как только все игроки поставили все жетоны, автоматически запускается разрешение:
  - Регионы проверяются по убыванию номинала жетонов (3 → 2 → 1).
  - Если в регионе на данном шаге у одного игрока жетон уникально старший — он основывает город, все остальные жетоны этого игрока снимаются и больше не участвуют в розыгрыше.
  - Настоящая ничья (два и более игрока с одинаковым старшим номиналом в регионе) остаётся неразрешённой до финального шага — блокирует разрешение и более младших жетонов в этом же регионе.
  - Финальный шаг: все ещё неразрешённые ничьи в самом старшем оставшемся для них номинале решаются случайным выбором.
  - Игрок, не выигравший уникально ни в одном регионе, стартового города не получает.
- Как только все стартовые города (уровень 1) основаны — каждый игрок получает на руки 2 карты с колоды (см. 2.3), и начинается обычная игра (WeGo-ходы).
- В будущей многопользовательской версии игроки ходят одновременно и видят чужие жетоны на карте, но не их номинал, до общего вскрытия; тестовая версия розыгрывает игроков по очереди без таймера — это упрощение для одиночного тестирования, а не финальный дизайн.

---

## 2. Игровой процесс (ход)

### 2.1. Таймер и завершение хода

- Длительность хода: 2 минуты.
- Автоматическое завершение: по истечении таймера ход заканчивается принудительно.
- Досрочное завершение: игрок может нажать «Готово» после того, как сыграл 2 действия. Если все игроки нажали «Готово» — ход завершается мгновенно.
- Дополнительное время: игрок может запросить +30 секунд (ограничение — 3 раза за всю сессию).

### 2.2. Ограничения на действия за ход

- **База — 2 действия** (правка по прямому уточнению, было 3), увеличивается за счёт:
  - **Демократия** (парадигма) — +1 (было и раньше).
  - **Религия** — +1, ЛЮБАЯ принятая религия, не конкретная (новое — стимул её принимать). Оба бонуса складываются: игрок с Демократией и религией одновременно получает 2 (база) + 1 + 1 = 4.
  - **Здание «Управление»** — до +1 РАЗ ЗА ХОД, платно (3💰, `GameSession.useUpravlenie`) — не тратит действие само по себе. Итого максимум за ход при всех трёх сразу: **5** (не 6 — при уточнении арифметики с пользователем расхождение осталось неразрешённым, возможно имелся в виду четвёртый источник, не названный явно).
- Это число может быть увеличено за счёт:
  - Построек.
  - Карточек событий.
  - Политических парадигм (перков).

### 2.3. Управление рукой

- В начале партии рука пуста (0 карт) — игра ещё не началась, карты появляются только по ходу игры.
- Колода (42 карты, см. раздел 3) перемешивается один раз в начале игры.
- Каждый конец хода (точнее — начало следующего цикла игрока, `GameSession.endTurn`) в руку добираются **3 карты** с верха колоды (в свободные слоты, если рука уже полная — не добирается) — **было 2**, поднято по прямому уточнению, потому что 1 из 3 тут же обязана уйти другому игроку (см. ниже) — чистый прирост руки тот же, что и раньше (2).
  - Исключение — самая первая раздача партии (сразу после расстановки, ещё до первого настоящего хода): по-прежнему **2 карты**, без обязательной передачи — отдавать пока некому и незачем.
- Максимум в руке: 7 карт.
  - **[ИСПРАВЛЕНО] Клиент не показывал карты сверх 7-й, даже когда рука временно раздута выше максимума** (по прямому запросу — «игрок не видит часть карт, если в руке больше 7»). Рука легитимно превышает 7 между концами хода (раздача ВСЕГДА добирает полные 3 карты поверх уже раздутой чужими передачами руки, см. выше, плюс сами обязательные передачи) — ровно до момента, когда СВОЙ конец хода этого игрока с рукой ≥8 сбросит её целиком (см. 2.3.1). Раньше слоты руки (`#card-slots`) создавались фиксированно, один раз при входе в фазу playing (`HAND_SIZE` = 7, `buildHandSlotEls`), и больше никогда не пересоздавались — 8-я и дальнейшие карты существовали в состоянии партии, но им физически некуда было отрисоваться. Теперь `ensureHandSlotCount` (main.ts) вызывается при каждом `renderHand()` и заводит/убирает слоты по факту размера руки (не меньше 7 — стабильный вид пустой руки); CSS (`.card-slots`, `flex-wrap: wrap`, ширина капается на 7 карт) переносит лишние карты на вторую строку вместо обрезки; `fitMapToArea()` пересчитывается заново на каждый рендер руки, чтобы карта корректно ужалась под подросшую нижнюю панель.
- **Сыгранная карта уходит на дно колоды** — отдельной стопки сброса нет, очередь карт идёт по кругу: колода → рука → снова дно колоды (реализовано как строгий FIFO — `deck.shift()` при раздаче, `deck.push()` при розыгрыше/сбросе/передаче, ни разу не вставка в произвольное место; 36 карт не создаются и не уничтожаются, только переходят между колодой/руками/рынком). Рука при этом «упаковывается» влево без дыр (сыгранная карта убирается, остальные сдвигаются, новые добавляются справа).
- **Сброс в конце хода за перебор** (реализовано, см. 2.3.1): если в конце хода рука ≥8 карт (на 1 больше максимума), вся рука сбрасывается целиком до 0 — не «до 3», как в раннем черновике этого пункта. Это включает и карты событий, помеченные «нельзя сбросить»: тут не выбор игрока, а штраф за переполнение, так что защита от сброса на них не действует. Каждая сброшенная карта при этом разыгрывает собственный эффект сброса (см. 2.3.1) — отдельный от эффекта её обычного розыгрыша. В этой ветке обязательная передача НЕ выставляется — рука обнулена, отдавать нечего.
  - Сброшенные карты, как и сыгранные, уходят на дно колоды (единая циклическая очередь, без отдельного сброса).
- **Обязательная передача карты — реализовано** (`GameSession.mustHandoff`/`handoffCard`, было «не реализовано» до этой сессии): сразу после обычной раздачи 3 карт игрок ОБЯЗАН передать 1 карту другому игроку, прежде чем сможет сделать хоть что-то ещё в СВОЁМ следующем ходу (проверяется централизованно в `dispatch()` — любое другое действие, включая «Завершить ход», отклоняется с подсказкой, пока долг не закрыт). **Нельзя вернуть ИМЕННО ТУ карту, которую вам когда-то дал этот игрок** — запрет на конкретный экземпляр карты (`CardDef.receivedFrom`, `cards.ts`/`GameSession.handoffCard`), а не на игрока целиком: другую карту (без такой метки, или полученную от кого-то ещё) тому же игроку передать можно. Метка живёт на самой карте (`freshDeck()` клонирует каждую из 36 копий отдельным объектом ровно для этого) и очищается, как только карта уходит в сброс/розыгрыш (снова свободна для кого угодно) или передаётся дальше третьему игроку. Получателя с уже полной рукой (7) выбрать МОЖНО (по прямому уточнению — «8-ю карту можно») — рука получателя дойдёт до 8 и сбросится целиком на его следующем конце хода как обычно (2.3.1), передача — не отдельное исключение из этого правила. Клиент (`main.ts`): клик по любой карте руки, пока долг не закрыт, открывает крупную модалку (`activeModal: "handoff-pick"`, `.handoff-modal`/`.handoff-player-btn`) со списком игроков — большие кнопки цветом игрока, запрещённый получатель показан серым/disabled; строка подсказок отдельно объясняет, что и зачем делать, пока долг висит.

#### 2.3.1. Эффекты сброса за перебор (`main.ts` — `resolveHandOverflowDiscard`/`growRandomForest`)

- **Триггер** — `onPlayingEndTurn()` проверяет размер руки текущего игрока раньше обычного добора 2 карт; при ≥8 добор не происходит вовсе (реализация одноразовая: либо штрафной сброс, либо обычный конец хода, никогда оба). Сейчас это недостижимо ни одним готовым путём — добор (2.3) и покупка на рынке (11.5) оба ограничены максимумом 7 карт заранее, — правило подготовлено на будущее, когда появится механика, которая сможет пробить этот потолок (см. 13).
- **Рабочий среди сброшенных карт отменяет эффекты сброса ТОЛЬКО у карт действий** — если хотя бы одна сброшенная карта этого хода — Рабочий, ни одна другая сброшенная КАРТА ДЕЙСТВИЙ не разыгрывает свой эффект сброса (у самого Рабочего эффекта сброса нет — ни плохого, ни хорошего). **Карты событий (3.2) в тот же нейтрализатор не попадают** — их эффект сброса срабатывает безусловно, даже если в той же пачке лежит Рабочий (явно уточнено — это разные механики, не общий «эффект сброса», а два независимых списка). Проверяется один раз для всей пачки сброса, до перебора карт.
- **Каждая сброшенная карта разыгрывает свой эффект отдельно**, по числу экземпляров (2 Поселенца в сбросе — эффект дважды, не один раз):

  | Карта | Тип | Эффект сброса |
  |---|---|---|
  | Население | событие | **Реализовано, не отменяется Рабочим** (см. 3.2.1) — население всех городов игрока −1 (не ниже 1). |
  | Соберите налоги | событие | **Реализовано, не отменяется Рабочим** (см. 3.2.2) — тот же единственный эффект, что и при добровольном розыгрыше (доход с населения минус содержание), только недоимка списывается автоматически (юниты — новые вперёд, затем здания), без модалки выбора. |
  | Катастрофа | событие | **Реализовано, не отменяется Рабочим** (см. 3.2.3) — тот же единственный эффект, что и при добровольном розыгрыше, только выбор «заплатить или принять» решается автоматически (платит, если есть 2 Силиката, иначе принимает последствия), без модалки. |
  | Рост леса | событие | **Реализовано, не отменяется Рабочим** (см. 3.2.4) — негативная ветка: лес на случайном своём гексе вырубается; если леса на территории нет — случайная Равнина превращается в Пустыню (пищевой ресурс на ней исчезает). |
  | Торговый путь | событие | **Реализовано, не отменяется Рабочим** (см. 3.2.5) — денежный баланс обнуляется; если денег не было — обнуляется склад; если ни того ни другого — эффекта нет. |
  | Мобилизация | событие | **Реализовано, не отменяется Рабочим** (см. 3.2.6) — игрок помечается пропустить свой следующий ход целиком; ход никогда не становится его текущим, пока пометка не сработает. |
  | Поселенец | действие | Население всех городов игрока −1 (не ниже 1 — города не исчезают). |
  | Воин | действие | Каждый юнит игрока требует 1 💰 содержания. Оплачивается сколько хватает денег; юниты сверх того распускаются (удаляются), начиная с последних построенных. |
  | Учёный | действие | **Спроектировано, не реализовано — см. 15.1.** Разыгрывает N катаклизмов (N = текущая максимальная эпоха сбросившего игрока), каждый — случайный из 6 глобальных видов (не только для сбросившего игрока). |
  | Строитель | действие | На случайном пригодном тайле карты (Равнина/Холмы без леса, в любом регионе, не только своём) вырастает лес — упрощённая версия будущей карты события «Рост леса» (3.2), без броска кубиков и привязки к конкретной области. |
  | Торговец | действие | **Новое, ещё не в коде.** Перестраивает один свой торговый путь (случайный, если их несколько) на другой свой город: тот же принцип, что и постройка маршрута (3.1.7) — конечная точка меняется на случайного кандидата среди других своих городов, путь ищется заново (`findRoutePath`, тот же тип рельефа, ≤12 гексов). Если подходящего города/пути нет вообще — маршрут просто исчезает (удаляется), а не остаётся как был. |

  Воин и Поселенец пересчитываются заново на каждом экземпляре карты (не по типу карты один раз) — например, 2 Воина в сбросе при нехватке денег на всех юнитов могут распустить армию до нуля быстрее, чем один и тот же расчёт «сразу на оба». Строчка Торговца выше — единственная в этой таблице, которая пока не реализована в коде (остальные 5 живьём протестированы, включая проверку, что Рабочий нейтрализует Поселенец, но не трогает Население в том же сбросе).
- Сброшенные карты уходят на дно колоды, как и обычно сыгранные (2.3) — отдельного «сброса вникуда» по-прежнему нет.

---

## 3. Колода карт

Всего в колоде 36 карт: 24 карты действий + 12 карт событий.

### 3.1. Карты действий (6 типов, по 4 шт. = 24 карты)

| Карта | Эффект | Цена |
|---|---|---|
| Поселенец | Клик по карте в руке открывает диалог с двумя действиями (см. 3.1.1): **Основать поселение** или **Увеличить население**. Если нужных ресурсов нигде нет — карту сыграть нельзя (в 3.1.1 больше нет ветки «потерять 1 население», это было заменено на прямой отказ). | Основание: 1 любой пищевой. Рост: N разных видов пищи, N = текущее население города |
| Воин | Клик открывает выбор своего города (карта или список), затем — типа юнита из доступных по 6 категориям (см. 3.1.2). Население города не тратится (в отличие от исходного черновика). Если нужных ресурсов нигде нет — юнит не строится. | Зависит от эпохи юнита (см. 3.1.2, ТЗ 7.1) |
| Строитель | Клик открывает панель застройки для стройки (см. 3.1.5) — следующий клик по свободному зданию строит его, списывая цену со столицы/склада/рынка. Прямой клик по зданию без активной карты больше ничего не строит. | Цена конкретного здания (4.4) |
| Рабочий | Клик открывает выбор своего города (карта или список, см. 3.1.4), собирает на склад все ресурсы его региона, которые ещё не добыты в этом цикле и на которые есть технология добычи. Если для типа открыта технология удвоенной добычи — приходит вдвое больше (см. 3.1.4). *(Механика «улучшить добычу ресурса на карте» убрана — региона, а не карты в целом.)* | Только действие — бесплатно по деньгам (платная альтернатива без карты — здание Склад, см. 3.1.4) |
| Ученый | Клик открывает окно исследования (см. 3.1.8) — до 4 доступных технологий, по одной на ветку, с учётом лидерства и досрочного открытия эпохи. | Цена по эпохе описана в 4.3, но пока не списывается |
| Торговец | Клик — выбрать свой город (см. 3.1.6). Приносит 1 💰 за каждый город торговой сети за каждый уникальный тип торгового ресурса, доступного сети (доступ + склад), но не больше суммарного населения сети. Списывает по 1 ед. каждого посчитанного уникального типа. | Ничего сверху — сама карта расходует посчитанные торговые ресурсы |

#### 3.1.1. Поселенец — первая полностью реализованная карта (`main.ts`)

Первая карта действий, доведённая от текста до кода — образец процесса для остальных 5 (сначала уточняем описание и эффект, потом внедряем).

- Клик по карте в руке открывает попап розыгрыша (как у остальных карт), но у Поселенца вместо «Играть» — две кнопки: **🏙 Основать поселение** и **👥 Увеличить население**. Выбор запускает «режим прицела»: следующий клик по карте (основание) или по своему городу — на карте или в списке городов справа (рост) — довершает действие. Esc отменяет, ничего не тратя. (Продажа — отдельный режим, запускается кнопкой «Продать», а не отсюда, см. 11.5.)
- **Основать поселение**: клик по гексу. Требования — регион обитаем (≥3 суши), в нём ещё нет города (ни одного игрока — правило «1 город на регион», ТЗ 9), и он **примыкает** (8-соседство по сетке регионов) к региону, где у игрока уже есть город. *(Альтернатива «или в регионе стоит военный юнит» из исходной формулировки не реализована — юнитов и их размещения на карте ещё нет.)* Новый город: население 1.
- **Увеличить население**: клик по своему городу. Население +1.
- **Цена и порядок списания** — вычисляется без предварительного резервирования (полный «сухой прогон» плана, и только если он целиком сходится — реальное списание; частичной траты при отказе не бывает). Порядок общий (доступ → склад → рынок), но по прямому уточнению («для поселенца нужен пищевой ресурс на складе, а не клети куда ставится поселение») **шаг «Доступ» пропускается целиком при Основании** — у ещё не поставленного города нет инфраструктуры добычи на месте, только уже существующий город (ветка «Увеличить население») может тратить доступ своего региона:
  1. **Доступ** (только при росте существующего города, `planFoodSpend(..., allowAccess=true)`) — ресурсы региона **только того одного города**, где происходит действие (не пул по всем городам игрока — см. поправку в 3.1.4), из тех что «обрабатывает город» (ТЗ 7.2/11.3). Список ресурсов — фактические ресурсы региона на сгенерированной карте, но **только те, что игрок уже умеет добывать** (`resourceIsExtractable`): сухопутная пища и сухопутные торговые — по умолчанию; морские (требуют воду — рыба/крабы/киты) — только после **Мореплавания** («открывает добычу морских ресурсов»); любой стратегический — только после **Горного дела** («открывает добычу ископаемых»). Негодный по этому правилу ресурс в списке города показан значком 🔒, отдельно от «уже потрачен в этом цикле». Гейт действует только на доступ (свою добычу) — купленное на складе или на рынке доступно независимо от того, умеет ли игрок сам это добывать. Отслеживается **по типу на город** (`accessUsed`), не по конкретному тайлу: один и тот же тип из одного города можно потратить не больше раза за цикл. Сбрасывается в начале нового цикла (когда ход возвращается к игроку 0). При Основании (`allowAccess=false`) этот шаг для целевого (ещё не существующего) региона не выполняется вовсе — сразу склад, затем рынок.
  2. **Склад** — реальное хранилище по игроку (`warehouse`), пополняется покупками на рынке и добычей (Рабочий/Склад-здание/Торговец, см. 3.1.4/3.1.6). **Вместимость ограничена — 6 единиц суммарно (все типы вместе), 12 при собственном здании Склад** (`WAREHOUSE_CAP`/`WAREHOUSE_CAP_WITH_SKLAD`, `addToWarehouse()`). Не отказ всей операции — просто обрезается по месту: если добавляемое не помещается целиком, влезает сколько есть свободного места, остаток теряется (как переполнившийся настоящий склад), а не блокирует вызвавшее действие (харвест региона на 4 ресурса при 2 свободных местах положит только 2 первых, а не откажет целиком).
  3. **Рынок** — если типа не хватает ни в доступе, ни на складе, но у игрока есть деньги, алгоритм ищет самый дешёвый подходящий лот на рынке (см. 11.5) и покупает его прямо в оплату (не через склад). Если и это не покрывает нужное число *разных* типов (для роста — обязательно разных, для основания — просто 1 любой) — розыгрыш отклоняется целиком.
- **Тестовый посев рынка**: при старте партии на рынок выложено по 1 единице **каждого** ресурса карты, цена 1, продавец — «Мировой рынок» (не игрок, оплата никому не зачисляется). Сделано специально, чтобы у алгоритма было что покупать без ожидания реальной системы дохода.
- **Лимит городов = 8, 9-й — территориальная победа** (ТЗ 9): основание проверяется после каждого успешного основания; при достижении 9 городов у игрока всплывает модалка победы. Игра формально не блокируется — полноценного состояния «партия окончена» ещё нет.

#### 3.1.2. Воин — вторая карта (`main.ts`, данные юнитов — `units.ts`)

- Клик по карте открывает попап с одной кнопкой **⚔ Выбрать город** — режим прицела: клик по своему городу (на карте или в списке справа) открывает модалку выбора юнита. Esc до этого момента отменяет без затрат; закрытие модалки крестиком/кликом вне неё — тоже отмена (карта остаётся в руке).
- **Модалка выбора юнита** — строка на каждую из 6 категорий (Штурмовые/Поддержка/Дальняя атака/Мобильные/Оборонительные/Корабли, ТЗ 5.1). В строке — лучший из уже доступных игроку юнит этой категории (самая высокая открытая эпоха), его цена и кнопка «Построить»; если в категории ничего не открыто — пометка «не открыто» без кнопки.
- **Доступность юнита** — Воин (Штурмовые, Э1) открыт всем с начала партии без исследований. Остальные 35 — по конкретной технологии на юнит (список — `units.ts`, тег «Юниты: …» у каждой технологии в `Технологии.md`); доступен, если игрок её исследовал.
- **Цена — по эпохе юнита**, не по названию (ТЗ 7.1, число единиц в коде по умолчанию 1 там, где ТЗ называет только тип ресурса без числа):

  | Эпоха | Цена |
  |---|---|
  | 1 | 1 Еда |
  | 2 | 1 Еда + 1 Металл |
  | 3 | 1 Металл + 1 Торговый |
  | 4 | 1 Углеводороды + 1 💰 |
  | 5 | 1 Металл + 1 Углеводороды + 1 💰 |
  | 6 | 1 Металл + 1 Углеводороды + 1 Редкоземельные + 1 💰 |

  Ресурсная часть цены списывается тем же порядком доступ → склад → рынок, что и у Поселенца (3.1.1, тот же `planResourceSpend`/`commitSpend`, включая гейт добычи по технологии — Мореплавание/Горное дело); 💰 проверяется и списывается отдельно, напрямую с баланса.
- Построенный юнит появляется на гексе своего города; движения и боя нет (сделаем позже) — юнит просто стоит там до соответствующих систем.

#### 3.1.3. Научный трек и эпоха игрока (готово для Воина, реальный триггер — «Учёный», см. 3.1.8)

- У каждого игрока — набор исследованных технологий (`researchedTechs`) и эпоха = максимальная эпоха среди них (1, если ничего не исследовано). `researchTech(playerId, techId)` — механизм постановки фишки; вызывается картой «Учёный» (3.1.8), а также напрямую через `window.__debug.researchTech` для тестирования.
- **Фишка на дереве технологий** — маленький цветной кружок в левом верхнем углу узла на каждого исследовавшего игрока (несколько фишек, если исследовали несколько игроков). Подсказка узла заодно перечисляет, кто исследовал.
- **Переход в новую эпоху обновляет юниты автоматически**: как только у игрока появляется технология более высокой эпохи, все его уже построенные юниты той же категории мгновенно становятся юнитами новой эпохи (`upgradePlayerUnits`) — старый дизайн юнита не остаётся на карте.

#### 3.1.4. Рабочий (карта) и Склад (здание) — общий харвест региона

- **Доступ — только по одному городу, не по всему государству.** Это исправление задним числом коснулось и Поселенца (3.1.1), и Воина (3.1.2): изначально `planFoodSpend`/`planResourceSpend` собирали доступные ресурсы со **всех** городов игрока разом — то есть еда, лежащая нетронутой в городе Б, ошибочно позволяла расти городу А. Теперь доступ (`AccessSource`) — всегда ресурсы **одного конкретного** региона: города постройки/роста для Поселенца/Воина, целевого региона для Рабочего/Склада. Склад (как хранилище, не здание) остаётся единственным по-настоящему общим пулом, добавляется поверх доступа этого одного города.
- **Рабочий** (карта, `tryWorkerCollect`) — клик открывает выбор своего города (карта или список, режим прицела как у Поселенца/Воина). Собирает **все** ресурсы региона этого города, которые (а) ещё не добыты в этом цикле и (б) на которые есть технология добычи (см. 1.2/3.1.1 — Мореплавание/Горное дело), и кладёт их на склад. Ничего не стоит, кроме обычного действия карты; если добывать нечего — карта всё равно разыгрывается (действие потрачено впустую).
- **Склад** (здание, `trySkladCollect`) — платная альтернатива той же функции без карты: клик по уже построенному своему Складу в панели застройки (справа от него подсветка, отдельная от «можно построить») открывает диалог «Выбрать регион», дальше тот же режим прицела. Тот же харвест региона, но по **1 💰 за каждую добытую единицу** (не за тип — удвоенный ресурс стоит вдвое); при нехватке денег — полный отказ, без частичного сбора. Тратит 1 обычное действие, как и было в исходном описании здания («тратя действие»). Доступно каждый цикл, пока Склад построен и принадлежит игроку — не требует карты в руке.
- **Общий механизм зданий с функцией** (`onBuildingClick`, `BUILDING_USE_LABEL`) — клик по **своему уже построенному** зданию, у которого есть функция, открывает диалог использования вместо (бессмысленной) попытки его перестроить. Диалог ветвится на два готовых сценария: Склад (регион + деньги по количеству, см. выше) и здания-производители — ГЭС/АЭС/Фабрика (фиксированная 1 💰 + 1 действие, без выбора региона, см. 4.4) — оба списаны в одну и ту же модалку `activeModal === "building-use"`, только с разной кнопкой действия. Остальные здания при клике по своим экземплярам по-прежнему ничего не делают (эффект есть в `Технологии.md`/`buildings.ts`, но не подключён к интерфейсу).
- **Технологии удвоения** (`extractionMultiplier`) — все пять из `Технологии.md` независимы и перемножаются, если ресурс подходит под несколько сразу (например Киты — морской и торговый одновременно, Навигация + Гильдии вместе дали бы ×4):

  | Технология | Что удваивает |
  |---|---|
  | Гончарное дело | Растительная пища (Злаки, Овощи, Фрукты — не Домашние животные) |
  | Навигация | Любой ресурс, требующий воды (Рыба, Крабы/Моллюски, Киты) |
  | Индустриализация | Любой стратегический ресурс |
  | Гильдии | Любой торговый ресурс |
  | Генная инженерия | Любой пищевой ресурс (все 6 типов) |

#### 3.1.5. Строитель — реальная цена застройки (`main.ts`, цены — `buildings.ts`)

- **Столица** (`City.isCapital`) — первый город игрока, основанный в стартовой расстановке (1.3). Строитель тратит ресурсы только из региона столицы (плюс склад/рынок сверху, тот же порядок, что у 3.1.1–3.1.4) — города, основанные позже Поселенцем, столицей не становятся и для стройки не считаются.
- **Панель застройки — сама и есть «окно строительства»**, отдельного диалога с городом не открывается: клик по карте «Строитель» в руке ставит панель в режим стройки (золотая подсветка свободных зданий), следующий клик по любому свободному зданию строит его. Esc отменяет.
- **Прямой клик по свободному зданию без активной карты Строителя ничего не делает** (кроме подсказки сыграть карту) — раньше можно было построить что угодно за 1 действие без ресурсов, эта лазейка убрана вместе с реализацией карты.
- **Цена — по данным конкретного здания** (`BuildingDef.costLines`, зеркалит текстовую `cost` из 4.4/`Технологии.md`): либо N единиц одного конкретного ресурса (например 2 Металл), либо N *разных* ресурсов одного класса (например 2 разных торговых) — так же, как описано в 4.4. Списывается тем же алгоритмом доступ → склад → рынок, что у Поселенца/Воина/Рабочего (3.1.1–3.1.4), включая гейт по технологии добычи. Если план целиком не сходится — здание не строится, ничего не тратится.
- Здание с функцией использования (Склад, 3.1.4) после постройки доступно через клик по нему самому — отдельная ветка `onBuildingClick`, не через Строителя.

#### 3.1.6. Торговец (`main.ts`, `tryTraderTrade`/`tradeNetworkOf`)

- **Торговая сеть — реализовано по-настоящему** (`tradeNetworkOf(city)`, больше не заглушка): (а) **своя часть** — `ownRouteComponent()` обходит граф `tradeRoutes`, отфильтрованный на маршруты ИМЕННО этого игрока (BFS от кликнутого города); город без единого маршрута — тривиальная сеть из него одного, как в самой первой версии правила («если город 1 то только по нему»); (б) **союзная часть** — `tradeAlliesOf(playerId)` возвращает игроков с «Торговым союзом»; для каждого такого союзника его ГОРОДА ЦЕЛИКОМ добавляются в сеть, без требования связности их МЕЖДУ собой маршрутами (это разрешает исходное, ещё до-маршрутное правило «считаются только города стран с которыми есть торговое соглашение» — соглашение само по себе «мост», физический маршрут между разными владельцами и не должен существовать, раз маршрут по правилу 3.1.7 всегда строится между двумя городами ОДНОГО игрока).
- **Дипломатии всё ещё нет (ТЗ 13) — временная заглушка для «Торгового союза»**: `tradeAlliesOf()` сейчас возвращает буквально всех остальных игроков, как если бы союз был у каждого с каждым, по прямому запросу «на время реализации у всех игроков есть торговое соглашение между собою». Как только появится настоящее состояние дипломатии — заменить одну эту функцию на реальную проверку соглашения, вся сеть/пошлина ниже пересчитается сама.
- Клик по карте открывает выбор своего города (карта или список) — выбор определяет, из ЧЬЕГО города считается «своя» (route-connected) часть сети, поэтому теперь важен не только для проверки владения: два разных своих города одного игрока могут дать разные сети, если они не соединены собственными маршрутами друг с другом.
- **Уникальные торговые ресурсы сети** — объединение (а) торговых ресурсов в доступе КАЖДОГО города сети (свои и союзные), которые ещё не добыты в этом цикле и на которые у ВЛАДЕЛЬЦА ТОГО КОНКРЕТНОГО города есть технология добычи (не у играющего Торговца — у союзника может быть/не быть своя Мореплавание/Горное дело независимо), и (б) торговых ресурсов НА СКЛАДЕ ТОЛЬКО играющего игрока (склады союзников не читаются — их вклад ограничен доступом). Каждый тип считается один раз, откуда бы он ни был.
- **Доход = min(число городов сети × число уникальных типов, суммарное население сети)** — население тоже суммируется по всей сети, включая союзные города. Пример: 1 город, 3 уникальных типа, население 5 → доход 3 (не срезан). Тот же город с населением 1 → доход 1 (срезан лимитом населения).
- **Списание** — по 1 единице каждого посчитанного уникального типа: из доступа (город, чей именно — свой или союзный, помечается использованным на этот цикл), иначе со склада играющего игрока. Порядок предпочтения — доступ прежде склада, как и везде в этой цепочке.
- Если в сети нет ни одного торгового ресурса — карта всё равно разыгрывается (действие потрачено), доход 0, ничего не списывается.
- **Пошлина за союзные сети — реализовано.** Флэт **1 💰 за каждого союзника, чьи города пополнили сеть** (не за маршрут, не за товар, не за население — «за факт проезда»), уходит этому союзнику из ВАЛОВОГО дохода, прежде чем остаток зачисляется играющему игроку — списывается по порядку союзников, пока валовый доход не иссякнет (в минус не уходит: если дохода меньше, чем союзников, задние просто не получают пошлину в этот раз). Живой пример на 3 игроках, 20 городах, 20×3 сети: валовый доход 36 (сработал потолок по населению), 2 союзника получили по 1💰 пошлины, играющему досталось 34.
- **Живой тест-сценарий** (`window.__debug.seedCitiesInAllRegions()`, `main.ts`) — не часть обычной игры (обычно города появляются только через Поселенца, 3.1.1), чисто демо/тест-утилита: раскидывает по 1 городу случайному игроку в каждый обитаемый регион без города (все 20 из 20 на сгенерированной карте), со случайным населением 1-3. Использовалась, чтобы построить настоящую разветвлённую сеть (маршруты между городами каждого игрока через `findRoutePath`) и живьём проверить пошлину — без нужды десятками кликов основывать города вручную.

#### 3.1.7. Постройка торговых маршрутов (`main.ts` — `findRoutePath`/`researchTech`/`pickRouteCity`)

- **Триггер — исследование технологии с маршрутом**, автоматически, без карты: `researchTech()` проверяет `TechDef.route` (новое поле в `techtree.ts`) и, если у игрока уже есть ≥2 города, сразу запускает выбор городов (режим прицела — клик по карте или списку, как у карточных действий, только не привязан к руке). Если городов меньше 2 — маршрут просто не строится, с подсказкой об этом.
- **8 технологий, дающих маршрут**, с типом рельефа:

  | Технология | Тип | Маршрут (Технологии.md) |
  |---|---|---|
  | Колесо | суша | Сухопутный I |
  | Верховая езда | суша | Сухопутный II |
  | Железные дороги | суша | Сухопутный II (продолжение) |
  | Мореплавание | море | Морской I |
  | Компас | море | Морской II |
  | Банковское дело | универсальный | Финансовый |
  | Авиация | универсальный | Воздух |
  | Космонавтика | универсальный | Орбита |

- **Кратчайший путь, без смешения суши и моря** — `findRoutePath()` ищет через BFS (гарантированно кратчайший на невзвешенной гекс-сетке) от гекса первого города до гекса второго; для «суша» каждый промежуточный тайл обязан быть сушей, для «море» — океаном (не льдом), для «универсальный» рельеф не важен. Оба города-конца исключены из этой проверки (город физически всегда на суше, даже у морского маршрута — порт). Если пути ≤12 гексов с таким ограничением не существует — маршрут не строится вовсе (не срезается, не идёт в обход правила).
- **Максимальная длина — 12 гексов** (`MAX_ROUTE_HEXES`) — считается в шагах между городами (гекс самого города в счёт длины не идёт).
- **Отрисовка** — пунктирная линия через центр каждого гекса пути, цветом владельца, под маркерами городов/юнитов (`drawTradeRoutes()`, вызывается вместе с `drawCityMarkers()`).
- Маршрут хранится как отдельный объект (`TradeRoute`: id, владелец, технология, тип, оба города, путь) — используется пока только для отрисовки; в доход/сеть Торговца (3.1.6) не подключён, см. выше.
- **[ИСПРАВЛЕНО] «Остаточный артефакт» права прокладки маршрута перехватывал клики ДРУГОГО игрока** (по прямому запросу пользователя, живой баг-репорт). `pendingRoute` на сервере — глобальное поле на всю партию (один слот, `playerId` просто поле внутри), не привязан к текущему игроку клиента: если исследовавший маршрутную технологию игрок не успел (или не стал) выбрать 2 города до конца своего хода, состояние оставалось висеть, и клиент до этой правки реагировал на голую истинность `pendingRoute` — не сверяя `playerId` — поэтому клик ДРУГОГО игрока по городу (например, для добычи ресурса Рабочим) на следующих ходах ошибочно перехватывался в режим выбора городов маршрута и отклонялся сервером с чисто клиентской подсказкой «Первый город маршрута должен быть вашим» вместо ожидаемого действия. Сервер (`pickRouteCities`) сам по себе был не уязвим — он и раньше проверял `pendingRoute.playerId !== playerId`; баг был чисто клиентский. Исправлено — новый `pendingRouteIsMine()` (`main.ts`) заменил все проверки голой истинности `pendingRoute` (клик по карте, клик по списку городов, подсветка «growable», подсказка в hint-баре, ESC-отмена) — теперь чужой незавершённый `pendingRoute` не влияет на UI других игроков вообще, только на того, кому он реально принадлежит.

#### 3.1.8. Учёный — последняя из 6 карт действий (`main.ts` — `availableResearchFor`/`confirmResearch`/`researchTech`)

- Клик по карте открывает попап с одной кнопкой **🔬 Открыть технологию** — в отличие от Поселенца/Воина/Рабочего/Строителя/Торговца, у неё нет отдельного шага прицеливания по карте/городу: кнопка сразу открывает модалку выбора технологии (открытие не привязано к месту).
- **Модалка** — от нуля до нескольких строк на КАЖДУЮ из 4 веток (не строго одна): каждая ещё не исследованная технология этой ветки от самого начала до текущей общей границы ветки включительно (название, эпоха, статус «новая технология» / «уже открыта другими», кнопка «Открыть») — если игрок отстал сразу на несколько позиций, все они предлагаются одновременно, любую можно наверстать первой. Ветка, где играть нечего (все технологии до границы уже свои, или технологии кончились), строк не показывает. Закрытие крестиком/кликом вне модалки — отмена, карта остаётся в руке.
- **Групповая граница ветки, БЕЗ бесплатной раздачи (`branchGroupDepth`/`availableResearchFor`/`techDiscoverer`)** — не реализация «кубика лидерства» из 4.2 в буквальном смысле (никакой фиксированной роли «лидер ветки» нет), и не автоматическая раздача (была промежуточная версия — убрана, см. ниже): технология на позиции N ветки становится ДОСТУПНОЙ ДЛЯ ИССЛЕДОВАНИЯ (за карту+действие+ресурсы, как обычно, НЕ бесплатно) любому игроку, если позиция N−1 той же ветки уже исследована КЕМ УГОДНО (`branchGroupDepth` — длина цепочки позиций подряд от начала ветки, коллективно закрытых кем угодно, каждая позиция может быть закрыта разными игроками). Игроку предлагается КАЖДАЯ технология от начала ветки до `branchGroupDepth` включительно, которой у него лично ещё нет (может получиться несколько строк на одну ветку сразу). Живой пример: если кем угодно уже открыты «Бронзовое дело», «Колесо» и «Каменная кладка» (позиции 0 веток 1/2/3), то любому исследующему уже доступны «Горное дело», «Письменность» и «Мистицизм» (позиции 1 тех же веток) — а «Гончарное дело» (позиция 1 ветки 4) НЕ доступно, потому что «Мореплавание» (позиция 0 ветки 4) пока никем не открыто. **Технологию можно переоткрыть повторно** — второй, третий и т.д. игрок получает её обычные эффекты (юнитов, здания), но НЕ бонус первооткрывателя: `techDiscoverer[techId]` фиксирует, кто исследовал технологию ЛИЧНО первым во всей партии, и только для него срабатывают авто-прокладка торгового маршрута (3.1.7) и право основать религию (4.5) — у повторных исследователей эти два эффекта просто не происходят. История модели: сперва была ролевая «лидер/отстающий» с потолком «глубина лидера − 1» (`branchLeaderDepth`/`isBranchLeader`/`canAdvanceBranch`), затем «глубина лидера включительно», затем «бесплатная автоматическая раздача техов ниже границы» (`syncBranchCatchup`) — все три подхода удалены по последовательным уточнениям; текущая модель («доступно для платного исследования, если предыдущая позиция открыта кем угодно, без бесплатной раздачи и без ролей») зафиксирована как финальная.
- **Досрочное открытие следующей эпохи (`branchesTouchedInEpoch`/`maxEligibleEpoch`)** — обычно эпоха N+1 стала бы доступна только после того, как эпоха N закрыта целиком (обе технологии всех 4 веток, 8 штук). Вместо этого: как только **любой** игрок партии (не обязательно тот, для кого сейчас проверяется) имеет **хотя бы одну** исследованную технологию эпохи N в 3 или 4 из 4 веток, эпоха N+1 открывается сразу для ВСЕХ игроков — даже если эпоха N ещё не закрыта полностью в остальных ветках. По прямому уточнению («не нужно одному игроку открывать всю ветку [эпохи] самому, учитываются открытия других игроков») порог общий на партию, не индивидуальный — считается по каждой эпохе последовательно (эпоха N+2 доступна только вслед за N+1 тем же правилом).
- **Розыгрыш** — выбор технологии в модалке сразу тратит карту и действие (`consumeHandCard`) и вызывает `researchTech()` — тот же путь, что и раньше был доступен только через `window.__debug.researchTech`: авто-апгрейд юнитов при переходе в новую эпоху (3.1.3) и авто-запуск прокладки маршрута, если технология его даёт (3.1.7), срабатывают как обычно.
- **Цена по эпохе (4.3) списывается** (`RESEARCH_COST_LINES`, `planBuildingSpend`/`commitSpend`) тем же порядком доступ → склад → рынок, что и у остальных карт. **Доступ берётся со ВСЕХ городов игрока**, а не только со столицы — по прямому запросу («ресурс есть в регионе, а пишет нет ресурсов»): само исследование не привязано к месту (в отличие от стройки здания «в столице», 3.1.5), поэтому прежнее ограничение столицей было произвольным и создавало ловушку — у столицы с населением 1 доступен ровно 1 тип ресурса за цикл (лимит населения, 7.2), а богатые регионы остальных городов игрока просто не считались. То же правило действует и у Университета (4.4), который отличается только доплатой 5💰.

### 3.2. Карты событий (6 типов, по 2 шт. = 12 карт)

Эти карты нельзя сбросить (продать на рынке через попап руки) — реализовано (см. 3.2.1: `cardChoiceHtml` показывает строку продажи только для `card.kind === "action"`). Если они попадают в руку, их эффект разыгрывается либо когда игрок сам решает сыграть карту (позитивный эффект, если он есть), либо — если карта не сыграна и уходит в вынужденный сброс за перебор руки (2.3.1) — тогда срабатывает её негативный эффект. Это НЕ то же самое, что «условия», упомянутые в раннем черновике этого раздела — отдельного «наступления условий» вне вынужденного сброса пока нет ни у одной карты.

| Карта | Эффект |
|---|---|
| Население | **Реализовано** (см. 3.2.1) — играется как рост населения у Поселенца; при вынужденном сбросе −1 населения всем городам. |
| Соберите налоги | **Реализовано** (см. 3.2.2) — +1💰 за население, −1💰 за юнит/здание; недоимка списывается юнитами/зданиями. |
| Катастрофа | **Реализовано** (см. 3.2.3) — заплатить 2 Силикаты или потерять случайное здание/население города. |
| Рост леса | **Реализовано** (см. 3.2.4) — заплатить 2 пищевых и посадить лес на своей территории; при вынужденном сбросе — вырубка/опустынивание. |
| Торговый путь | **Реализовано** (см. 3.2.5) — заплатив 2 торговых, перенаправить любой путь на карте или оставить как есть; при вынужденном сбросе — обнуление денег/склада. |
| Мобилизация | **Реализовано** (см. 3.2.6) — 10💰 за неограниченные действия и здания в этот ход; при вынужденном сбросе — пропуск следующего хода. |

#### 3.2.1. Население — первая полностью реализованная карта событий (`main.ts` — `tryGrowCity`/`resolveHandOverflowDiscard`)

- **Позитивная ветка — играется игроком, как рост населения у Поселенца, и только он** (`pendingCardAction.kind === "population-grow"`, тот же `tryGrowCity()`, что и у `settler-grow` — функция теперь обслуживает оба режима прицела одним и тем же кодом). Ветки «основать город» нет вообще — попап карты в руке показывает единственную кнопку «👥 Увеличить население», без строки «Продать за:» (событийные карты не продаются). Цена и порядок списания — буквально те же, что у Поселенца (3.1.1): N разных видов пищи, N = население города, доступ → склад → рынок.
- **Негативная ветка — только через вынужденный сброс за перебор руки (2.3.1), не «если не сыграна к какому-то сроку»**: если карта «Население» не сыграна и попадает в тот сброс (рука ≥8 в конце хода), населению всех городов игрока −1 (не ниже 1). В отличие от 5 карт действий в той же таблице 2.3.1, этот эффект **не отменяется Рабочим** — присутствие Рабочего в том же сбросе нейтрализует только действия (settler/warrior/scientist/builder), карты событий разыгрывают свой эффект безусловно (реализовано отдельной веткой в `resolveHandOverflowDiscard()`, до проверки `neutralized`).
- Розыгрыш проверен живьём в обе стороны: позитивная ветка (клик по карте → рост населения → карта уходит на дно колоды), и негативная (2 карты «Население» в вынужденном сбросе дали −2 населения, при этом Рабочий в том же сбросе нейтрализовал соседний Поселенец-эффект, но не тронул «Население»).

#### 3.2.2. Соберите налоги — вторая реализованная карта событий (`main.ts` — `collectTaxes`/`removeForTaxShortfall`)

- **Единый эффект вне зависимости от того, как карта уходит из руки** — в отличие от Населения (3.2.1), у этой карты нет отдельной «позитивной» и «негативной» ветки: и добровольный розыгрыш, и вынужденный сброс за перебор руки (2.3.1) считают ровно одно и то же (`collectTaxes()`), разница только в том, как покрывается недоимка (см. ниже). Карту нельзя сбросить/продать — но раз её эффект и так безусловно срабатывает при любом уходе из руки, это не создаёт для неё никакого «зависания», в отличие от гипотетической карты без такого запасного пути.
- **Доход**: +1💰 за каждую единицу населения ИГРОКА (сумма по всем его городам, `totalPopulationOf`).
- **Содержание**: −1💰 за каждый юнит игрока + за каждое здание, которым он владеет (`builtBy`, с учётом правила «до 2 владельцев», 4.4 — считаются оба, если оба свои... то есть если этот игрок один из двух). Списывается из денег ПОСЛЕ начисления дохода (доход и старые деньги — общий котёл на оплату).
- **Если денег хватает** — просто списывается содержание, никаких дальнейших шагов.
- **Если не хватает — недоимка гасится юнитами/зданиями, а не остаётся долгом**: `недоимка = содержание − доступные деньги`, деньги обнуляются, и ровно `недоимка` активов (в любой комбинации юнитов и зданий) должны быть списаны.
  - **При добровольном розыгрыше — выбирает игрок**: открывается модалка (`activeModal === "tax-shortfall"`) со списком всех своих юнитов и зданий, каждое — с кнопкой «Списать»; счётчик «осталось списать N» в заголовке. Модалку нельзя отменить закрытием — состояние `pendingTaxShortfall` не привязано к `pendingCardAction`/`cancelPendingCardAction`, крестик просто прячет окно, недоимка никуда не девается, пока не будет списано ровно N активов (или пока не кончится, что списывать — тогда снимается автоматически).
  - **При вынужденном сбросе (2.3.1) — без выбора, автоматически**: юниты списываются первыми (новые — раньше, та же политика, что у Воина в той же таблице), затем здания (порядок среди зданий не важен, первое найденное). Модалку показать негде — упрощение, явно принятое как компромисс.
- Проверено живьём во всех трёх режимах: (1) денег хватает — просто списание, без модалки; (2) недоимка при добровольном розыгрыше — модалка открылась с верным списком и счётчиком, ручное списание довело до 0 и закрыло модалку; (3) недоимка при вынужденном сбросе — автосписание юнитов, затем здания, до полного покрытия, без модалки.
- Остальные 4 карты событий по-прежнему не реализованы.

#### 3.2.3. Катастрофа — третья реализованная карта событий (`main.ts` — `resolveCatastrophe`/`applyCatastropheLoss`)

- **Эффект всегда против того игрока, который её разыгрывает** (сам сыграл, или принудительно при сбросе, 2.3.1) — не случайный игрок, только он сам, как явно уточнено.
- **Развилка «заплатить или принять» — одинаковый порядок и при добровольном розыгрыше, и при вынужденном сбросе**, только способ выбора разный:
  - **Добровольный розыгрыш** — открывает модалку (`activeModal === "catastrophe-choice"`) с 2 кнопками: «Заплатить 2 Силикаты» (недоступна и подписана «не хватает», если у игрока их меньше 2 на складе) и «Принять последствия». Модалку нельзя отменить закрытием крестиком с тем же эффектом, что не отвечать — `pendingCatastrophe` не привязан к `pendingCardAction`, крестик просто прячет окно, выбор всё ещё предстоит сделать.
  - **Вынужденный сброс (2.3.1)** — без модалки: платит автоматически, если на складе есть 2 Силиката, иначе автоматически принимает последствия.
- **Оплата — 2 Силиката прямо со склада**, без цепочки доступ → склад → рынок, как у цены карты/здания: это разовая проверка возможности, а не покупка (`canAvertCatastrophe`). Если заплатил — эффекта вообще никакого, кроме траты 2 Силикатов.
- **Последствия при отказе/невозможности заплатить** (`applyCatastropheLoss`):
  1. Если у игрока есть хоть одно здание — теряется одно случайное из них (`Math.random()`, владение снимается тем же способом, что и при недоимке налогов, 3.2.2).
  2. Если зданий нет — случайный ЕГО ЖЕ город теряет 3 населения.
  3. Если у этого случайного города населения было меньше 3 — город исчезает полностью (`cities.splice`), а не просто падает в 0/отрицательное значение.
- Проверено живьём во всех 4 исходах: успешная оплата (склад −2 Силиката), потеря случайного здания (владение снято), потеря 3 населения у города (5 → 2), полное исчезновение города (население было 2 — города не стало).
- Остальные 3 карты событий по-прежнему не реализованы.

#### 3.2.4. Рост леса — четвёртая реализованная карта событий (`main.ts` — `tryPlantForest`/`degradeForestOrLand`)

- **Та же двухветочная форма, что у Населения (3.2.1)** — позитив только при добровольном розыгрыше, негатив только при вынужденном сбросе (2.3.1), никогда наоборот и никогда одновременно.
- **Позитивная ветка** — режим прицела как у Поселенца-основателя (`pendingCardAction.kind === "forest-plant"`, клик по гексу карты вместо города): платит 2 пищевых ресурса (`planFoodSpend(..., requireDistinct=false)` — тот же режим, что у основания города, разные виды НЕ обязательны, в отличие от роста населения) и сажает лес на выбранном гексе. Ограничения на гекс: (а) должен лежать в регионе, где у ИГРОКА есть свой город («своя территория» — любой из его регионов, не только столица), (б) рельеф — Равнина или Холмы (`TERRAIN_BY_ID[...].canHaveForest`), (в) леса там ещё нет. Доступ для цены сцеплен с городом того самого региона (тем же способом, что и everywhere else — один город, не пул по всем).
- **Негативная ветка** — без интерактивности (в вынужденном сбросе показать модалку прицела негде): сначала пытается вырубить случайный лесной гекс на территории игрока (по всем его регионам, `degradeForestOrLand()`); если леса на территории нет вообще — превращает случайный гекс Равнины в Пустыню, и если на нём был пищевой ресурс — тот исчезает с карты насовсем (`doc.set(..., {resource: undefined})`). Если нет ни леса, ни равнины на территории — эффект пропадает впустую («деградировать было нечего»).
- Осознанно НЕ переиспользует `growRandomForest()` (эффект сброса Строителя, 3.1.4/2.3.1) — та функция работает по всей карте без привязки к владельцу; здесь по условию именно «своя территория», поэтому обе функции (посадка и деградация) сканируют только регионы с городами конкретного игрока.
- Проверено живьём: посадка на выбранном гексе (с корректным списанием денег через рыночный фолбэк), отказ при клике вне своей территории и повторный отказ при клике по уже занятому лесом гексу (карта остаётся в руке в обоих случаях), вырубка случайного леса до полного исчерпания территории, переход на опустынивание равнины, и исчезновение ресурса «Злаки» с превращённого в пустыню гекса.
- Остальные 2 карты событий по-прежнему не реализованы.

#### 3.2.5. Торговый путь — пятая реализованная карта событий (`main.ts` — `pickRedirectCity`/`applyTradeRouteNegative`)

- **Позитивная ветка — не одно действие, а выбор из двух** (попап карты в руке показывает 2 кнопки): либо **перенаправить** какой-то маршрут, либо **оставить как есть**, доплатив те же 2 торговых ресурса — второй вариант явно добавлен по уточнению («игрок имеет право оставить прежний маршрут»), обе кнопки одинаково «разыгрывают» карту (снимают с руки, платят ресурс), различие только в том, меняется ли сам маршрут.
- **Перенаправление — маршрут ЛЮБОГО игрока, не только своего** — прямо по формулировке («любой город на карте, в который идёт торговый путь»), без ограничения владельца. Режим прицела в 2 клика, отдельное состояние `pendingRouteRedirect` (не часть `PendingCardAction`, тот же приём, что у `pendingRoute`, 3.1.7 — не привязано к одному конкретному виду цели):
  1. Клик по городу (карта или список) — должен быть текущим концом КАКОГО-ТО маршрута (чей угодно); определяет, какой маршрут и какой именно его конец затрагивается — второй (нетронутый) конец и владелец фиксируются.
  2. Клик по НОВОМУ городу — должен принадлежать ТОМУ ЖЕ владельцу маршрута (не играющему картой игроку!) и быть отличным от обоих текущих концов — маршрут не может связывать 2 чужих игроков или самого себя дважды (то же правило «оба конца — один игрок», что и при постройке, 3.1.7). Путь ищется заново (`findRoutePath`, тот же тип рельефа/категория, ≤12 гексов); **если не находится — маршрут НЕ исчезает, а просто остаётся как был** (в отличие от Торговца, 3.1.6/2.3.1, у которого в этом же случае маршрут удаляется — разные карты, разное правило, обе зафиксированы явно) — карта и 2 ресурса при этом всё равно потрачены, попытка засчитана, просто без результата.
- **Оплата отложена до фактического разрешения**, а не списывается сразу при открытии режима прицела: если игрок отменит Esc'ом на середине пути (после 1-го клика), ничего не тратится и карта остаётся в руке — `cancelPendingCardAction()` просто чистит `pendingRouteRedirect`, никакого возврата ресурсов не нужно, потому что ничего ещё не списано. Платёж (2 торговых ресурса, любых, необязательно разных — как и у «Оставить как есть») происходит в момент подтверждения нового города (или сразу для «оставить как есть»), напрямую со склада (`spendTradeResourcesFromWarehouse`), без цепочки доступ → склад → рынок — та же логика упрощения, что у 2 Силикатов в Катастрофе (3.2.3).
- **Негативная ветка** — та же двухветочная форма, что у Населения/Роста леса, только через вынужденный сброс: обнулить деньги игрока; если денег и так не было — опустошить склад целиком; если ни того ни другого — эффекта нет вообще («терять нечего»).
- Проверено живьём: перенаправление чужого (другого игрока) маршрута на новый город того же владельца с реальным пересчётом пути, попытка перенаправить на недостижимый город (>12 гексов) — маршрут остался байт-в-байт неизменным, но карта и 2 ресурса всё равно списаны, «оставить как есть» с корректным списанием 2 ресурсов и без изменения маршрута, отмена Esc на середине без каких-либо потерь, и все 3 исхода негативной ветки (деньги → 0, склад → пусто, «нечего терять»).

#### 3.2.6. Мобилизация — шестая и последняя реализованная карта событий (`main.ts` — `startMobilization`/`applyMobilizationNegative`)

- **Позитивная ветка — снимает лимит действий на весь текущий ход**: платит 10💰 и ставит `actionsLeft[playerId] = Infinity`. Само по себе это не может «протечь» на следующие ходы — `onPlayingEndTurn()` безусловно сбрасывает `actionsLeft` обратно к `ACTIONS_PER_TURN` для того, чей ход заканчивается, так что дополнительного «выключателя» не требуется. `renderActionPips()` (3 фишки максимум по дизайну) корректно рисует все 3 как заполненные при `Infinity`, не пытаясь нарисовать «бесконечно много» — не баг, просто визуальный потолок в 3 фишки при факте «сколько угодно».
- **И здания — по уточнению («и все здания (но по разу) тоже»)**: для каждого здания, которым владеет игрок, если оно уже отмечено использованным в этом цикле (`productionUsedThisCycle` — ГЭС/АЭС/Фабрика/Радиовышка), флаг снимается — это даёт ровно ОДНУ дополнительную активацию за цикл, не безлимит: как только здание использовано повторно, тот же флаг взводится обратно и здание снова заблокировано до конца цикла. Здания без такого флага (Казарма, Склад-харвест и т.д.) этим уточнением не затронуты — им и так ничего не мешало срабатывать несколько раз за ход при наличии действий, только предыдущий лимит действий (сейчас снятый) их сдерживал.
- **Негативная ветка — только через вынужденный сброс (2.3.1)**: помечает игрока во `skippedTurn` (глобальный `Set<playerId>`). `onPlayingEndTurn()` после продвижения `currentPlayerIndex` прогоняет цикл `for` (с защитой от зависания на PLAYERS.length итераций, на случай если помечены все) — пока новый текущий игрок помечен, снимает пометку и передаёт ход ещё на одного дальше, так что пропущенный игрок вообще никогда не становится текущим (не рендерится его рука/подсказка, ход просто прыгает через него). Флаг одноразовый — снимается в момент пропуска.
- Проверено живьём: неограниченные действия (все 3 фишки остаются заполненными после трат), повторная активация ГЭС, уже использованной в этом цикле (сработала один раз, вторая попытка сразу же отказана), и реальный пропуск хода через игровой цикл — «Завершить ход» у игрока 1 перевело подсказку сразу на «Игрок 3», игрок 2 (помеченный) был пропущен целиком, а не просто обнулил себе действия.
- Все 6 карт событий (3.2) теперь реализованы — это закрывает и весь раздел 3.2, и весь набор из 36 карт колоды (24 действия + 12 событий, ТЗ 3).

---

## 4. Технологическое дерево — актуальная структура

Полный список всех 48 технологий, их эффектов, зданий и цен — в `Технологии.md` (авторитетный источник дизайна, обновляется вручную). Данные для клиента — `web/src/game/techtree.ts`, синхронизируются с `Технологии.md` тоже вручную (это заведомо дублирование, не единый источник правды — при правке одного проверять другое).

### 4.1. Структура дерева

- **48 технологий: 4 ветки × 6 эпох × 2 технологии на эпоху.**
- Ветки — **чисто позиционная структура для механики лидерства** (см. 4.2), не тематическая. Ранний черновик называл их «Военная / Экономическая / Культурная / Научная» — на практике это не подтвердилось: реальные технологии внутри одной ветки относятся к разным категориям, сгруппированы по историческому первенству (какие 2 открытия человечества пришлись на одну эпоху), а не по общей теме.
- Смысловая категория технологии — отдельное измерение, не привязанное к ветке: 🏙 архитектура/город, 🚢 логистика, ⚔️ характер войны, ⚖️ политика/социум, 🗣 дипломатия/коммуникации, ⚙️ производство, ☯ религия/философия. В клиенте категория красит и подписывает узел иконкой, а не колонку.
- 8 технологий (по 2 на ветку, в разных эпохах) помечены 🔓 **«уникальная»** — доступна только лидеру ветки (см. 4.2).
- 11 технологий дают здание (🏛, после переработки 4.4 — одна из них, **Атомная энергия**, сразу два: АЭС и Ядерный арсенал), 8 дают торговый маршрут (Колесо, Верховая езда, Мореплавание, Компас, Банковское дело, Железные дороги, Авиация, Космонавтика — каждая ровно 1 маршрут), 6 задают политическую парадигму (Монотеизм / Монархия / Парламентаризм / Демократия / Фашизм / Коммунизм), 6 открывают дипломатические соглашения (Открытые границы, Вассалитет, Торговый союз, Совместная оборона, Научное сотрудничество, Союз).
- Пустых технологий (без эффекта) нет — все 48 определены. Полный список открытых вопросов (частично определённые эффекты, если появятся) — раздел «Сводка пустых мест» в `Технологии.md`.

### 4.2. Кубик лидерства

- На каждой технологической ветке лежит шестигранный кубик (цвета соответствуют игрокам).
- Кубик показывает, кто лидирует в данной ветке.
- Лидер — единственный, кто имеет доступ к самой последней (самой мощной) технологии в этой ветке — то есть к 🔓-уникальным технологиям (см. 4.1).
- Остальные игроки знают все предыдущие технологии ветки (на шаг позади от лидера). Они получают доступ к новой технологии только когда лидер продвигается дальше.
- **Реализовано** через карту «Учёный» (3.1.8, `branchDepth`/`branchLeaderDepth`/`canAdvanceBranch`) — без выделенного объекта «кубик», но с эквивалентной механикой: лидер ветки = у кого в ней больше всего исследованных технологий сейчас (пересчитывается на лету, а не хранится отдельно), остальные жёстко ограничены потолком «глубина лидера − 1». Единственное реально не подключённое место — соглашение «Научное сотрудничество» не даёт своим участникам уровня лидера без отставания, потому что дипломатии между игроками в клиенте вообще нет.

### 4.3. Стоимость и условия открытия технологий

Цена зависит только от эпохи — все технологии одной эпохи стоят одинаково, растёт с 1 ресурса в Э1 до 4 в Э4–Э6 (тот же потолок, что у зданий, см. 4.4). Источник — раздел «Цена открытия технологии» в `Технологии.md`, данные — `EPOCH_RESEARCH_COST` в `web/src/game/techtree.ts`.

| Эпоха | Цена |
|---|---|
| 1 | 1 Пищевой |
| 2 | 1 Пищевой + 1 Стратегический |
| 3 | 1 Пищевой + 1 Стратегический + 1 Торговый |
| 4 | 1 Пищевой + 2 разных Стратегических + 1 Торговый |
| 5 | 1 Пищевой + 2 разных Стратегических + 1 Электричество |
| 6 | 1 Пищевой + 1 Редкоземельные + 1 Уран + 1 Электричество |

«Стратегический»/«Торговый» без уточнения — класс (любой ресурс класса, без указания конкретного вида, как у цены зданий в 4.4); Электричество/Редкоземельные/Уран — конкретный ресурс. Э5 и Э6 требуют Электричество — ресурс только от здания (ГЭС/АЭС, см. 4.4, теперь реально производится по клику), поэтому исследования этих эпох по дизайну невозможны без собственной электростанции. Цена по эпохе всё ещё не списывается в коде (3.1.8) — это ограничение сейчас не проверяется никак.

Дополнительное условие (пока не пересмотрено вместе с ценой выше, из раннего черновика): у игрока должен быть город соответствующего уровня (уровень города не ниже номера эпохи).

Как и 4.2 — пока не реализовано в клиенте: ничего в коде эту цену не списывает, исследование технологий по игрокам не отслеживается.

### 4.4. Городская застройка (здания)

16 зданий, 4 группы по 4: ⚔️ Военные, 💰 Экономические, ⚙️ Промышленные, 🗣 Социальные. Полный список с ценами и эффектами — раздел «Городская застройка» в `Технологии.md`; данные и владение — `web/src/game/buildings.ts`.

**Статус на 2026-08-29: этот раздел описывает актуальный ДИЗАЙН после большой переработки зданий (см. таблицу ниже). В коде реализованы ГЭС/АЭС/Фабрика/Склад (как было раньше), правило «до 2 владельцев» (`buildingOwners: Record<string, number[]>`, диагональная 2-цветная заливка в панели застройки), и теперь ТАКЖЕ тех-гейтинг 4 бывших «стартовых» зданий (Казарма/Фортификация/Склад/Управление — см. следующий пункт, раньше в коде у них так и стоял `tech: null`, отчего панель подсвечивала их доступными с самого начала партии вопреки уже написанному здесь дизайну — исправлено) — конкретные ещё не существующие новые здания (Аэропорт, Ядерный арсенал вместо Военного министерства, Рынок, Радиовышка, новый Космодром, Храм, новый Университет, Интернет) по-прежнему НЕ закодированы, это техзадание на следующий шаг.** Сверено с пользователем 2026-08-30 (после серверной миграции, см. §8/раздел про сервер) — расхождение подтверждено, реализация начата тем же днём (переделка 5 зданий + Университета/Интернета под новый эффект, «Контент», накопление ядерного оружия у Ядерного арсенала как счётчик БЕЗ механики применения — сама механика удара по гексам осталась отдельной будущей задачей, см. пункт про Ядерный арсенал выше).

- **Все 16 зданий теперь открываются технологией** — раньше 4 (Казарма, Фортификация, Склад, Управление) были доступны с начала игры без исследований; распределены по свободным эпохам, чтобы заполнить перекос из таблицы «Итого зданий по эпохе» ниже (было Э2 — 0 зданий, Э1/Э4 — по 1): **Казарма → Бронзовое дело (Э1)**, **Фортификация → Феодализм (Э2)**, **Склад → Стандартизация (Э2)**, **Управление → Всеобщая воинская повинность (Э4)** — последняя как раз только что осталась без здания (Ядерный арсенал переехал на Атомную энергию, см. ниже), так удобно заняла её вместо новой дыры.
- **Здание может принадлежать одновременно ДО 2 игрокам — реализовано** (изменено — раньше было «только одному за партию»). Каждый из двух владельцев получает собственный, независимый экземпляр (свой цикл активации, отдельный флаг «уже использовано в этом цикле», и т.д.) — это не общая постройка на двоих. Как только 2 разных игрока построили здание, оно недоступно всем остальным до конца партии, даже если у них тоже открыта нужная технология; один и тот же игрок по-прежнему не может иметь 2 экземпляра одного здания. Модель данных: «id здания → до 2 id игроков» (`BuildingOwners = Record<string, number[]>`, `MAX_BUILDING_OWNERS = 2`, `buildings.ts`) — `ownersOf`/`isTaken`/`isOwnedBy`/`claimBuilding`/`builtBy` все читают/пишут список, не одно число. `productionUsedThisCycle` (main.ts) держит ключ `` `${buildingId}:${playerId}` ``, а не просто id здания — иначе 2 владельца одного здания делили бы один флаг «уже использовано». Панель застройки (11.4) красит клетку с 2 владельцами диагональным градиентом (`linear-gradient(135deg, colorA 50%, colorB 50%)`, инлайновый `style`, побеждает класс с одним `--pc`), с 1 владельцем — как раньше, сплошной заливкой.
- Цена постройки любого здания — ровно **4 ресурса на руках**: всегда 1–3 Силикаты, остаток — либо конкретный стратегический ресурс (2–3 единицы одного типа), либо N *разных* ресурсов одного класса — торговых или пищевых, без указания конкретного вида (например «2 разных торговых ресурса», а не «2 Хлопка»). Списывается картой «Строитель» (3.1.5) — доступ → склад → рынок, из региона столицы. Это цена ПОСТРОЙКИ; у большинства зданий есть отдельная цена АКТИВАЦИИ (см. таблицу ниже) — не путать одно с другим.
- **Победных зданий теперь три условия, всё ещё вместе с территориальным (см. 9):**
  - **Космодром** (технология Космонавтика, Э6) — переработан: победа не «постройка и запуск ракеты», а **сбор 3 компонентов корабля**. Компонент производится активацией здания (см. таблицу) за ресурсы, накопительно за всю партию (не за цикл — ограничения «раз в цикл» для Космодрома нет). Кто первым набрал 3 компонента — побеждает через космос.
  - **Технология Космонавтика также даёт защиту от ядерного оружия** (новое, привязано к Ядерному арсеналу ниже): у любого игрока, исследовавшего Космонавтику, — **50%** шанс перехвата ядерного удара по себе; у **лидера той же ветки, где лежит Космонавтика** (в данных `branch: 1`, в интерфейсе подписана «Ветка 2» — `isBranchLeader(playerId, 1)`, см. 4.2/3.1.8, та же механика лидерства, что и у остальных технологий, отдельного объекта не заводим) — **100%**, то есть неуязвимость. Вероятность — на срабатывание перехвата, а не гарантия; сам обмен ударами (кто по кому бьёт) зависит от боевой системы (раздел 6), которой в клиенте ещё нет, так что пока это тоже чисто дизайн-намерение, не за что зацепиться в коде.
  - **ООН** (технология Права человека, Э5) — простая модель голосования, описанная здесь исторически (раз в цикл голосование среди игроков, большинство в поддержку владельца завершает партию его победой), **заменена по прямому запросу пользователя моделью кандидатов/генсека/резолюций — см. 15.3.** Текущий код после этой сессии реализует именно эту устаревшую простую модель (`useOon`/`voteOon`/`pendingUnVote`) и потребует переработки под 15.3.
  - **Применение ЯО — теперь описано подробно** (по прямому запросу, дополняет пункт про Ядерный арсенал выше — «предмет пока без применения»; сама постройка Ядерного арсенала и бой всё ещё не перенесены в код, см. «Статус» вверху раздела, зафиксировано, не реализуется в рамках этой сессии):
    - Бьёт по гексу, куда упала бомба, И по всем 6 соседним гексам сразу, разными эффектами.
    - **Гекс попадания**: урон 10 ВСЕМ юнитам на клетке (включая резерв гарнизона города), независимо от владельца и от того, воюют ли стороны между собой (не действует обычное правило «нужна война для урона»). Население города −3 (если после этого ≤0 — город уничтожен). Все постройки города уничтожаются. Рельеф превращается в Пустыню. Ресурс на клетке исчезает с карты. Лес исчезает.
    - **Соседние гексы (6 штук)**: урон юнитам 5, население −1. Лес исчезает. Рельеф НЕ меняется, постройки НЕ уничтожаются, ресурс НЕ пропадает — только эти два эффекта (урон + убыль населения) и лес.
    - Анимация взрыва должна быть эффектной/зрелищной (в духе уже реализованной анимации дальних атак, 6.8, — тот же принцип отдельного Pixi-слоя `fxLayer`, не завязана на состояние партии).
- Электричество/Промтовары/**Контент** — три building-only ресурса, которых нет на карте (ГЭС/АЭС/Фабрика — Электричество/Промтовары уже в коде; **Контент — новый, от Радиовышки, не реализован**): не `ResourceId` карты и не в `warehouse`, отдельное хранилище `buildingResources`, чтобы не попасть в категорийную логику food/strategic/trade и в посев мирового рынка. Контент — кино, музыка и подобное — торговый ресурс по смыслу (тег/категория при реализации), но не с карты, только от Радиовышки, так же монопольный, как Промтовары у Фабрики.
- **Общая механика активации здания с функцией** (`onBuildingClick`, диалог `activeModal === "building-use"`) делится на несколько схем в зависимости от здания — раньше было только 2 (Склад и «производство за цикл»), теперь 5:
  1. **Склад** — 1 действие + 1💰 за КАЖДУЮ добытую единицу (любого типа) выбранного региона, где есть поселение (не один ресурс на выбор, а весь регион целиком, как у карты «Рабочий», см. 3.1.4). Реализовано.
  2. **«Производит N за цикл»** (ГЭС/АЭС/Фабрика — реализовано; Радиовышка — не реализовано, тот же паттерн) — фиксированная **1 💰** + 1 действие, не больше 1 раза за цикл на экземпляр здания (`productionUsedThisCycle`, сбрасывается при возврате хода к игроку 0). Радиовышка — та же схема, тот же `buildingResources`, что у Электричества/Промтоваров: +3 единицы **Контента** (новый building-only ресурс — кино, музыка и подобное, а не 3 случайных ресурса с карты).
  3. **Ресурсное производство без денег, без лимита цикла** (Ядерный арсенал, Космодром — не реализовано) — платится не 1💰, а конкретные ресурсы карты (доступ → склад → рынок, из региона столицы, как у Строителя/Учёного), плюс 1 действие; можно активировать сколько угодно раз за цикл, если хватает ресурсов и действий.
  4. **Разовая сделка за деньги** (Рынок, Управление, Университет, Интернет — не реализовано) — своя цена у каждого, без завязки на «цикл» вообще: Рынок продаёт 1 ресурс склада за деньги, Управление покупает +1 действие за деньги, Университет и Интернет платят деньгами за технологический эффект (см. ниже).
  5. **Сжечь карту → доход** (Храм — не реализовано) — 1 действие, ни денег, ни ресурсов на входе: сжигает 1 любую выбранную карту действия из руки (обычный сброс на дно колоды, БЕЗ побочных эффектов из таблицы 2.3.1 — те привязаны именно к штрафному сбросу за перебор, не к любому способу отправить карту в колоду) и конвертирует её в **+1💰 за каждый город любого игрока, чья страна приняла эту религию** (см. таблицу ниже — зависит от ещё не спроектированной системы принятия религии/парадигмы, см. 13).
- **Полная сверка цены активации по всем 16 зданиям** (сделано по прямому запросу — где цены не было даже в старом дизайне, отмечено отдельно):

  | Здание | Группа | Технология | Цена активации | Что даёт |
  |---|---|---|---|---|
  | **Казарма** | ⚔️ Военные | **Бронзовое дело, Э1** (было — с начала игры, без исследования) | 1 действие + обычная цена юнита по эпохе (7.1/3.1.2), без карты | Строит 1 Воина в любом своём городе |
  | **Фортификация** | ⚔️ Военные | **Феодализм, Э2** (было — с начала игры, без исследования) | Нет активации — пассивный бонус | Бонус обороны, растёт по эпохам (нечем пока подтвердить — боевой системы нет) |
  | **Ядерный арсенал** | ⚔️ Военные | **Атомная энергия, Э5** (было Всеобщая воинская повинность/Э4 — тематически логичнее рядом с АЭС/атомной темой) | 1 действие + 2 Уран + 1 Металл, без денег, без лимита цикла | +1 ядерное оружие (предмет пока без применения — нет боя/системы целей). См. также защиту от ядерного оружия у Космонавтики выше |
  | **Аэропорт** | ⚔️ Военные | **Авиация, Э5** (было Робототехника/Э6 — логичнее рядом с авиацией) | 1 действие, без денег (не уточнено — предполагаемый дефолт), без лимита цикла | Переброска 1 своего юнита со столицы на любую клетку карты, если: сектор нейтрален ИЛИ свой; на клетке нет вражеского юнита; если сектор чужой — нужно «Открытые границы» с его владельцем (дипломатии нет — пока сектор с чужим городом просто блокируется) |
  | **Склад** | 💰 Экономические | **Стандартизация, Э2** (было — с начала игры, без исследования) | 1 действие + 1💰 за каждую добытую единицу региона | Собирает все ресурсы выбранного региона на склад (реализовано — цена активации не меняется, меняется только технология/эпоха открытия) |
  | **Радиовышка** | 💰 Экономические | Радио, Э5 | 1 действие + 1💰, не больше 1 раза за цикл | +3 Контента (новый building-only ресурс — кино/музыка и подобное, монополия владельца, как Промтовары у Фабрики; не ресурс карты, не в `warehouse`) |
  | **Рынок** | 💰 Экономические | Экономика, Э3 | 1 действие, продаёт 1 торговый ресурс со склада | +2💰 |
  | Фабрика | 💰 Экономические | Конвейер, Э5 | 1 действие + 1💰, не больше 1 раза за цикл | +3 Промтовара (реализовано) |
  | ГЭС | ⚙️ Промышленные | Паровой двигатель, Э4 | 1 действие + 1💰, не больше 1 раза за цикл | +1 Электричество (реализовано) |
  | АЭС | ⚙️ Промышленные | Атомная энергия, Э5 | 1 действие + 1💰, не больше 1 раза за цикл | +2 Электричества (реализовано) |
  | **Храм** | ⚙️ Промышленные | **Мистицизм, Э1** (было «Арсенал», Нарезное оружие/Э4, урон юнитов — снято, здание переименовано и перенесено) | 1 действие, ни денег, ни ресурсов — платится самой сжигаемой картой | Сжечь 1 любую выбранную карту действия из руки (обычный сброс на дно колоды, без побочных эффектов из 2.3.1 — это не штрафной сброс за перебор) и получить **+1💰 за каждый город любого игрока, чья религия совпадает с религией владельца Храма** (см. 4.5 — 6 религий на выбор в окне «Гос. управление», 11.6, после Мистицизма; если владелец Храма — атеист, сравнивать не с чем, доход 0, см. 4.5 про то, что «нет религии» ни с кем не совпадает). Принятие религии — по игроку целиком, все его города автоматически «под» ней. **Само состояние религии (`playerReligion`, 4.5) теперь реализовано** — блокирует только сам Храм, который пока не перенесён в код |
  | **Космодром** | ⚙️ Промышленные | Космонавтика, Э6 | 1 действие + 1 Углеводороды + 2 Редкоземельные + 2 Металла + 1 Уран, без денег, без лимита цикла | +1 компонент корабля (накопительно за партию); 3 компонента = 🏆 победа через космос |
  | **Управление** | 🗣 Социальные | **Всеобщая воинская повинность, Э4** (было — с начала игры, без исследования; заодно вернула здание технологии, опустевшей после переезда Ядерного арсенала) | 2💰, без действия (иначе бессмысленно), без лимита цикла | +1 действие в этом ходу |
  | **Университет** | 🗣 Социальные | Образование, Э3 | **Изменено — цена теперь определена.** 1 действие + 5💰 | То же самое открытие технологии, что и карта «Учёный» (3.1.8, тот же `availableResearchFor`/`researchTech`, та же механика лидерства по веткам) — только не через карту в руке, а через здание, и с доплатой 5💰 сверху. Старый эффект («посмотреть 3 карты колоды, выбрать 1 в руку») снят |
  | **Интернет** | 🗣 Социальные | Интернет, Э6 | 1 действие + 5💰, выбрать другого игрока | Сравняться с выбранным игроком по всем веткам, где он впереди (подтянуть недостающие технологии до его глубины); если он нигде не впереди — активировать нельзя, деньги не списываются |
  | ООН | 🗣 Социальные | Права человека, Э5 | Постройка = становится кандидатом (№1 или №2, см. 15.3) — отдельной цены АКТИВАЦИИ нет, здание само по себе даёт статус кандидата | 🏆 Победа дипломатией через Совет ООН — кандидаты/генсек/резолюции, см. 15.3 (переработано, заменяет упрощённую модель голосования) |

- Цена открытия технологий (4.3) по-прежнему не реализована так же, как и цена активации — там числа всё ещё чисто дизайн-намерение.

- **Итого зданий по эпохе технологии** (после распределения 4 бывших «бесплатных» зданий по дырам — 16 зданий, ни одного без технологии):

  | Эпоха | Зданий | Какие |
  |---|---|---|
  | Э1 | 2 | Храм (Мистицизм), Казарма (Бронзовое дело) |
  | Э2 | 2 | Фортификация (Феодализм), Склад (Стандартизация) |
  | Э3 | 2 | Рынок (Экономика), Университет (Образование) |
  | Э4 | 2 | ГЭС (Паровой двигатель), Управление (Всеобщая воинская повинность) |
  | Э5 | 6 | Ядерный арсенал, АЭС (оба — Атомная энергия), Радиовышка (Радио), Фабрика (Конвейер), Аэропорт (Авиация), ООН (Права человека) |
  | Э6 | 2 | Космодром (Космонавтика), Интернет (Интернет) |

  Э1–Э4 теперь ровно по 2 — дыры закрыты. Э5 всё ещё выделяется (6 из 16, из-за переезда Аэропорта и Ядерного арсенала) — не трогал, отдельный вопрос, если понадобится выровнять и его.

### 4.5. Религии — реализовано (`main.ts` — `adoptReligion`, окно «Гос. управление», см. 11.6)

Отдельная от политической парадигмы система (изначально задумывалась как замена содержимого парадигмы «Монотеизм» — на практике реализованы обе параллельно, парадигма «Монотеизм» сама по себе про прирост населения, см. 11.6, а религия — про Храма и единоверный бонус). Даёт Храму (4.4, ещё не в коде) от чего считать доход.

- **Триггер — окно «Гос. управление» (11.6), не автопопап при исследовании.** Изначально планировалось всплывающее окно сразу при первом личном исследовании «Мистицизм» — по факту реализации выбор просто стал доступен в уже существующем окне «Гос. управление» (`canAdoptReligion`, тот же гейт — нужен «Мистицизм»), без отдельного попапа поверх остального интерфейса.
- **6 вариантов, выбор свободный и МЕНЯЕТСЯ** (уточнение задним числом — раньше предполагалось «не меняется потом», но в реализации свободная пересборка в любой момент, без штрафа пропуска хода, в отличие от смены парадигмы, 11.6): Иудаизм, Буддизм, Христианство, Ислам, Конфуцианство, **Атеизм — явный выбор «нет религии»**, а не то же самое, что «ещё не выбрал» (`playerReligion: Religion | null`, `null` ≠ `"atheism"`).
- **Основатель** — первый игрок, когда-либо выбравший эту религию (`religionFounder`), назначается один раз и не меняется, даже если основатель сам сменит религию позже — исторический факт, не текущее членство. Основатель видит сводку: сколько игроков её приняли, сколько у них городов и суммарного населения (`religionFollowerStats`).
- **Принятие — по игроку целиком, а не по городу**: как только выбор сделан, ВСЕ города этого игрока (в том числе основанные позже) автоматически считаются «под» этой религией — нет отдельного шага «обратить город». То же правило уже описано у Храма (4.4).
- **Единоверный бонус в бою**: если страна одной религии атакует другую страну ТОЙ ЖЕ религии, войска атакующего теряют 1 урона (штраф, не бонус — единоверная агрессия не поощряется). Не действует между двумя атеистами и вообще ни для кого, у кого религия — Атеизм: «нет религии» не считается общей верой ни с кем, включая другого атеиста (интерпретация — явно не оговорено, но следует из формулировки «нет религии»). **Design-only** — зависит от боевой системы (раздел 6, атака/урон юнитов), которой в клиенте ещё нет, так же как бонус Монархии (11.6) и защита от ядерного оружия у Космонавтики (4.4) — тот же класс «сформулировано, применить не к чему».
- Реализовано: `playerReligion`, `religionFounder`, `canAdoptReligion`, `adoptReligion`, `religionFollowerStats`, модалка выбора внутри «Гос. управления». Не реализовано: сам Храм (4.4, ждёт этого состояния — теперь готово, когда дойдёт очередь), единоверный бонус (нет боя).

---

## 5. Юниты и их характеристики (исправленные)

### 5.1. Шкала развития юнитов по эпохам

| Тип юнита | Эпоха 1 | Эпоха 2 | Эпоха 3 | Эпоха 4 | Эпоха 5 | Эпоха 6 |
|---|---|---|---|---|---|---|
| Поддержка | Лучник | Арбалетчик | Мушкетер | Самолёты | Истребители | Ракеты |
| Дальняя атака | Катапульта | Требушет | Пушка | Артиллерия | САУ | Дроны |
| Мобильные | Колесница | Конница | Рыцари | Танки | Совр. танки | Роботы |
| Штурмовые | Воин | Секироносец | Легионер | Пехотинец | Мотострелок | Киборг |
| Оборонительные | Копейщик | Алебардщик | Крестоносец | Пулемётчик | ПТУР | ПВО |
| Корабли | (галера) | (каравелла) | (фрегат) | **(крейсер)** | **(линкор)** | (дредноут) |

**Правка кораблей по эпохам (по прямому уточнению — была ошибка в названиях)**: Э4 теперь **Крейсер** (было «Линкор»), Э5 теперь **Линкор** (было «Авианосец»). Э1-3/Э6 не менялись.

### 5.2. Полные характеристики юнитов (по классам)

**Штурмовые войска (Воин и аналоги)**
- Жизни (сила): 2, 3, 4, 5, 6, 7 (по эпохам).
- **Атака: 2 удара за ход** (было 1 — правка по прямому уточнению).
- Дальность атаки: 1 гекс (в соседнюю клетку).
- Дальность хода: **2 гекса за ход** (было 1 — правка по прямому запросу).

**Поддержка**
- Жизни (сила): всегда 1.
- Атака: 0 (не атакует самостоятельно).
- **Сила поддержки: увеличивает урон союзных ШТУРМОВЫХ и МОБИЛЬНЫХ юнитов на +1** (было «штурмовых и дальних» в раннем черновике этого пункта — заменено по прямому уточнению: «проще в понимании и реализации»). Действует на ВСЕ подходящие союзные юниты в радиусе одновременно, не только на ближайшего одного — тоже часть упрощения.
- Дальность поддержки (радиус): 1, 2, 3, 5, 8, 13 гексов (по эпохам).
- Дальность хода: 1 гекс.

**Дальняя атака (Артиллерия и аналоги)**
- Жизни (сила): 1, 2, 3, 4, 5, 6.
- Атака: 1 урон за ход.
- **Двойной урон на снятие защиты** (по прямому уточнению) — часть урона, идущая на защиту местности/юнита (буфер гекса, 6.8), удваивается; то, что доходит до основного HP цели (когда защита уже снята), урона не удваивает.
- Дальность атаки: 2, 3, 4, 5, 6, 7 гексов (растёт с эпохой).
- Особенность: бьёт по скоплению (наносит урон всем юнитам в цели, а не одному). ~~Может концентрировать огонь по одному юниту~~ — снято, см. 6.6.
- Условие стрельбы (уточнено, см. 6.6): нужен свой юнит любой категории (включая корабли) в гексе, СОСЕДНЕМ С ЦЕЛЬЮ — «передаёт позицию»/целеуказание. Нет такого соседа у цели — выстрел недоступен.
- Дальность хода: 1 гекс.

**Мобильные юниты (Колесница и аналоги)**
- Жизни (сила): всегда 3.
- Атака: 1 удар.
- Дальность атаки: 1 гекс.
- Дальность хода (скорость): 3, 4, 5, 6, 7, 8 гексов за ход (по эпохам).

**Оборонительные юниты (Копейщик и аналоги)**
- Жизни (сила): всегда 3.
- **Атака: 1 удар за ход** (было 0/не атакует вообще — правка по прямому уточнению; дальность атаки соответственно тоже 1 гекс, иначе атаковать было бы физически нечем).
- Множитель обороны (броня): x2, x3, x4, x5, x6, x7 (по эпохам). Это множитель, который умножает его базовую оборону.
- Дальность хода: 1 гекс.

**Корабли — «плавающая артиллерия» (правка, см. 6.8)**
- Жизни (сила): 2, 4, 6, 8, 10, 12 (по эпохам).
- **Атака: растёт с эпохой — 1, 2, 3, 4, 5, 6** (было фиксированная 1).
- **Дальность атаки: 1 гекс, фиксированная** (не растёт по эпохам, в отличие от сухопутной Дальней атаки).
- **Бьёт по скоплению (AoE)** — как сухопутная Дальняя атака, по всем юнитам на клетке цели сразу, не по одному.
- ~~Сила поддержки~~ — убрана по прямому запросу, корабли больше не поддерживают союзников.
- Вместимость: 1 сухопутный юнит на борту — реализовано как «мост»: корабль проходим для сухопутного юнита как обычная суша, цепочка кораблей = переправа через пролив; возврат на настоящий берег обрывает весь остаток хода этого цикла. Не отдельная механика посадки/высадки.
- Дальность хода: **4 гекса за ход** (было 2 — правка по прямому запросу), по воде и через клетки городов (гавани, см. 6.8 — канал).

---

## 6. Боевая система (подробно)

### 6.1. Порядок боя (строгая последовательность)

1. Атакующий юнит наносит урон защитнику своей силой атаки **+ по 1 за каждого своего Штурмового/Мобильного союзника поддержки в радиусе** (см. правку ниже — суммируются ВСЕ подходящие, не только ближайший один).
2. Если защитник выжил — защитник наносит ответный удар атакующему, тем же способом (+1 за каждого своего подходящего союзника поддержки).
3. ~~Поддержка атакующего наносит урон защитнику (+1) отдельным ударом~~ — свёрнуто в п.1 выше, тот же суммарный эффект.
4. ~~Поддержка защитника наносит урон атакующему (+1)~~ — свёрнуто в п.2.

**Поддержка — переработана (по прямому уточнению, «проще в понимании и реализации»):**
- Не отдельный удар — увеличивает урон ТОЛЬКО Штурмовых и Мобильных юнитов (не бьёт сама). Корабли и остальные категории поддержку не получают вообще, даже если рядом стоит союзник поддержки.
- Суммируются ВСЕ подходящие союзники поддержки в радиусе (не выбирается один ближайший, как в раннем черновике этого пункта) — 3 союзника поддержки в радиусе дают +3, не +1.
- Один юнит поддержки может поддержать сколько угодно раз за цикл — лимита «раз за цикл на юнита поддержки» больше нет.
- **Анимация линиями** (по прямому запросу) — при бое от каждого фактически поддержавшего юнита к тому, кого он поддержал, летит затухающая линия, чисто визуально (`main.ts` — `playSupportLineAnimation`, тот же `fxLayer`/RAF-паттерн, что у снаряда дальней атаки).

### 6.2. Бонусы к обороне (+1 сила за каждый активный фактор)

- Лесная местность.
- Родная территория.
- Оборона города (если юнит стоит в городе).
- Горная местность.

### 6.3. Команда «Обороняться»

Если юнит получил команду «Обороняться» (не двигается и не поддерживает союзников), то все бонусы из п. 6.2 удваиваются.

### 6.4. Последовательность списания урона

1. Сначала снимается бонусная сила (щиты, броня от обороны) — **у Дальней атаки (категория, не корабли) удваивается именно эта часть** (по прямому уточнению): защита снимается вдвое быстрее, но то, что доходит до HP цели (когда защита уже кончилась), урона не удваивает — ровно столько же HP теряется, сколько потерял бы обычный (неудвоенный) урон в той же ситуации. Реализовано (`GameSession.applyDamage`, параметр `doubleDefenseDamage`).
2. Только после того, как бонусы исчерпаны, урон списывается со здоровья (жизней) юнита.

### 6.5. Итог боя

- **Отступление/принуждение к отходу работает только в упор** (дистанция удара ≤1, по прямому уточнению — «дальнобойные... не наступают и не могут заставить отступить»): дальнобойный удар издалека наносит урон как обычно, но не запускает п. «юнит с меньшей силой отступает или гибнет, если некуда» — тот срабатывает только при ударе с соседнего гекса.
- Если после всех ударов В УПОР никто не погиб, то юнит с меньшей оставшейся силой отступает на соседний гекс.
- Если отступать некуда (зажат или окружён), юнит умирает.
- **Атакующий юнит всегда остаётся на исходной позиции** (той клетке, с которой атаковал) — не наступает и не занимает освободившуюся клетку сам по себе. Захват города требует отдельного явного приказа на движение — см. 6.9 (город без размещённых защитников обороняется своим населением, не резервной очередью юнитов).
- Здоровье (сила) всех участвовавших юнитов восстанавливается до полного значения после завершения боевого цикла.

### 6.6. Артиллерия (дальняя атака)

- Наносит урон по всем юнитам в целевой клетке (скопление), ослабляя их.
- ~~Концентрированный огонь~~ — **снято как отдельная механика** (по прямому уточнению): персистентная защита гекса (6.8, `hexDefense`, обновляется только в начале нового цикла) уже даёт тот же практический эффект — повторные удары по той же клетке продолжают с того места, где остановилась защита, так что «сосредоточение огня» получается естественным образом через несколько ударов подряд, без отдельного режима «весь урон одному юниту».
- **Условие видимости — теперь точно определено** (по прямому уточнению, раньше был общий пункт «не реализовано»): выстрел по гексу разрешён, только если в ЛЮБОМ гексе, соседнем С ЦЕЛЬЮ (не с самим стрелком), стоит СВОЙ юнит любой категории, включая корабли — он «передаёт позицию» (эффект — целеуказание/корректировка). Нет такого соседа у цели — выстрел недоступен, независимо от дальности оружия.

### 6.7. Порядок ударов по юнитам в одной клетке

Если в клетке находится несколько юнитов, урон сначала наносится юниту с наибольшей силой (жизнями). Только когда он выбит, урон переходит на следующего по силе.

### 6.8. Реализация — движение, бой, гарнизон города (`main.ts`, характеристики — `units.ts`)

Раздел 5.2/6/9 теперь реально в коде, не только дизайн. `UnitInstance` (main.ts) хранит `hp`/`defending`/`moveOrder` поверх старых полей; `statsFor(category, epoch)` (units.ts) — таблица HP/атаки/дальности/хода один в один по 5.2.

- **Клик по своему юниту выбирает его** (`tryStartUnitCommand`) — в городе по очереди гарнизона (см. ниже), вне города однозначно (не более 1 юнита от игрока на обычном гексе). Следующий клик — приказ: пустой/проходимый гекс = движение, гекс с чужим юнитом ИЛИ чужим городом = атака (`tryCommandSelectedUnit`). Esc снимает выбор.
- **Стек юнитов**: обычный гекс — максимум 2 юнита, не больше 1 от одного игрока; занятый под завязку (2) гекс блокирует и вход, и проход насквозь. Город — без ограничений (`canEnterHex`).
- **Движение — взвешенный Дейкстра** (`computeUnitPath`) по `hexNeighbors`, только по своему типу местности — сухопутные по суше, корабли по воде, **с одним исключением: клетка со СВОИМ кораблём проходима и для сухопутного юнита, как обычная суша** («корабли как гекс, через который могут проходить юниты как по суше» — цепочка кораблей работает мостом через пролив, можно пройти несколько подряд за один конец цикла, если хватает очков хода). Вместимость моста — 1 сухопутный юнит на корабль одновременно (5.2, `canEnterHex`). **Дороги (существующие торговые маршруты, любого владельца) стоят 0.5 очка хода за гекс вместо 1** — по прямому запросу, `isRoadHex`.
- **Высадка на настоящий берег обрывает бюджет хода ЭТОГО цикла целиком**, даже если очки ещё остались (`resolveUnitMovementForCycle` замечает переход «был на воде (на мосту) → стал на суше» и останавливает юнита сразу же) — до начала следующего цикла этот юнит вообще не принимает новых команд (`landedThisCycle`, ни движение, ни атака). Погрузка на мост (суша → вода со своим кораблём), наоборот, ход не обрывает — можно пройти весь мост в одну «переправу».
- **Приказ растягивается на несколько концов цикла** (5.3/9: «в конце цикла... чтобы игрок мог сменить команду») — `unit.moveOrder` копит остаток пути, `resolveUnitMovementForCycle()` продвигает на `moveRange` очков КАЖДЫЙ раз, когда `currentPlayerIndex` возвращается к 0 (конец цикла, `onPlayingEndTurn`), заново проверяя блокировку на каждом шаге (доска могла измениться). Юнит, дошедший до гекса чужого города, где на этот момент нет вражеских юнитов, захватывает его (переход `playerId`, население переходит новому владельцу как есть) — см. 6.9, вход туда разрешает `commandUnit` только пока осада этого цикла уже пробита.
- **Снабжение (ТЗ 9)** — приказ на движение ИЛИ атаку списывает 1 единицу последнего ресурса в цепочке цены постройки этой эпохи (7.1, `EPOCH_UNIT_COST`), доступ → склад → рынок от родного города юнита. Списывается **один раз за отданный приказ**, не за каждый гекс и не повторно за каждый цикл, пока тот же приказ ещё выполняется — упрощение ради предсказуемости, тот же принцип, что у цены любой карты в этом клиенте.
- **Защита принадлежит КЛЕТКЕ, не юниту** (`hexDefense: Map<string, number>`, персистентная, не пересоздаётся на каждый бой) — по прямому уточнению: ходы в hotseat идут по очереди, а не одновременно, и артиллерийский удар по юниту, который к следующему выстрелу уже сбежал на другой гекс, не должен требовать «сбивать защиту заново» — раньше защита была привязана к юниту и обнулялась при каждом новом вызове боя. Теперь: первый урон по клетке в текущем цикле лениво считает её полное значение (используя того, кто там стоит на этот момент) и запоминает остаток; следующий удар по ТОЙ ЖЕ клетке продолжает с места, где остановились; юнит, сбежавший на ДРУГУЮ клетку, получает там её собственное, ещё нетронутое значение. Восстанавливается целиком вместе со здоровьем юнитов в начале нового цикла (`hexDefense.clear()`).
  - **База защиты = 1** (ТЗ 9, «если сила не прописана явно — 1»), у Оборонительных юнитов (5.2) умножается на множитель их эпохи (x2..x7) — **множитель масштабирует ТОЛЬКО базу, местность в него не попадает** (уточнение). Монархия (11.6) добавляет базу ещё раз (то же число ещё раз, то есть ×2 к базе, в т.ч. поверх брони Обороны) — теперь реально подключена к бою, раньше была design-only.
  - Поверх базы — ситуативные бонусы клетки: Лес +1, Холмы +1, Горы +2, Город +1 (любой, не только свой — чисто позиционный), своя территория +1 (регион со своим городом), Фортификация +эпоха игрока («соразмерно уровню»), свой торговый путь на этой клетке +1 (снабжение).
  - **Пустыня/Тундра — бонусы местности вообще не действуют** («окопаться нельзя»): не штраф −1, как было раньше, а обнуление ВСЕЙ ситуативной суммы выше (лес там и так невозможен, но и территория/форт/дорога тоже не считаются) — **кроме случая, когда на этой клетке стоит город**, тогда бонусы (включая городской) работают как обычно.
  - **Холмы увеличивают дальность стрельбы Дальней атаки на +1**, пока юнит стоит на них (`effectiveAttackRange`) — не влияет на другие категории (их дальность и так 0/1).
  - **«Обороняться» удваивает ИТОГ целиком** (база + местность вместе, по прямому уточнению) — юнит теряет `moveOrder` при включении (не двигается, 6.3).
  - При наведении на юнита другого игрока видна только текущий остаток защиты его клетки в этом цикле (не HP/атака) — намеренно меньше данных, чем у своего юнита.
- **Бой** (`resolveCombat`, 6.1 полностью) — атака → контрудар (если защитник жив) → поддержка атакующего (+1, если есть) → поддержка защитника (+1, моральный урон, даже если защитник уже погиб). Поддержка — ближайший в радиусе союзник с `supportBonus > 0`, **не более одного применения за цикл на юнита поддержки** (`supportUsedThisCycle`, сбрасывается в конце цикла вместе с HP и защитой клеток). Юнит на мосту (сухопутный на клетке своего корабля) не атакует и не поддерживает вообще — только высадившись (`isAboardShip`).
  - **Отступление (6.5) — только у защитника**: если оба живы и защитник слабее (HP ≤ атакующего) — отступает на соседний проходимый гекс, некуда — погибает. **Атакующий всегда остаётся на исходной позиции** («возвращается на исходную позицию, если не захватил клетку» — бой в клиенте дистанционный/позиционный, атакующий физически никуда не перемещается для удара, так что для него это буквально «не сдвинулся»); автоматического захвата освободившейся клетки нет — нужен отдельный приказ на движение.
  - **Артиллерия (6.6, `stats.aoe`)** — бьёт сразу по ВСЕМ юнитам на клетке цели, включая резерв гарнизона, снижая защиту клетки и, если она кончилась, здоровье каждого из них по отдельности; погибшие убираются из игры только после того, как отработал весь залп целиком, не по одному в процессе. **Не реализовано**: концентрированный огонь по одному юниту (альтернативный режим из ТЗ) и условие видимости («не может стрелять по клеткам вне зоны видимости союзных юнитов») — открытый пункт, см. правку 13.
  - **Порядок целей в стеке (6.7)** — при обычной (не AoE) атаке бьётся юнит с наибольшим HP среди защитников этой клетки.
  - **«Высадка с моря снижает атаку на 1»** — применяется, когда атакующий сам корабль (`attacker.category === "ship"`, атака −1, не ниже 0); сухопутный юнит, только что высадившийся с моста, физически не может атаковать в тот же цикл вообще (см. выше, `landedThisCycle`), так что отдельного штрафа именно для него не требуется.
- **Стоимость гекса зависит от рельефа** (уточнение, `terrainMoveCost`): обычный гекс — 1 очко хода, Холмы — 2, лес (оверлей, складывается с рельефом) — ещё +1 (Холмы+лес = 3). Если юниту не хватает остатка бюджета ЭТОГО конца цикла на клетку, куда он идёт, он **всё равно входит в неё** и останавливается там (бюджет обнуляется, приказ на дальнейшее движение не отменяется — просто ждёт следующего конца цикла), но в ТЕКУЩЕМ цикле не может ни атаковать, ни встать в «Обороняться» (`outOfMoveThisCycle`, `resolveUnitMovementForCycle`/`tryCommandSelectedUnit`/панель юнита) — в отличие от `landedThisCycle` (высадка с моря), которое блокирует вообще все команды, здесь заблокированы только бой и оборонительная стойка, новый приказ на движение принять можно. Сбрасывается вместе с `landedThisCycle` и здоровьем в начале нового цикла.
- **Горы без дороги съедают всё движение за цикл** (уточнение) — вход на такую клетку не стоит фиксированное число очков, а списывает ВЕСЬ остаток бюджета этого конкретного цикла (`resolveUnitMovementForCycle`, `isBarrierMountain`), сколько бы ни оставалось; юнит гарантированно останавливается там до следующего конца цикла. Дорога через горы отменяет это — обычная скидка 0.5, как везде. Оценка «дойдёт за N циклов» в `computeUnitPath` использует `moveRange` юнита как стоимость этого шага — самосогласованное приближение, не идеально точное (реальное списание зависит от того, сколько бюджета оставалось НА МОМЕНТ этого шага), но для ETA этого достаточно.
- **Карта замкнута тором по обеим осям («земля круглая», по прямому запросу)** — уходя с правого края, юнит продолжает движение с левого (и симметрично сверху/вниз), если там вообще есть куда пройти (проходимость по-прежнему проверяется как обычно, просто соседний гекс теперь берётся с противоположного края). Действует на ВСЮ игровую смежность — движение (`computeUnitPath`), дистанцию для дальней атаки и поддержки (`hexDistance`), отступление, прибрежность галер и защиту-бухту (`hexNeighborsGameplay` в main.ts, обёртка над новым `hexNeighborsWrapped` в hexMath.ts). Генерация карты и торговые маршруты (`findRoutePath`) намеренно НЕ обёрнуты — остаются как есть, чтобы не задеть уже настроенные алгоритмы и отрисовку линии маршрута.
- **Вражеский (в состоянии войны) юнит блокирует и вход, и проход насквозь через свою клетку целиком** — не только «под завязку» (2 юнита), как раньше: `canEnterHex` теперь сначала проверяет именно это, ДО правила о городе — иначе можно было бы «пройти» движением прямо в защищаемый город противника, вообще не вступая в бой. Добраться до такой клетки можно только атакой, не приказом на движение.
- **Корабли — теперь «плавающая артиллерия»** (правка): `attack` растёт с эпохой (1..6, не фиксированная 1), `aoe: true` (бьют по всем на клетке цели, как Дальняя атака, не по одному), `attackRange: 1` фиксированная (не растёт по эпохам, в отличие от сухопутной Дальней атаки), поддержки больше нет (`supportBonus`/`supportRadius` — 0, было +1/1 гекс).

### 6.9. Осада города — гарнизон = население (переработано, по прямому уточнению)

Города НЕ держат резервную очередь юнитов-гарнизона. Пока в городе стоит хотя бы один размещённый (обычный, построенный игроком) юнит-защитник — весь урон идёт по нему как в обычном бою (6.1–6.7), население города вообще не задействовано и урона не получает. Только когда в городе НЕТ ни одного размещённого защитника, атака начинает бить по «встроенному» гарнизону самого города, чья сила равна его населению — отдельного юнита для этого не существует.

- **Буфер осады цикла** (`GameSession.citySiegeBuffer`, ключ — id города, отдельно от `hexDefense` юнитов) — считается один раз лениво при первом ударе по безоружному городу в этом цикле (`cityGarrisonDefense`): `(max(1, население) × 2 при Монархии) + бонусы местности (город+1, холмы+1/горы+2, своя территория+1, Форт +эпоха, свой торговый путь +1)`, всё это **×2** (город всегда считается «в обороне», как юнит на команде «Обороняться»). Копится урон нескольких атак подряд в одном цикле — тот же принцип, что у `hexDefense` (6.8): Дальняя атака/корабли удваивают именно снятие этого буфера (`atkDoubleDefense`), как и с обычными юнитами.
- **Пробитие буфера в 0 — «победа над гарнизоном» — снимает РОВНО 1 население**, не по фактическому урону удара. Как только буфер этого цикла пробит, город на ОСТАТОК цикла становится целью для ВХОДА (`commandUnit` перестаёт считать его вражеской целью для атаки, путь строится как обычное перемещение) — заведите в него юнита ДО конца хода, чтобы захватить (владение переходит новому игроку, население остаётся как есть, не обнуляется). Артиллерия и корабли бьют по этому гарнизону так же, как обычные юниты — исключений по категориям нет.
- **Не успели зайти в этом же цикле** — при обороте `currentPlayerIndex` на 0 буфер осады сбрасывается вместе с `hexDefense` (`onPlayingEndTurn`). На новом цикле город снова закрыт (гарнизон «собрался заново»), но уже слабее — буфер пересчитывается из УЖЕ сниженного на 1 населения. Нужно пробивать заново, население продолжает таять на 1 за каждый успешно пробитый цикл, пока либо не успеют захватить, либо население не дойдёт до 0.
- **Население дошло до 0 — город уничтожается целиком** (`destroyCity`), а не остаётся пустым и захватываемым: снимается как игровой объект (пропадает из списка городов), любые торговые маршруты с ним как концом рвутся, юниты, приписанные к нему (`cityId`), отвязываются (остаются на карте обычными юнитами), клетка отмечается руинами (`ruins` — визуальная пометка, обычная проходимая местность, не отдельный тип террейна карты, юниты по ней ходят как по обычной суше).
- Проверено живым тестом на сервере: пробитие буфера снимает ровно 1 население и открывает вход до конца цикла; без входа — на новом цикле буфер собирается заново из уже сниженного населения (меньше, чем был); размещённый защитник (даже один) полностью блокирует урон по гарнизону, пока жив; многократное пробитие без захвата в итоге доводит население до 0 и уничтожает город (руины).
- **Галера (Э1, «галеры и триеры») — только вдоль берега** (по прямому уточнению): гребной флот эпохи 1 проходим (`unitPassable`, `isCoastalSeaTile`) только по морским клеткам, у которых есть хотя бы один сухопутный сосед, — в открытое море, оторванное от берега, зайти не может (город на маршруте по-прежнему проходим). С Каравеллы (Э2) и дальше это ограничение снимается — открытое море доступно.
- **Постройка корабля** — только у города с хотя бы одним соседним морским гексом (`cityHasAdjacentSea`; в модалке выбора юнита категория «Корабли» показана заблокированной «🔒 нет моря рядом» иначе). Появляется НЕ на клетке города, а на свободном соседнем морском гексе — приоритет самому защищённому (по формуле бухты ниже, `shipSpawnHex`). Фашизм (11.6, 2 юнита за раз) пересчитывает место для каждого корабля отдельно, чтобы оба не пытались встать в одну клетку.
- **Города — гавани для кораблей**: клетка города проходима для кораблей точно так же, как для сухопутных юнитов (`unitPassable`), значит корабль можно «убрать в запас» гарнизона города (та же очередь, что у сухопутных юнитов, 5.3 — **корабль в городе такой же защитник, как и любой другой юнит**) и провести СКВОЗЬ город на противоположную сторону, если там снова море — специальной «канал»-логики не потребовалось, это просто следствие того, что города проходимы: город на перешейке шириной в 1 гекс превращается в морской канал между двумя акваториями.
- **Защита морского гекса — бухта** (уточнение, `computeFreshHexDefense`): каждый сухопутный сосед («берег») даёт +1 к защите этой морской клетки, КРОМЕ первого — открытая вода с одним берегом рядом защиты не даёт вообще, а полностью закрытая бухта (5 сухопутных соседей — максимум для одной клетки, шестой сам остаётся морем) даёт +4. Хорошее место прятать корабли.
- **Гарнизон города** (`cityGarrisonQueue`, по возрастанию `id` = по времени постройки) — только первый (самый старый) командуем, остальные в резерве: участвуют в обороне (это тот же юнит из очереди, который реально принимает урон — резерв просто не выбирается активным, «не оказывают поддержки» в смысле бонуса, отдельного от их собственного HP, не даётся). Погиб/отступил активный — следующий в очереди становится активным сам по себе (это просто пересчёт сортировки, не отдельное состояние).
  - **Пустой гарнизон (0 юнитов) обороняется населением города** — «защита равна размеру населения», урон ПОСТОЯННЫЙ (не буфер на цикл, реальная убыль population, `city.population -= dmg`, не ниже 0). Пока в городе стоит хоть один юнит — population урона не получает вообще, весь бой идёт через юнитов.
  - **Захват** — только явным входом юнита на гекс города с `population === 0` (артиллерия/бой могут свести оборону к нулю, но сама постройка требует зайти туда отдельным приказом на движение, по прямому уточнению).
- **Дипломатия ↔ бой (11.7)**: атака игрока, с которым нет войны, ИЛИ вход на территорию чужого региона без соглашения «Открытые границы» — показывает `window.confirm` («это начнёт войну — продолжить?»); отказ отменяет действие, согласие вызывает `declareWar` и только потом выполняет операцию. Уже идущая война ничего не переспрашивает.
- **Панель юнита при наведении/выборе** (`#unit-info-panel`, `renderUnitInfoPanel`) — имя, HP, атака, защита, дальность хода/атаки, у своего командуемого юнита кнопка «🛡 Обороняться»; отдельно отмечено, если юнит сейчас на мосту (не может атаковать/поддерживать), только что высадился (ход исчерпан), или **в резерве гарнизона — уточнение по прямому запросу: резервный юнит по-прежнему обороняет город собой (принимает урон как обычная цель, см. п. «Гарнизон города» выше), но НЕ может атаковать, грабить/пиратствовать, и НЕ может встать в команду «Оборона» (ту самую, что удваивает итоговую защиту, 6.8) — эта конкретная команда, как и атака, доступна только самому старому юниту очереди; резервному доступен лишь приказ покинуть город (см. §14 п.1)**. **Упрощение**: фиксированное место в углу карты, а не плавающий тултип за курсором — курсор в мировых координатах Pixi (с учётом зума/панорамирования) отдельно не пересчитывается.
- **[ИСПРАВЛЕНО, версия 2] Подсказка при наведении на гекс (`#hex-info-panel`/`renderHexInfoPanel`, нигде отдельно в ТЗ не описан — добавлен по прямому запросу без записи сюда) — компактная СВОДКА по клетке, не дублирует `#unit-info-panel`.** Первая версия правки убрала юнитов из hex-info-panel целиком, раз они дублировали `#unit-info-panel` (тот показывает ПОЛНУЮ карточку одного наведённого/выбранного юнита — HP/атака/ход/дальность). По уточнению юниты вернули, но в другом виде — не полный дубль, а СВОДКА по всей клетке сразу: список ВСЕХ юнитов клетки с их защитой (`unitTotalDefense`, коротко, без остальных статов — тех, кто нужен подробно, наводят на маркер юнита отдельно), плюс для города — справочная защита и ответная атака его гарнизона-по-населению (`cityGarrisonDefense`, зеркалит `GameSession.cityGarrisonDefense`; ответная атака гарнизона нигде явно не задана — те же default-1, что и у остальных сил без явного числа, ТЗ §9). Это разное назначение (обзор клетки целиком vs подробности одного юнита), не повтор одного и того же — обе панели теперь сосуществуют без дублирования.
- **[ИСПРАВЛЕНО] На карте в городе виден маркер только активного (командуемого) юнита очереди, не всех сразу.** По прямому запросу — «два юнита в столице... двойное отображение». Раньше `drawCityMarkers()` рисовал отдельный (притушенный) маркер и для резервных юнитов гарнизона, стопкой со смещением рядом с активным; теперь для городских клеток рисуется только маркер `cityGarrisonQueue[0]` (самый старый, единственный командуемый) — резервные не получают своих маркеров на карте вовсе. Их можно увидеть только через hex-info-panel (наведение, см. выше) или клик по городу (модалка «city-detail», §14 п.1) — там же и выбор конкретного резервного юнита для вывода из города. Вне города (не более 2 юнитов разных игроков на обычном гексе, без очереди) стек маркеров рисуется как и раньше, без изменений.
- **Гибель корабля с сухопутным юнитом на мосту** — пассажир тонет вместе с кораблём (`removeDeadUnits`, снимает всех некорабельных юнитов на клетке погибшего корабля).
- **Сознательно НЕ реализовано в этом проходе**: полноценный захват через любой другой канал, кроме входа на гекс с population 0. (Концентрированный огонь и условие видимости артиллерии, 6.6, — сняты с этого списка: первое больше не отдельная механика, второе теперь точно определено и ждёт переноса в `GameSession.ts` вместе с остальными боевыми правками этого дня.)
- **Анимация дистанционных атак** (по прямому запросу — «летящий снаряд и взрыв... для эпох где уже есть порох») — чисто визуальный эффект, `playRangedAttackAnimation`/`playImpactFlash`, отдельный Pixi-слой `fxLayer` НАД `markerOverlay` (тот при каждом рендере боя перестраивается заново, живущая в нём анимация была бы стёрта на полпути). Срабатывает только для дистанционных категорий (Дальняя атака/Корабли — `stats.aoe || stats.attackRange > 1`) и только с эпохи пороха (Э3, Мушкетёр/Пушка) включительно — более ранние дальнобойные (Лучник, Катапульта, Требушет, Арбалетчик, Требушет) стреляют без анимации. На состояние партии не влияет, чисто картинка: маленькая точка летит по прямой от атакующего к цели (~260мс), затем на месте цели — расширяющаяся затухающая вспышка (~260мс).

## 7. Экономика и стоимость

### 7.1. Стоимость создания юнитов (по эпохам)

| Эпоха | Требуемые ресурсы |
|---|---|
| 1 | Еда |
| 2 | Еда + Металлы |
| 3 | Металлы + Торговые ресурсы |
| 4 | Деньги + Углеводороды |
| 5 | Металлы + Углеводороды + Деньги |
| 6 | Металлы + Углеводороды + Деньги + Редкоземельные |

### 7.2. Добыча ресурсов

- Каждый ход игрок автоматически получает все ресурсы, которые добывает его столица.
- Ресурсы из остальных городов добываются только с помощью карты «Рабочий» — за раз с одного региона, целиком (см. 3.1). Механики «улучшить добычу ресурса на карте» больше нет — только сбор с региона города.
- Если для типа ресурса открыта технология удвоенной добычи (Навигация, Индустриализация, Генная инженерия и т. п. — полный список в `Технологии.md`), с региона приходит вдвое больше.
- **Ресурсы на складе города восполняются в начале каждого цикла до полного набора региона, но не накапливаются** — неиспользованный остаток предыдущего цикла не складывается с новым, просто перезаписывается. Использованный (потраченный) ресурс исчезает с табло города до следующего восполнения.
- **Вражеский юнит физически блокирует добычу** (реализовано, по прямому запросу) — если на КОНКРЕТНОЙ клетке с ресурсом стоит юнит игрока, с которым идёт война, этот ресурс временно выпадает из доступа (`resourcesInRegion(..., playerId)`/`resourceTileBlocked`): недоступен ни через автоматический сбор столицы, ни через «Рабочего»/Склад, ни через Строителя/Воина/Торговца — блокируется именно клетка, не весь регион, и только конкретный тип ресурса на ней. Уйдёт вражеский юнит — доступ возвращается сам, без отдельного действия.
- **Город не может добывать больше типов ресурса, чем у него населения** (реализовано, по прямому запросу) — например, население 2 при 3 разных ресурсах в регионе даст доступ ровно к 2 из них (`resourcesInRegion(..., capToCity)`/`accessTypesUsedThisCycle`). Лимит считается по РАЗНЫМ типам за текущий цикл, не по клеткам — несколько тайлов одного и того же ресурса в регионе не расходуют лимит повторно, и уже отработанный в этом цикле тип не занимает лимит заново при повторном обращении (доступ → склад → рынок всё ещё пробуется в этом порядке, просто «доступ» теперь может исчерпаться раньше, чем регион физически закончится).
  - **[ИСПРАВЛЕНО] Подсказка при отказе роста населения/посадки леса из-за этого лимита была непонятной** (живой баг-репорт — игрок видел 2 физически нетронутых типа пищи в регионе и не понимал, почему рост населения всё равно отклонён). Причина в самом правиле выше: лимит общий на ВЕСЬ город за цикл, единый на все траты (Рабочий/Строитель/Учёный/рост населения — не важно), а не отдельный под каждое действие — если единственный слот бюджета (при населении 1) уже потрачен на что-то ещё в этом цикле (не обязательно на еду), рост населения не может занять НОВЫЙ тип, даже если он физически не тронут. Подсказка (`GameSession.explainFoodShortfall`, подключена в `growCity`/`plantForest`) теперь явно называет причину и перечисляет, что именно уже занято в этом цикле у этого города.
- **Ресурсы никогда не размещаются на тундре под ледяной шапкой** (правка — раньше требовали технологию «Горное дело» для добычи там; теперь генератор ресурсов (`resourceGenerator.ts`, `eligibleCoords`) вообще исключает клетки с `iceCover` из размещения, как и открытый лёд/океан) — места на открытой тундре после сокращения числа тундровых континентов до 2/2 (см. 1.1) достаточно, отдельная арктическая технология больше не нужна.

### 7.3. Торговля (карта «Торговец»)

При использовании карты «Торговец» игрок выбирает один из двух вариантов расчёта прибыли:

- **Вариант 1:** Прибыль = 1 за каждый ресурс, который есть у игрока и отсутствует у других игроков на данном торговом маршруте.
- **Вариант 2:** Прибыль = (Количество разных типов торговых ресурсов в наборе) × (Число полных наборов этих ресурсов).

Ограничение: прибыль не может превышать общее население на торговом маршруте.

---

## 8. Сетевая архитектура (синхронизация)

### 8.1. Модель синхронизации

Используется детерминированная модель с блокчейн-подобной фиксацией ходов:

1. **Фаза планирования (2 минуты):** клиенты отправляют на сервер список действий (макс. 2 карты).
2. **Фаза разрешения (Resolve):** сервер применяет логику всех игроков одновременно, используя единый Seed (зерно) для всех случайных событий (броски кубиков, катастрофы, рост леса).

После разрешения сервер рассылает всем клиентам дельту изменений (State Delta) — только то, что изменилось на карте и в ресурсах.

### 8.2. AI (Искусственный интеллект)

- В сессии одновременно играют от 4 до 6 цивилизаций.
- Часть из них управляется AI (заменяет отсутствующих игроков или выступает как полноценный противник).
- Если игрок потерял соединение во время фазы планирования — сервер автоматически передаёт управление его цивилизацией AI. AI делает ход вместо него.
- Переподключение: если игрок переподключается в любой момент (даже в середине хода), он немедленно получает управление обратно (AI отключается). Состояние синхронизируется с сервером.

### 8.3. Обработка дисконнекта

- Нет технического поражения. Игрок может переподключаться сколько угодно раз в течение сессии.
- На время отсутствия игрока его цивилизацию ведёт AI.
- При переподключении игрок получает актуальное состояние игры и продолжает управлять своими юнитами и картами.

---

## 9. Важные правила, которые нужно зафиксировать

- В одном регионе можно разместить только 1 город. Всего доступно для заселения — 20 обитаемых регионов (см. 1.1).
- Перемещение войск не отнимает действия. Оно ограничено только дальностью хода юнита.
- Перемещение требует снабжения — наличия последнего ресурса в цепочке стоимости создания этого юнита (см. п. 7.1).
- Если число силы не прописано явно для юнита — считается, что его сила = 1 (по умолчанию).
- Обычный гекс вмещает 2 юнита. Корабль вмещает 1 юнита на борту.
- Чем выше уровень города, тем больше видов пищи требуется для размещения там поселенца (игрок должен иметь на руках нужный набор ресурсов).
- Карточки, которые нельзя сбросить, рано или поздно придётся сыграть, когда наступит время сброса.
- Полученная на руки карта не передаётся в этом же цикле — только в следующем ходу.
- Действия имеют цену. Все цены прописаны в таблицах (разделы 3, 7).
- Порядок хода может быть любым, но важно помнить, что в конце цикла силы восстанавливаются (здоровье юнитов становится полным).
- **Лимит городов на игрока — 8.** Постройка или захват 9-го города завершает партию **территориальной победой** этого игрока — третье условие победы наравне со зданиями-победами Космодром и ООН (см. 4.4). В правой панели клиента (см. 11.5) под лимит всегда зарезервировано 8 слотов; пустые слоты показываются пустыми, а не скрываются.
- **[ИСПРАВЛЕНО] Запрет основания города в регионе с вражеским юнитом** (по прямому запросу) — если в регионе клика стоит юнит игрока, с которым идёт война (тот же критерий «враг», что и везде в клиенте — `resourceTileBlocked`/`canEnterHex`, не просто «любой другой игрок»), карта «Поселенец» отказывает с явной подсказкой, а не основывает город рядом с вражеским войском. Реализовано в `GameSession.regionHasEnemyUnit`/`foundCity` — проверяет весь регион (все гексы), не только конкретную клетку клика.

---

## 10. Жёсткие ограничения на дизайн (для разработчиков)

- 2D-графика. Вся игра выполняется в двухмерном пространстве (вид сверху).
- Карта гексагональная. Все расчёты дальности и соседства — по шести направлениям.
- Одновременные ходы. Сервер НЕ ждёт подтверждения от каждого игрока по очереди — всё применяется пачкой в конце таймера.
- AI должен уметь: строить города, создавать юнитов, воевать, открывать технологии, торговать и использовать карты событий.

---

## 11. Интерфейс (UI/UX)

### 11.1. Карта и зум (мобильная версия)

- Максимальный зум: на экране виден один регион (4 × 3 гекса).
- Минимальный зум: на экране видна вся карта целиком (24 × 18 гекса, все 36 регионов).
- Между этими значениями — плавное масштабирование.

### 11.2. Постоянные элементы интерфейса

- Область карточек (рука игрока) закреплена внизу экрана и отображается всегда, независимо от уровня зума карты и открытых окон.
- Панель ресурсов закреплена вверху экрана и отображается всегда.

### 11.3. Ресурсы: доступ и хранение

- Ресурсы региона столицы доступны всегда и отображаются в панели ресурсов в реальном времени — их количество обновляется автоматически каждый игровой цикл (см. 7.2).
- Ресурсы остальных городов не поступают автоматически: их нужно добывать картой «Рабочий», по одному региону за раз (см. 3.1, 7.2).
- Добытые таким образом ресурсы хранятся на складе игрока и расходуются оттуда (постройки, юниты, технологии, торговля и т. д.).
- **Ресурсы восполняются в начале каждого цикла, но не накапливаются** (см. 7.2) — табло города/склада в клиенте всегда показывает текущий набор, а не растущий запас; потраченный ресурс пропадает с табло до следующего восполнения.

### 11.4. Пространство стола (игровой клиент, `web/game.html`)

Игровой клиент (отдельно от конструктора карты) — рабочая раскладка «стола» со стартовой расстановкой (1.3), колодой/рукой (2.3, 3), боем и движением юнитов (5–6) и реальной добычей ресурсов; технологии/здания по-прежнему без реального списания цены их ОТКРЫТИЯ (сама постройка/юниты списываются, см. 4.4/7.1):

- **Слева — вертикальная колонка (`.left-rail`) из двух панелей**, растянутых на всю доступную высоту и ширину колонки без пустот (свободная высота делится 3:1 в пользу дерева технологий — у него 12 строк узлов против 4 строк зданий, при делении пополам одно из двух вырождается в нечитаемые квадраты):
  - **Дерево технологий** сверху (`renderTechTree()`) — сетка 4 (ветки) × 6 (эпох), эпоха 1 внизу, эпоха 6 наверху (см. 4.1). Каждый узел раскрашен и подписан иконкой по смысловой категории технологии, а не по ветке. 🔓 — уникальная технология (золотое кольцо), 🏛 — угловая метка «даёт здание». Подсказка при наведении показывает теги, эффект технологии и, если технология даёт здание, отдельно эффект и цену этого здания (подтягивается из `buildings.ts`, чтобы здание не упоминалось без объяснения, что оно даёт).
  - **Городская застройка** снизу (`renderBuildings()`) — та же визуальная сетка, 4 (группы) × 4 (здания в группе, см. 4.4). Свободная ячейка — контур цветом группы, занятая одним игроком — заливка его цветом с его номером, занятая двумя (правило «до 2 владельцев», 4.4) — диагональный градиент двух цветов, номера через «+» (например «1+3»), пунктир — эффект ещё не задан (сейчас таких нет). 🏆-здания (Космодром, ООН) выделены золотым кольцом, как уникальные технологии — **только пока не заблокированы**: у `state-locked` (технология ещё не открыта) кольцо не показывается вообще, иначе оно визуально путалось с жёлтой обводкой «доступно, не хватает ресурсов» (был баг-репорт — «Космодром и ООН светятся жёлтым» — хотя логика доступности была верной, кольцо просто накладывалось на пустую заблокированную клетку и читалось как ложный сигнал доступности).
  - **4 состояния клетки по обводке** (`canAffordBuilding`, поверх заливки владения выше — не вместо неё): **зелёная** — уже построено текущим игроком; **жёлтая** — технология открыта, не построено, ресурсов хватает — под названием здания показана цена **иконками ресурсов и числом** (`costIconsHtml`, категорийные строки цены типа «2 разных торговых» берут иконку по `RESOURCE_CATEGORY_META`, а не одну конкретную); **красная** — технология открыта, но ресурсов не хватает — та же иконочная цена, но красным; **пустая, без обводки и без текста внутри** — технология ещё не открыта, клетка не выдаёт даже цену.
- **Справа — вторая вертикальная колонка (`.right-rail`)**, зеркальная левой по ширине и по принципу «без пустот» (см. 11.5) — ряд из 4 кнопок сверху, список городов, склад снизу.
- **Карта — всегда по центру всего окна**, а не по центру оставшегося после панелей места: обе колонки — реальные равные `flex: 1 1 0` соседи карты, а не панель + фиктивный отступ, как в самой первой версии раскладки. Ширина карты определяется её собственной высотой (карта почти всегда упирается в высоту, не в ширину), и именно эта вычисленная ширина, а не фиксированное число, определяет, сколько места остаётся панелям с обеих сторон — на широком окне они заметно шире условного минимума (`MIN_RAIL = 210px`). В отличие от конструктора карт, широтные подписи слева от карты в клиенте скрыты.
  - **Сама сетка гексов внутри канвы теперь центрирована симметрично** (баг-репорт — «карта не центрована по вертикальной оси симметрии»): старая формула `mapContentWidth` считала поля независимо от фактического отступа `MapRenderer.root` (20px для клиента без подписей широт), из-за чего левое поле выходило ~2px, а правое ~55px — сетка была ощутимо прижата влево. Новая формула (`2 × отступ + фактическая ширина сетки`) выводится из того же отступа, так что поля совпадают с обеих сторон.
- **Фильтры отображения карты** (`#map-filters`, правый верхний угол) — три галочки «Юниты / Города / Ресурсы», по умолчанию все включены. Юниты и города рисуются в собственных Pixi-контейнерах внутри `markerOverlay` (порядок добавления = порядок отрисовки: маршруты → города → юниты, юниты видны поверх всех, как и запрошено), ресурсы — общий с редактором карт `renderer.markerLayer` целиком прячется/показывается одной галочкой.
- **Лес рисуется по краю гекса, не в центре** (правка — раньше пятна леса были смещены ближе к центру клетки, где и так толпятся иконки ресурсов/городов/юнитов): блобы леса (`drawForestPatch`, `map/renderer.ts`) теперь ставятся на расстоянии 0.55–0.8 радиуса гекса от центра вместо 0–0.45, оставляя середину клетки свободной.
- **Зум и панорамирование карты** — колесо мыши масштабирует **только саму карту** (мировой контейнер внутри Pixi, `renderer.root.scale`/`.position`, а не CSS-размер канваса), диапазон 1×–5×, с привязкой к точке под курсором. Остальной интерфейс (панели, колода, нижняя панель) не масштабируется вообще и не сдвигается. При зуме > 1× карту можно панорамировать перетаскиванием; порог в 5px отличает перетаскивание от клика, чтобы панорамирование не путалось с постановкой жетона в фазе расстановки (1.3). Клик/панорамирование реализованы через `pointerdown`/`pointerup` с `setPointerCapture` — обёрнуты в try/catch: если захват указателя не удался (нестандартный ввод), обработка клика всё равно должна дойти до конца, а не оборваться неперехваченным исключением.
- **Колода** — рубашкой вверх, отдельной узкой колонкой в нижней панели, сразу слева от руки (не сбоку от карты, как в первой версии клиента) — карты зрительно «выходят» из неё в руку. Показывает число оставшихся в ней карт. Игра начинается с пустой рукой (0 карт) — набор идёт по ходу игры (см. 2.3).
- **Нижняя панель** (три колонки: счётчик слева, зона колода+рука+баланс по центру, кнопка справа):
  - Слева — счётчик действий: ряд пипсов (кружков) шириной ровно с одну карту, число пипсов = базовое количество действий за ход (см. 2.2).
  - По центру — колода, затем 7 слотов руки (упакованы слева без дыр, см. 2.3), затем карточка баланса денег — того же размера, что карта в руке, но с жёлтой обводкой, чтобы не путать её с картой (см. 11.5).
  - Справа — кнопка завершения хода.
  - В фазе стартовой расстановки (1.3) вместо счётчика действий, слотов руки и баланса показывается индикатор текущего игрока и номинал следующего жетона; колода в этой фазе тоже видна.
- Иконки карт в руке пока без подписи текстом (только форма/цвет по типу — действие или событие) — конкретное содержимое руки на старте не определено дизайном.

### 11.5. Правая панель — одно окно: вкладки вместо модалок, продажа на месте

**Редизайн (текущая версия) — вся игра в одном окне**, без отдельных всплывающих модалок для Рынка/Дипломатии/Гос. управления: раньше эти три открывались поверх стола (`#side-modal-backdrop`), теперь они — вкладки самой правой панели, подменяющие список городов+склад на месте. Модалки остались только у по-настоящему кратких точечных выборов (юнит Воина, цена продажи, выбор технологии, налоговая недоимка, катастрофа, победа) — см. `renderModal()`/`ModalKind`.

Реализовано в `main.ts` (`renderActionButtons`, `switchRightPanelView`, `renderRightPanelExtra`, `renderCityList`, `renderWarehouse`).

- **Верхний ряд — 4 кнопки-вкладки, ровной ширины: «Ресурсы» / «Купить (N)» / «Дипломатия» / «Гос. управление»**, активная подсвечена (`.side-btn.active`). Клик переключает `rightPanelView` (`"resources" | "market" | "diplomacy" | "government"`) и перерисовывает панель — ничего не открывается поверх карты.
  - **«Ресурсы»** — исходный/дефолтный вид: список городов + склад (как было). Возврат к нему — по этой же кнопке.
  - **«Купить»** — подменяет город+склад списком всех лотов рынка, **отсортированным по цене от дешёвых к дорогим**, с собственной прокруткой (`#right-panel-extra`, `overflow-y: auto` — список не ограничен по высоте панели). Каждая строка — что, продавец, цена, кнопка «Купить» (или пометка «ваш лот»).
  - **«Дипломатия»** — реальная система отношений и предложений между игроками, круговая схема + составитель, см. 11.7 (раньше был статичный справочный список). **«Гос. управление»** — та же механическая разметка, что раньше была в модалке, просто отрисована в `#right-panel-extra` вместо `#side-modal-backdrop`, см. 11.6.
- **Продажа переехала: кнопки «Продать» больше нет — точки входа контекстные, обе ведут сразу к выбору цены (модалка `sell-price`, без промежуточного «нажмите кнопку, потом нажмите на цель»):**
  - **Карта действия** — попап выбора розыгрыша карты (тот же, что открывает «▶ Играть» и именованные действия Поселенца/Учёного/т.д., см. 3.1.х/3.2.х) теперь содержит и кнопку **«💲 Продать»** (только у `kind === "action"` — у карт событий её нет, продать нельзя). Клик сразу открывает выбор цены.
  - **Ресурс со склада** — у каждой единицы ресурса на панели склада своя маленькая кнопка **💲** (только у настоящих ресурсов карты, `warehouse` — не у Электричества/Промтоваров/Контента, `buildingResources`, 4.4). Всегда кликабельна, без отдельного режима — клик сразу открывает выбор цены.
  - Дальше — без изменений: цена **1–10**, Esc до выбора цены отменяет бесплатно (ничего не списано). **Карта** — цена выбрана → карта **не уходит из руки**, слот помечается проданным (**красная рубашка**, `isSlotListed`), недоступен для розыгрыша/повторной продажи, пока не купят; продажа не тратит действие; если карта попадёт в вынужденный сброс за перебор (2.3.1) раньше покупки — лот снимается автоматически, карта разыгрывает обычный эффект сброса. **Ресурс** — цена выбрана → 1 единица **сразу списывается со склада**. **Деньги продавцу — только по факту покупки** (`buyListing()`), никогда раньше.
- **Рынок торгует и картами, и ресурсами** — лот либо карта (при покупке физически удаляется из слота продавца, `sellerSlotIndex`), либо 1 единица ресурса (от игрока или из тестового посева, 3.1.1). Покупка карты кладёт её в руку (если есть место), покупка ресурса — на склад. **Видимость «только торговая сеть или сосед» пока не реализована** — показаны лоты всех игроков без фильтра.
- **Слот-индекс проданной карты «плавает» вместе с рукой** — рука хранится плотным массивом (2.3), и любое другое удаление карты слева от проданной (розыгрыш, вынужденный сброс) сдвигает её позицию; `shiftListingSlotsAfterRemoval()` вызывается из каждой такой точки (`consumeHandCard`, `playCard`), чтобы лот всегда указывал на ту самую карту. Проверено живьём: карта в слоте 2 выставлена, карта в слоте 0 сыграна (сдвиг руки на 1 влево), лот скорректировался на слот 1 автоматически, покупка сняла именно верную карту.
- **«Дипломатия» теперь полноценная механика** — реальные отношения и составные предложения между игроками, см. 11.7. **«Гос. управление» — тоже полноценная механика**, см. 11.6.
- **Список городов** — до `MAX_CITIES = 8` слотов (см. 9 — лимит городов), пустые слоты показываются пустыми, а не скрываются. У занятого слота — номер города, население (👥) и реальные ресурсы его региона (`resourcesInRegion()`): потраченный в этом цикле тип затемняется, тип без нужной технологии добычи (3.1.1) показан значком 🔒. В режиме «Увеличить население» / «Выбрать город» (Воин) строки своих городов кликабельны — золотая рамка. Список — для текущего игрока хода.
- **Склад** под списком городов — реальное хранилище по игроку (3.1.1), заполняется покупками на рынке и добычей (3.1.4/3.1.6). Вместимость 6 суммарно, 12 со зданием Склад — переполнение просто не влезает, а не отказывает действие целиком.
- Баланс денег — общий, без встроенного источника дохода: старт 0 у всех игроков; источники движения — продажа/покупка карт и ресурсов на рынке, включая автоматическую докупку недостающей пищи картой «Поселенец» (3.1.1).

### 11.6. Гос. управление — парадигмы, религия, сводка (`main.ts` — `adoptParadigm`/`adoptReligion`, вкладка `rightPanelView === "government"`)

Кнопка «Гос. управление» переключает правую панель на вкладку из 5 блоков (см. 11.5 — больше не отдельная модалка): политическая парадигма, религия, сводка по своей стране, сводка по партии, справочный список стратегий ИИ. Всё, кроме последнего блока и оборонного эффекта Монархии, реально считается по текущему состоянию игры, не заглушка.

- **6 политических парадигм, каждая привязана к своей технологии** (проверено — это уже было верно в самих данных `techtree.ts`, просто раньше нигде не проверялось программно):

  | Парадигма | Технология | Эффект | Реализация |
  |---|---|---|---|
  | Монотеизм (община) | Мистицизм, Э1 | Прирост населения удвоен — Поселенец/Население растят сразу 2 РАЗНЫХ города за один розыгрыш, не 1 | `citiesLeft`/`grownCityIds` в `PendingCardAction` — карта и действие списываются один раз (на первом городе), второй город бесплатен по действию, но платит свою обычную цену едой |
  | Монархия | Богословие, Э2 | Оборона юнитов +2 | **Design-only** — боевой системы в клиенте нет (раздел 6), применить не к чему, как и у Космонавтики/Арсенала раньше |
  | Парламентаризм | Экономика, Э3 | Раз в ход 1 бесплатная постройка (без действия) | `parliamentarismUsedThisTurn` (Set, сброс при начале хода игрока) — после `consumeHandCard` внутри стройки действие возвращается (`actionsLeft++`), ресурсы платятся как обычно |
  | Демократия | Права человека, Э5 | +1 действие в ход | `ACTIONS_PER_TURN + 1` при подготовке `actionsLeft` в конце СВОЕГО хода (та же точка, где это всегда предзагружалось на будущий ход) |
  | Фашизм | Идеология, Э5 | Воин строит 2 юнита за цену 1 | `buildUnit()` кладёт в `units` 2 объекта вместо 1 при той же цене |
  | Коммунизм | Коммунизм, Э6 | Строитель берёт доступ со всех городов своей торговой сети (по своим маршрутам, `ownRouteComponent`), не только столицы | `planBuildingSpend()` теперь принимает `AccessSource | AccessSource[]` — при Коммунизме передаётся весь массив городов сети, каждый access-кандидат помнит, из какого именно города он взят (`cityId`), не просто `source.id` |

  По умолчанию не выбрана ни одна («нигде не стоит галочка») — `playerParadigm[playerId] = null`. **Смена парадигмы (включая самую первую) пропускает игроку следующий ход** — тем же `skippedTurn`, что у негативной ветки «Мобилизации» (3.2.6). **При переходе на Коммунизм религия автоматически сбрасывается в «не выбрана»** (`playerReligion[playerId] = null` внутри `adoptParadigm`, не в `"atheism"` — при коммунизме религии нет вообще, это не осознанный выбор «нет религии», а недоступность выбора как такового) — пока действует эта парадигма, `canAdoptReligion` возвращает `false` и блок религии в этой же вкладке показывает «🚫 При коммунизме религии нет.» вместо списка.
- **Религия** — то же 6-вариантное состояние из 4.5 (5 религий + Атеизм), только выбирается ЗДЕСЬ, а не автоматическим попапом при исследовании Мистицизма, как было спроектировано изначально в 4.5 — окно «Гос. управление» стало реальной точкой входа. Доступна только после исследования «Мистицизм» **и только вне Коммунизма** (`canAdoptReligion` проверяет оба условия, см. выше). Выбор свободный из всех 6 (не только уже принятых другими) — но список показывает, кто уже её принял, и кто основатель:
  - **Основатель** религии — первый игрок, когда-либо её выбравший (`religionFounder`, назначается один раз, не меняется, даже если основатель сам сменит религию потом — исторический факт, не текущее членство).
  - **Основатель и сводка последователей — в подсказке при наведении (`title`), не инлайн-текстом** (редизайн 11.5 — экономия места в узкой правой панели): если смотрящий игрок — основатель (и религия не Атеизм), наведя на строку религии, он видит сколько игроков её приняли, сколько у них городов и суммарного населения (`religionFollowerStats`); иначе подсказка просто называет основателя или «ещё никем не основана».
  - Смена религии НЕ пропускает ход (в отличие от смены парадигмы — явно другое правило, не переиспользует `skippedTurn`). Кнопка «Выбрать» / пометка «текущая» — тот же паттерн, что у парадигмы, не изменился в редизайне.
  - Храм (4.4, ещё не в коде) сможет наконец использовать это состояние, когда дойдёт очередь до его реализации — вся инфраструктура для его дохода («у кого религия совпадает с моей») теперь готова.
- **Сводка по своей стране**: городов (N/8) и население (список по городу + сумма); войск и зданий по количеству плюс их суммарное содержание, если наступит фаза налогов (`unitsAndBuildingsUpkeep` — та же формула, что в «Соберите налоги», 3.2.2, юниты + здания); доход торговой сети за 1 уникальный торговый ресурс — буквально множитель «города сети» из формулы Торговца (3.1.6, `tradeNetworkOf(capital).cities.length`), от столицы, раз само окно не привязано к конкретному городу.
- **Сводка по партии**: ходов до конца (`turnsRemaining`, старт 60, убывает на 1 за каждый отдельный ход в `onPlayingEndTurn()` — не за цикл; **чисто информационный счётчик, ничего не завершает при достижении 0**, как и territorial victory формально не блокирует игру, см. 9); лидер по населению планеты (с % от общего — победа по этому критерию нигде в клиенте не запускается автоматически, только отображается); лидер по городам (из 9, тот же лимит, что у territorial victory, 9); лидер в космосе (из 3 компонентов, `SPACE_VICTORY_COMPONENTS`) — **счётчик `spaceComponents` существует, но у всех всегда 0**, потому что Космодром (4.4) ещё не перенесён в код, это задел на будущее; лес на своей территории (тот же скан регионов со своими городами, что у `degradeForestOrLand`, 3.2.4, просто считает, а не меняет).
- **Стратегия ИИ** — чисто справочный список из 5 названий (Военная/города, Космическая, Население, ООН, Окончание по времени). **В клиенте нет никакого ИИ вообще** (3-человеческий hotseat-прототип, ТЗ 8.2) — этот список ни на что не влияет и ни к чему не подключён, ровно как раньше был справочный список парадигм до этой сессии.

### 11.7. Дипломатия — круг из 6 слотов, статусы, составные предложения (`main.ts`, вкладка `rightPanelView === "diplomacy"`)

Кнопка «Дипломатия» переключает правую панель на круговую схему отношений плюс составитель сделок под ней. Реально считается по игрокам, не заглушка — только сами ЭФФЕКТЫ конкретных соглашений (кроме передачи денег/ресурсов/городов) design-only, см. ниже.

- **Статус пары игроков — `war: boolean` + `agreements: Set<Agreement>`** (`relationOf(a,b)`, ключ — неупорядоченная пара id). **Война эксклюзивна** — объявлена, значит все соглашения этой пары автоматически снимаются, технология не нужна. Всё остальное — «мирные» состояния: **Мир — это просто отсутствие войны и соглашений** (нейтралитет по умолчанию, ничего специально выбирать не надо), а поверх мира можно накопить **сколько угодно соглашений одновременно** (Открытые границы + Торговый союз и т.д. — не взаимоисключающие), каждое своей технологией — тот же список из 6, что раньше был статичным текстом: Открытые границы (Письменность), Вассалитет (Феодализм), Совместная оборона (Кодекс законов), Торговый союз (Гильдии), Научное сотрудничество (Книгопечатание), Союз (Коммунизм).
- **Круговая схема (`diplomacyCircleHtml`)** — 6 фиксированных позиций по кругу (SVG), **свой игрок всегда внизу** (слот 0), остальные живые игроки распределены по кругу от него в порядке id; лишних слотов (при < 6 игроков в партии — сейчас тестируется 3 из 2–6 по дизайну) — пустые пунктирные кружки без иконки, не кликабельны. Под каждой цветной иконкой — подпись именем игрока (`pl.name`, формат «Игрок N»; когда появится ИИ — та же подпись без специальной пометки «бот», отдельного признака человек/ИИ в модели игрока нет и не планируется).
- **Линии между каждой ПАРОЙ живых игроков** (не только относительно текущего) кодируют статус: **красная толстая** — война; **зелёная** — мир с хотя бы одним активным соглашением (какими именно — видно, выбрав игрока целью, см. ниже; на самой линии соглашения не перечисляются по отдельности ради простоты схемы); **тонкая пунктирная серая** — чистый нейтралитет, без войны и без соглашений.
- **Выбор цели** — клик по любой ЧУЖОЙ иконке на схеме (не своей) устанавливает её целью (`diplomacyTargetId`), под схемой открывается составитель для этой пары.
- **Два односторонних мгновенных действия** (не требуют согласия цели — как в реальности, войну не объявляют «с разрешения», а от действующих соглашений всегда можно отказаться в одностороннем порядке): **⚔ Объявить войну** (`declareWar`) и **🚫 Прекратить отношения** (`breakOffRelations`, тот же эффект на мирную пару — сброс до нейтралитета без войны).
- **Составное предложение — 9 условий, любое сочетание, отправляется и решается ЦЕЛИКОМ** (ТЗ: «несколько вариантов можно компоновать в одно предложение... либо отклонено, либо принято»):

  | Кнопка | Условие (`ProposalTerm.kind`) | Что делает при принятии |
  |---|---|---|
  | 🤝 Сменить статус | `agreement` | Добавляет выбранное соглашение к паре (список показывает все 6 всегда — недоступные (нет технологии у отправителя, или уже действует) серые, без кнопки, а не скрыты, по прямому запросу; недоступно при войне вообще) |
  | 🕊 Заключить мир | `peace` | `war = false` (кнопка показана только когда пара реально в состоянии войны) |
  | 💰 Потребовать денег | `demandMoney` | Получатель платит отправителю выбранную сумму (быстрый выбор 1/2/5/10/20/50) |
  | 💸 Предложить денег | `offerMoney` | Отправитель платит получателю |
  | 🏙 Передать город | `giveCity` | Один из городов ОТПРАВИТЕЛЯ (список на выбор) переходит получателю |
  | 🏙 Попросить город | `demandCity` | Один из городов ПОЛУЧАТЕЛЯ переходит отправителю |
  | 📦 Попросить ресурс | `demandResource` | N единиц выбранного ресурса со склада получателя — отправителю (клик «+1» по конкретному ресурсу, можно повторно нажать, чтобы увеличить N, вплоть до того, что было на складе в момент составления) |
  | 📤 Передать ресурс | `giveResource` | То же самое, только со склада отправителя — получателю |
  | ⚠ Ультиматум (чекбокс, не отдельная кнопка) | `Proposal.ultimatum` | Не условие, а флаг всего предложения: если получатель ОТКЛОНЯЕТ — отправитель автоматически объявляет ему войну (`declareWar`), а не просто ничего не происходит |

  Деньги/ресурсы/города **клампятся к фактическому состоянию в момент решения**, не в момент отправки (`applyProposalTerms`) — если получатель к тому времени потратил деньги или лишился ресурса, спишется сколько реально есть, не больше.
- **Решение — в начале хода получателя, не сразу** (по явному запросу — «в начале хода получателя», не мгновенно всплывающим окном у отправителя): предложение уходит в очередь `pendingProposals`; `onPlayingEndTurn()`, определив нового текущего игрока (уже после пропуска ходов за «Мобилизацию», 3.2.6), вызывает `checkPendingProposalsForCurrentPlayer()` — если для него есть висящее предложение, открывается модалка (`activeModal === "proposal-review"`) со списком условий и кнопками ✅ Принять/❌ Отклонить; после решения очередь проверяется снова для того же игрока (несколько предложений от разных отправителей разбираются по одному, сколько накопилось). Модалку можно закрыть крестиком, не решив — предложение просто останется висеть в очереди, ничего не ломает, но и не пропадёт само.
- **Частично design-only** — сама структура соглашения (флаг `agreements.has(...)`) сохраняется и видна для всех шести; из эффектов реально подключены **только Открытые границы** (пускают через чужую территорию юнитом без объявления войны, 6.8) и, задним числом, **Монархия** (больше не design-only — теперь удваивает базовую защиту в бою, 6.8, 11.6). Остальные так и без эффекта: Вассалитет не отчисляет доход сюзерену; Совместная оборона не подключена к объявлению войны союзника; Торговый союз не освобождает от пошлины в `tradeNetworkOf`/`tryTraderTrade`, 3.1.6; Научное сотрудничество не уравнивает ветки технологий; единоверный бонус в бою (4.5) тоже всё ещё не подключён, хотя боевая система теперь есть и подключить было бы несложно. Сама негоциация (условия статуса, деньги, ресурсы, города) — по-настоящему рабочий обмен состоянием игры, не заглушка.

### 11.8. Стартовое меню (`web/start.html`, `web/src/start/`)

Отдельная страница ПЕРЕД самим игровым клиентом (`game.html` по-прежнему открывается напрямую с дефолтными 3 игроками — удобно для быстрой проверки/дебага) — 4 режима:

- **«Против AI»** и **«Интернет»** — не реализованы, кнопки честно так и говорят при клике («Не реализовано» + короткое пояснение почему), не спрятаны и не задизейблены совсем.
- **«За одним компьютером» — полноценно работает.** От 2 до 6 игроков (степпер), у каждого свой ник (текстовое поле, по умолчанию «Игрок N») и цвет (`<input type="color">`, подсказка «цвет повторяется», если два игрока выбрали одинаковый — не блокирует старт, просто предупреждает). «Начать игру» пишет выбор в `sessionStorage` (ключ `civ-setup`) и переходит на `game.html`; `placement.ts` читает его при загрузке модуля и строит `PLAYERS` оттуда (`loadPlayersFromSetup`) — если ключа нет (прямой заход на game.html), используется старый дефолт из 3 игроков. Сам игровой клиент ничего не знает про существование меню — просто получает готовый список игроков.
- **«Инструкция»** — статичная страница с кратким описанием механик (циклы/ходы, города/ресурсы, карты, здания/технологии, юниты/бой, дипломатия) — не копия этого ТЗ, а сжатая выжимка для игрока.
- **«Загрузить игру»** — пятая кнопка на этом же экране, появляется только если в `localStorage` (`civ-save`) есть сохранение. Читает список игроков прямо из сохранения, пишет его в тот же `civ-setup` (чтобы `PLAYERS` в game.html поднялся именно таким, каким он был на момент сохранения — не дефолтным), ставит одноразовый флаг `civ-load-pending` в `sessionStorage` и переходит на `game.html`, которое сразу после обычной инициализации подхватывает сохранение целиком.

### 11.9. Сохранение и загрузка (`main.ts` — `serializeGameState`/`deserializeGameState`, ESC-меню паузы)

- **ESC открывает меню паузы**, если ничего другого не было в процессе (сначала, как и раньше, ESC отменяет текущий выбор цели/юнита, если он есть — меню паузы открывается только на «пустой» ESC). Повторный ESC, пока меню открыто, закрывает его же, не заново отменяя что-то. Меню: ▶ Продолжить, 💾 Сохранить игру, 📂 Загрузить игру (недоступна, если сохранения ещё нет), 🏠 Выйти в главное меню (`start.html`, обычный переход без предупреждения — по прямому запросу «добавь кнопку», без доп. уточнений про подтверждение выхода).
- **Один слот сохранения** (`localStorage`, ключ `civ-save`) — не несколько именованных, полная перезапись при каждом сохранении.
- **Что сохраняется**: игроки (имя/цвет), фаза партии, вся карта (`doc.tiles`), города, юниты, рынок, торговые пути, склад/деньги/руки/колода/действия, исследованные технологии, владение зданиями, парадигма/религия каждого игрока, дипломатические отношения (война/соглашения по каждой паре), очередь дипломатических предложений, все «использовано в этом цикле» флаги (доступ к ресурсам, здания-производители, поддержка юнитов, только что высадившиеся), защита клеток, счётчик ходов до конца.
- **Что НЕ сохраняется (осознанно)** — любое «в процессе» состояние интерфейса: открытая модалка, что выбрано в составителе дипломатического предложения, какой юнит выделен/наведён, какая карта раскрыта попапом. После загрузки всё это просто пусто, как в начале обычного рендера — не ломает партию, просто не помнит промежуточный клик.
- **Загрузка прямо в игре** (через ESC-меню, без ухода на `start.html`) корректно восстанавливает состояние партии, но НЕ подменяет уже поднятых в этой вкладке `PLAYERS` (имена/цвета) — если загружаемое сохранение принадлежит другой расстановке игроков, картинка может разойтись по именам/цветам с тем, что было при сохранении. Загрузка со стартового экрана (11.8) этого не имеет — там `PLAYERS` поднимается из самого сохранения перед стартом.

---

## 12. Технологический стек и платформы

### 12.1. Движок (пересмотрено)

- **Web-стек: TypeScript + PixiJS** (2D WebGL-рендер карты) **+ HTML/CSS** для интерфейса (панель ресурсов, рука карт, окна построек/технологий). Единая кодовая база для всех платформ — браузер.
- Изначально был выбран Unity, но отказались от него на этапе прототипа: цикл разработки в Unity (батчмод-сборка, лицензия, компиляция) занимал 5–15 минут на каждую проверку, тогда как веб-стек даёт мгновенную обратную связь (dev-сервер с hot reload, прямой доступ к консоли/DOM браузера). Поскольку Steam — не близкая цель, а мобильный сценарий и так подразумевал браузер, минусов у смены не увидели.
- Наработки по генератору карты (структура регионов/ресурсов, счётчики) переносятся на TypeScript — сама логика не переделывается с нуля, меняется только язык/платформа.

### 12.2. Платформы и сборки

| Платформа | Сборка | Дистрибуция |
|---|---|---|
| Мобильный телефон / планшет / ПК (браузер) | Обычная веб-сборка (Vite) | Открывается по ссылке в браузере, без установки, без магазинов приложений |
| PC (Steam) | Та же веб-сборка, обёрнутая в Electron | Steam, когда до этого дойдёт очередь (не приоритет на старте) |

### 12.3. Ограничения, которые нужно учитывать

- Размер загрузки и производительность рендера карты должны быть под контролем на мобильных браузерах (много гексов на экране при минимальном зуме).
- Ввод должен поддерживать touch наравне с мышью/клавиатурой (см. раздел 11 — зум карты, работа с картами и панелями).
- Steam-специфичные функции (достижения, оверлей) появятся позже вместе с Electron-обёрткой; в чистой браузерной версии не реализуются.

### 12.4. Сеть

- Клиент-серверная архитектура (см. раздел 8) одинаково обслуживает всех веб-клиентов вне зависимости от платформы — сервер не различает мобильный/десктопный браузер.

### 12.5. Локальный запуск для разработки

- **[ИСПРАВЛЕНО] Единый файл запуска** (`start-server.bat`, корень репозитория, по прямому запросу) — раньше поднимал только клиентский dev-сервер (порт 5183), backend приходилось запускать отдельно вручную. Теперь одним двойным кликом: завершает любые процессы, уже слушающие порты 8787 (backend, `web/server`) и 5183 (клиент, `web/`), поднимает оба заново каждый в своём окне консоли (с текстовым баннером внутри окна — заголовок окна не всегда закрепляется, потому что `npm run dev` может сам его менять после запуска, это чисто косметика), ждёт несколько секунд и открывает `http://localhost:5183/start.html` в браузере по умолчанию. Повторный запуск при уже работающих серверах корректно их перезапускает.

---

## 13. Открытые вопросы реализации (нужно закрыть)

Все 6 карт действий (3.1) реализованы и живьём протестированы, но внутри них остались осознанно отложенные хвосты — записано здесь, чтобы не потерять, когда до них дойдёт очередь.

- **Дипломатия между игроками — реализована** (круг из 6 слотов, статусы война/мир+соглашения, составные предложения, решение в начале хода получателя — см. 11.7). Открыт только более узкий хвост: **игровые ЭФФЕКТЫ конкретных соглашений** (Открытые границы, Вассалитет, Совместная оборона, Торговый союз, Научное сотрудничество, Союз) сами по себе ни на что не влияют — сохраняется только флаг «соглашение действует», подключение к другим системам (проходу юнитов, пошлине, лидерству по веткам технологий и т.д.) не сделано, см. следующие два пункта.
- **Торговец (3.1.6): торговая сеть — заглушка.** `tradeNetworkOf()` возвращает все города игрока без исключения, хотя по определению сеть — это города, реально связанные торговыми маршрутами (`tradeRoutes`, 3.1.7, уже существуют как объекты, просто не читаются здесь). Из-за этого не с чем сравнивать «чужой маршрут», поэтому пошлина владельцу маршрута (1 💰 флэтом, требует «Торгового союза», теперь реально выбираемого статуса, см. 11.7 — но сама пошлина всё ещё не списывается) — место в коде помечено комментарием в `tryTraderTrade()`.
- **Учёный (3.1.8): цена открытия по эпохе (4.3) не списывается.** Само лидерство по ветке и досрочное открытие эпохи работают; не хватает только подключения `planResourceSpend`/`commitSpend` к розыгрышу карты (Электричество/Промтовары теперь есть, см. 4.4, но берутся не из `warehouse`, а из отдельного `buildingResources`). Соглашение «Научное сотрудничество» (уравнивание с лидером ветки без штрафного отставания) теперь можно заключить (11.7), но сам эффект уравнивания веток по-прежнему не подключён — статус существует, применение нет.
- **Воин (3.1.2): юниты стоят на месте.** Построенный юнит появляется на гексе города, но движения и боя всё ещё нет — по плану сделаем позже, отдельным этапом (боевая система описана в разделе 6, но ничего оттуда не подключено к клиенту).
- **Переработка конкретных зданий + защита Космонавтики от ядерного оружия (4.4) — всё ещё зафиксированы в ТЗ, не в коде** (правило «до 2 владельцев» из этого же пункта — уже реализовано, см. 4.4/11.4, вычеркнуто отсюда). Аэропорт (был Роботоцех, тех. Авиация вместо Робототехники), Ядерный арсенал (было Военное министерство, тех. Атомная энергия вместо Всеобщей воинской повинности), Рынок (была Биржа), Радиовышка (был Банк, производит Контент — новый building-only ресурс, не случайные ресурсы карты), Храм (был Арсенал — не бонус урона, а сжигание карты руки в доход по религии, тех. Мистицизм вместо Нарезного оружия), новый Космодром, новый Университет (открытие технологии как у «Учёного» + 5💰) и Управление/Интернет полностью описаны в 4.4 с ценой активации у каждого, но `buildings.ts`/`main.ts` всё ещё содержат старые версии этих конкретных зданий — ничего из этого списка не перенесено в код. У Аэропорта цена активации — предполагаемый дефолт, не подтверждённый явно (см. таблицу 4.4) — стоит подтвердить перед реализацией.
- **Принятие религии игроком (4.5) — реализовано** (`playerReligion`/`adoptReligion`/`religionFounder`, окно «Гос. управление», 11.6). Единоверный бонус в бою (−1 урон атакующему при войне единоверцев) остаётся design-only — боевой системы всё равно нет. Сам Храм (4.4) — единственное, что ещё блокирует эта система: здание не перенесено в код, хотя вся инфраструктура дохода под него уже готова.

---

## 14. Предстоящие правки (очередь на реализацию, зафиксировано по прямому запросу — сначала список, потом коммит/пуш, реализация поэтапно ПОСЛЕ)

Список записан со слов пользователя как есть, без предварительного анализа реализуемости — уточнения и решение неоднозначностей делаются на своём шаге, не здесь.

1. **Военные юниты в модалке города.** Показать юнитов в резерве города символами (если хватает места в модалке), клик по юниту в этом списке подсвечивает его на карте перекрестием и открывает стандартный набор действий внизу карты (тот же, что при клике по юниту на самой карте) — так можно вызвать и сходить конкретным юнитом из резерва города, не выцеливая его на карте вручную.
   - **[ИСПРАВЛЕНО] Ручная смена активного юнита гарнизона** (по прямому запросу) — «игрок может сделать верхним юнитом в городе другого юнита, тогда текущий уйдёт в запас (защита от команды «Оборона», если была, снимается); отправить в запас можно только юнита, который ещё не совершал действий». В модалке города у каждого резервного юнита кнопка «⬆ Сделать активным» — доступна, только пока ТЕКУЩИЙ активный юнит ещё не отдавал ни одного приказа в этом цикле (движение/атака/оборона/грабёж — `GameSession.unitActedThisCycle`, новый Set, сбрасывается в начале цикла вместе с `landedThisCycle`/`outOfMoveThisCycle`); иначе кнопка неактивна с подсказкой почему. Реализовано как `UnitInstance.garrisonRank` — необязательное поле, при назначении переопределяет обычную сортировку очереди по `id` (по времени постройки): `cityGarrisonQueue` сортирует по `garrisonRank ?? id`, новое назначение всегда получает ещё более низкое значение (`nextGarrisonRank`, монотонно убывает), так что последнее ручное назначение побеждает. Бесплатно — не тратит действие/деньги. У юнита, ушедшего в запас, снимается `defending` (команда «Оборона» даёт эффект только активному); «Грабёж»/«Пиратство» НЕ снимается — это пассивный эффект по позиции клетки, действует независимо от активного статуса.
2. **[ИСПРАВЛЕНО] Перемещение/атака юнитов — проверить и почему-то не работают, почитать.** Плюс новое правило: каждое перемещение ИЛИ атака военным юнитом стоит 1 💰 (универсальная плата деньгами, не ресурсом). Поддержка и оборона остаются бесплатными. — Причина «юниты не двигаются»: `chargeUnitActivation` требовал конкретный стратегический ресурс из города приписки юнита; при его нехватке `commandUnit` молча отказывал и в перемещении, и в атаке. Заменено на плоское списание 1💰 (`this.money[unit.playerId] -= 1`), поддержка/оборона `chargeUnitActivation` не вызывают — бесплатны, как и требовалось. Заодно снято старое «плавающее» окно действий юнита поверх карты (дублировавшиеся кнопки «Оборона/Грабёж» в информационной панели юнита в `main.ts`) — единственное место управления юнитом теперь нижняя панель команд (`#unit-command-bar`).
3. **[ИСПРАВЛЕНО] Баг: «Философия» не даёт эффект первооткрывателю** (разовый прирост населения +1 во всех городах) — сейчас, судя по всему, ничего не происходит при исследовании. — Причина: у технологий в `techtree.ts` есть только описательный `summary`, никакого механизма подключения игрового эффекта к конкретной технологии не было — «Философия» была текстом без кода. Добавлен явный хук в `GameSession.researchTech`: при `techId === "Философия"` и `isFirstDiscovery` (тот же принцип «бонус только первооткрывателю», что у авто-маршрута/религии) всем городам этого игрока население +1. Проверено пробником — первое открытие даёт +1 каждому городу, повторное исследование этой же технологии другим/тем же игроком (когда `techDiscoverer` уже занят) эффект не повторяет.
4. **Не показывать в окне выбора исследования недоступные игроку технологии** — сейчас, судя по всему, показывает и заблокированные варианты; должны быть видны только реально доступные для розыгрыша карты «Учёный» строки.
5. **Переработка правила открытости технологий (снова, см. историю в 3.1.8 и в самом коде — `syncBranchCatchup`/`branchDepth`/`availableResearchFor` в `GameSession.ts`).** По прямому уточнению — модель «бесплатная автоматическая раздача техов ниже границы» больше НЕ та, что нужна. Новое правило (насколько сформулировано, требует уточнения на своём шаге): технология уровня N+1 в ветке становится ДОСТУПНОЙ ДЛЯ ИССЛЕДОВАНИЯ (за карту+действие+ресурсы, НЕ бесплатно) любому игроку, если технология уровня N той же ветки уже исследована КЕМ-ТО (не обязательно тем же игроком, не обязательно лично запрашивающим). Одну и ту же технологию МОГУТ исследовать несколько разных игроков по очереди (переоткрытие разрешено), но игровые бонусы «первооткрывателя» (авто-прокладка торгового маршрута у маршрутных технологий, право основать религию) достаются только первому, у повторных исследователей — просто юниты/здания технологии, без этих бонусов. Живой пример от пользователя: если кем угодно уже открыты «Бронзовое дело», «Колесо» и «Каменная кладка» (позиции 0 каждой из веток 1/2/3), то любому исследующему уже доступны «Горное дело», «Письменность» и «Мистицизм» (позиции 1 тех же веток) — а «Гончарное дело» (позиция 1 ветки 4) НЕ доступно, потому что «Мореплавание» (позиция 0 ветки 4) пока никем не открыто. Упомянутое в исходной формулировке «3 и 4 технологии» — скорее всего отсылка к уже работающему правилу досрочного открытия эпохи (3-4 ветки затронуты, см. `maxEligibleEpoch`), а не отдельный счётчик для этого правила — уточнить на своём шаге, пример важнее общей фразы.

   **[ИСПРАВЛЕНО, отменяет абзац выше про «не бесплатно» — версия 2, первая попытка исправления была неверной] Автоматическая раздача технологий ниже по ветке — ВСЕМ игрокам, не только исследовавшему.** Первая попытка исправления (раздавать только тому, кто сам исследовал технологию) оказалась неверной трактовкой — пользователь уточнил явно и многократно: «Технология предыдущая в ветке изучается автоматом ВСЕМИ игроками, как только КАКОЙ-ТО игрок открыл НОВУЮ технологию в ветке». Правило теперь такое:
   - Игрок, лично исследующий технологию на позиции P своей ветки, как и раньше, автоматически получает себе и все технологии позиций 0..P-1 той же ветки, которых у него ещё нет (личный бэкфилл, `grantWithBackfill`).
   - **Если это НОВАЯ технология — то есть ещё никем в партии не открытая** (продвигает коллективную границу ветки: `techDiscoverer[techId]` был не задан до этого момента) — тогда ВСЕ ОСТАЛЬНЫЕ игроки в партии (не только исследовавший) тоже автоматически и бесплатно получают себе все технологии позиций 0..P-1 той же ветки, которых у них ещё нет. Именно это и есть правило из живого примера: красный первым в партии открывает Мистицизм (позиция 1, ветка та же, что у Каменной кладки) → ВСЕ остальные игроки (не только красный) сразу получают Каменную кладку (позицию 0), даже если сами ничего не делали.
   - Сама позиция P (только что открытая новая технология) остальным игрокам НЕ раздаётся — её каждый должен исследовать лично, точно так же, как и раньше. Раздаётся только то, что СТРОГО РАНЬШЕ P в этой же ветке.
   - Если технология НЕ новая (кто-то её уже открывал раньше — «переоткрытие») — трансляция всем игрокам НЕ срабатывает, только личный бэкфилл того, кто сейчас её переоткрыл (иначе каждое переоткрытие снова и снова раздавало бы всем то, что и так уже давно всем доступно).
   - Бонусы первооткрывателя (маршрут/религия/«Философия») применяются к каждой раздаваемой технологии отдельно — если получатель (в личном бэкфилле или в общей трансляции) оказывается первым, кто её лично получает (обычно нет, раз она уже коллективно открыта, но не исключено).
   - Реализовано в `GameSession.researchTech` (решает, нужна ли трансляция всем) + `grantWithBackfill` (личный бэкфилл одного игрока) + `grantSingleTech` (один грант с бонусами). Эпоха каждого затронутого игрока пересчитывается один раз по итогу его личной пачки технологий, не после каждой отдельной.
6. **[ИСПРАВЛЕНО] Механизм перемещения юнитов «не работает» — перепроверить с нуля**, и следом живой тест боя: расставить юнитов разных категорий и попробовать взять город, обороняемый укреплённым юнитом, тройкой разных атакующих юнитов. — Живой серверный тест (2 игрока, город с укреплённым Копейщиком, 3 разных атакующих юнита Э1) нашёл вторую, отдельную от п.2 причину: `commandUnit` считал город «вражеской целью» (веткой атаки) по одному владельцу клетки — попытка зайти в беззащитный вражеский город ВСЕГДА уходила в атаку вместо перемещения, `moveOrder` не выставлялся, юнит физически не мог войти и захватить город. По итогам теста пользователь уточнил всю модель захвата города заново (см. §6.9) — гарнизон города это его НАСЕЛЕНИЕ, отдельных юнитов-защитников резерва нет; пробитие буфера осады снимает ровно 1 население и открывает окно захвата ДО КОНЦА ЭТОГО цикла, не успели — на новом цикле гарнизон восстанавливается заново, но слабее (из уже сниженного населения); население 0 — город уничтожается целиком, оставляя руины (не остаётся как захватываемая пустышка). Пока в городе жив хотя бы один размещённый защитник — гарнизон-по-населению урона не получает вообще, весь урон идёт по этому защитнику как в обычном бою. Всё реализовано в `GameSession` (`cityGarrisonDefense`/`citySiegeBuffer`/`destroyCity`/`commandUnit`/`resolveCombat`/`resolveUnitMovementForCycle`) и проверено живым тестом сервера (4 сценария — см. §6.9). Отступление защитника при уроне, полностью погашенном буфером защиты клетки, не срабатывает при реальных характеристиках юнитов (сравнение `defender.hp <= attacker.hp` не выполняется, если атакующий не сильно превосходит защитника по HP) — окружённый со всех сторон (отступать некуда) защитник погибает, а не отступает; штатное поведение существующей боевой формулы, отдельного бага не обнаружено.
7. **[ИСПРАВЛЕНО] Уведомление обо всех последствиях вынужденного сброса карт.** Сейчас негативные эффекты сброшенных карт применяются молча — нужно показать игроку, что именно произошло (по каждой карте). Если сокращение населения из сброса привело к потере столицы — партия для этого игрока завершается: все его города, юниты и торговые маршруты снимаются с карты (см. п. 12 — та же механика «выбывания», общая для всех причин потери всех городов). — По прямому уточнению доработано до полноценного окна подтверждения: конец хода с рукой ≥8 больше не сбрасывает её сразу — сервер (`GameSession.endTurn`, без `confirmed`) возвращает превью (`ActionResult.needsDiscardConfirm`) вместо применения, клиент показывает модалку со списком последствий по каждой карте и двумя кнопками: «Принять и завершить ход» (повторяет `endTurn` с `confirmed:true` — реальный сброс) и «Попробовать что-то ещё» (просто закрывает окно, ничего не шлёт на сервер — доступна, только если `actionsLeft > 0`, иначе рендерится одна «Принять»). Детерминизм — превью считается через `previewHandOverflowDiscard`, которое прогоняет ТОТ ЖЕ `resolveHandOverflowDiscard` (он переписан, чтобы возвращать `string[]` с описанием по каждой карте) на ПОЛНОСТЬЮ независимом клоне сессии (`GameSession.fromJSON(id, structuredClone(toJSON()))` — форкает RNG ровно с текущей позиции по `rngCallCount`) — живую сессию превью не трогает вообще, можно звать сколько угодно раз без побочных эффектов; реальное подтверждение гарантированно даёт те же последствия, если между превью и подтверждением никто больше не походил (в пределах одного хода это всегда так). Заодно найден и исправлен реальный баг: `toJSON()`/`fromJSON()` — НЕглубокое копирование (вложенные массивы/объекты, включая руки игроков, передаются по ссылке — нормально для сохранения на диск, где старая сессия тут же выбрасывается, но не для клонирования живой сессии) — без `structuredClone` клон делил бы те же объекты с реальной сессией, и прогон превью тут же портил бы настоящую руку. Если последствия убивают игрока — тот же существующий `eliminatedPlayers`/модалка «Игрок выбыл» подхватывает это автоматически, без дополнительного кода. Проверено пробником (детерминизм двух подряд превью, отсутствие побочных эффектов, точное совпадение превью и реального исхода) и живьём через реальный WebSocket в браузере — включая сценарий, где сброс убил единственный город игрока и тут же показал модалку выбывания.
8. **[ИСПРАВЛЕНО] Постоянные лоты биржи (рынка).** По 1 единице ровно 6 видов ресурса — Злаки, Рыба, Овощи, Силикаты, Металлические руды, Хлопок — ПОСТОЯННО доступны на бирже от казны/мира; цена растёт с каждой покупкой ЛЮБЫМ игроком, без верхнего предела (выше обычных 10💰, разрешённых игроку при выставлении своего лота). — Первая версия правки (5 партий по всем 20 ресурсам, ценами 1..5💰 фиксированно) была ошибочной, заменена полностью. Реализовано в `GameSession.seedStartingMarket()` (вызывается один раз при переходе в фазу `playing`) — заводит ровно 6 лотов (`WORLD_MARKET_RESOURCES`) по 1 единице, стартовая цена 1💰 (в правке явно не задана — предположение). Возобновление реализовано в ДВУХ местах, где лот от `WORLD_SELLER` может быть куплен — `buyListing` (явная покупка с биржи) и `commitSpend` (автопокупка ресурса как части оплаты цены здания/юнита/исследования): вместо простого исчезновения лота при покупке тут же создаётся новый лот того же ресурса по цене `price + WORLD_MARKET_PRICE_STEP` (шаг +1💰 — тоже предположение, явно не задан). Обычные лоты игроков (`sellResource`, капнутые на 10💰) не затронуты — возобновление проверяется строго по `sellerId === WORLD_SELLER` и принадлежности к списку из 6 ресурсов. Проверено пробником — 6 лотов после сидирования, 4 покупки зерна подряд поднимают его цену 1→2→3→4→5 при неизменном общем числе лотов (6), собственный лот игрока не возобновляется.
9. **Два бага торгового маршрута.** (а) Постройка маршрута сейчас доступна только первооткрывателю технологии, дающей маршрут — должна быть доступна любому игроку с этой технологией (сверить итоговое поведение с новым правилом открытости из п. 5, раз оно меняет, что вообще считается «получил технологию»). (б) Если маршрут построить некуда (нет второго города для соединения), сейчас нет кнопки/способа превратить это право в карту «Право прокладки маршрута» в руке — при том что сама карта (`makeRouteRightCard`) в коде уже есть, значит где-то не срабатывает или недоступна в этом сценарии — перепроверить.
10. **[ИСПРАВЛЕНО, переработано по прямому уточнению] Лимит склада на экране + проверка переполнения.** Показать справа от заголовка «Склад» текущее заполнение («X из Y»). — Первая версия правки (блокировать «Рабочего», если добытое не поместится, с модалкой-отказом сразу при сборе) заменена: по прямому уточнению это неудобно — теперь лимит НЕ проверяется вообще в процессе хода (склад свободно растёт выше лимита, `addToWarehouse` больше не обрезает и не отказывает), а проверяется ТОЛЬКО в конце хода. `GameSession.endTurn` (перед всем остальным, включая проверку руки ≥8) сверяет `warehouseTotal` с `warehouseCapFor` (6, 12 со зданием «Склад») — если склад больше лимита, конец хода жёстко отклоняется (`ActionResult.needsWarehouseTrim: { total, cap, overBy }`, БЕЗ пути «подтвердить и продолжить», в отличие от needsDiscardConfirm рядом) — клиент показывает модалку «Склад переполнен» с текстом, сколько единиц минимум нужно продать; игрок продаёт лишнее на бирже и повторяет конец хода. Индикатор «X из Y» у заголовка «Склад» (`renderWarehouse`) подсвечивается красным, когда фактически превышен лимит. Проверено пробником — свободный рост склада выше лимита без обрезки, отказ `endTurn` с точным `overBy`, успешный конец хода после продажи до лимита, корректный лимит 12 при здании «Склад».
11. **Транзитные города торгового маршрута.** Если при прокладке маршрут физически проходит через гекс ещё одного (третьего) города — не только двух городов-концов, — этот транзитный город тоже должен войти в торговую сеть.
12. **[ИСПРАВЛЕНО] Потеря столицы и выбывание игрока.** Если столица уничтожена каким-либо событием — новая столица назначается случайно среди оставшихся городов игрока, все здания старой столицы теряются безвозвратно. Если игрок в результате каких-либо событий лишился ВСЕХ городов — он выбывает из партии: все его оставшиеся юниты и торговые маршруты снимаются с карты, всем остальным игрокам показывается уведомление о его гибели поверх карты. — Реализовано как единый `GameSession.handleCityLoss(oldOwnerId, wasCapital)`, вызывается ПОСЛЕ любой потери города, из трёх мест, которые сейчас реально могут отобрать город (по прямому уточнению — «из-за эффектов сброса, войны или катастроф»): `destroyCity` (население упало до 0 — осада §6.9, катастрофа, сброс), захват при завершении хода в `resolveUnitMovementForCycle` (война), и мирная передача города по дипломатии (`transferCity`, giveCity/demandCity — сделано заодно, для того же инварианта «ровно одна столица на активного игрока»). Столица потеряна, другие города остались — случайный из оставшихся становится новой столицей (`this.rng()`), все здания игрока снимаются (здания в этой модели общие на игрока, а не привязаны к конкретному городу — фактически «привязаны к столице»); городов не осталось совсем — `eliminatedPlayers` пополняется этим id, юниты/маршруты/оставшиеся здания снимаются. Заодно закрыт побочный баг у катастрофы: `applyCatastropheLoss` раньше убирала маленький город (`population < 3`) через прямой `splice` в обход `destroyCity` — руины/разрыв торговых маршрутов/выбывание не отрабатывали; теперь оба исхода катастрофы (мгновенное исчезновение и «−3 населения» до 0) идут через `destroyCity`. Клиент — `main.ts`: зеркалит `eliminatedPlayers`, копит вновь появившихся в `eliminationNoticeQueue` и показывает модалку «Игрок выбыл» поверх карты каждому клиенту по одному, следующая в очереди открывается сразу по закрытии текущей. Проверено пробником — 5 сценариев (переизбрание столицы со сносом зданий, выбывание при захвате последнего города, выбывание от катастрофы, выбывание от сброса карты «Население», плюс стартовая биржа из п.8).
13. **[ИСПРАВЛЕНО] Продукция фабрики (промтовары) — универсальный ресурс-джокер.** При списании цены (любой — здания, юнита, исследования и т.д.) промтовары должны засчитываться за ЛЮБОЙ требуемый ресурс, КРОМЕ урана, углеводородов и электричества — своего рода джокер вместо конкретного типа. Ничто не удваивает его добычу — это эффект здания, не сбор с карты. — Реализовано ОДНИМ общим хелпером `GameSession.matchWithJoker(match)`, оборачивающим любой `match`-предикат требования: кандидат считается подходящим, если он проходит обычную проверку ИЛИ это Промтовары И у требования есть хоть один реально подходящий ресурс ВНЕ исключённой тройки (перебор по `RESOURCES` — не нужно вручную размечать, какие строки цены «только для урана/углеводородов/электричества», это выводится из самого `match`). Подключено во ВСЕ три системы трат в игре, которые до этого были независимы друг от друга: `planResourceSpend` (юниты, 7.1), `planFoodSpend` (Поселенец/рост города/посадка леса — хардкодная фильтрация по категории `food` заменена на `matchWithJoker`), `planBuildingSpend`→`pickOne` (здания/Ядерный арсенал/исследования — единая точка, все line-виды `specific`/`anyOf`/`category` получили джокер сразу). Добыча промтоваров НЕ удваивается ни одним техом извлечения (Гончарное дело/Индустриализация/Гильдии/Генная инженерия) — это уже было так само по себе и правки не потребовало: `activateProductionBuilding` кладёт на склад фиксированный `b.produces.qty`, минуя `extractionMultiplier` целиком (тот участвует только в добыче Рабочим/Складом с карты региона, а промтовары там в принципе не бывают — `targetCount: 0`, генератором карты не размещаются). Проверено пробником — джокер платит за еду (Э1) и за еду+металл (Э2, 2 юнита), корректно ОТКАЗЫВАЕТ, когда цена требует именно Уран (Ядерный арсенал: 2 Уран + 1 Металл — есть только промтовары, план не складывается) или именно anyOf(Углеводороды,Электричество) (исследование Э5), при этом обычная еда (Э1 исследования) джокером оплачивается свободно.
14. **Подсказка при наведении на игрока в окне дипломатии.** Показывать: сколько у него денег, сколько карт на руках, сколько зданий, сколько военных юнитов (числом, без разбивки по типам), сколько городов, его религию и социально-политическую парадигму.
15. **[ИСПРАВЛЕНО] Вырубка последнего леса в регионе.** Если вырубка (Рабочий, `chopForest`) убирает последний лес региона — случайная равнина региона опустынивается (ресурс на ней, если был, пропадает вместе с ней); равнин в регионе нет — исчезает случайный ресурс где-нибудь в регионе. — Реализовано в `GameSession.cascadeLastForestLoss(rc, rr)`, вызывается из `chopForest` после снятия леса с клетки: сканирует весь регион на оставшийся лес — если не осталось, тот же паттерн опустынивания/потери ресурса, что уже был у штрафного эффекта карты «Рост леса» (`degradeForestOrLand`), только теперь ОТДЕЛЬНО и по региону, а не по всей территории игрока. Каскад намеренно НЕ подключён к автоочистке леса под новым городом (`clearForestUnderCity`) и к штрафу `degradeForestOrLand` — правка была про явную вырубку Рабочим, эти два места оставлены как есть, чтобы не удваивать наказание/не удивлять игрока при основании города. Результат сообщается игроку через `hint` на успешной вырубке. Проверено пробником — вырубка последнего леса региона (все остальные лесные гексы предварительно убраны) корректно опустынила случайную равнину и убрала с неё ресурс.
16. **[ИСПРАВЛЕНО] Цена Галеры и ранней артиллерии — дерево вместо еды/металла.** Галера (Э1, была 1 еда) → 1 Лес. Катапульта/Требушет (Дальняя атака Э1/Э2) — «по аналогии с кораблями» → тот же паттерн, что у деревянных кораблей (`WOODEN_SHIP_RESOURCE_COST`): Э1 полностью на Лес (был только пищевой), Э2 — металл заменён на Лес, пищевой остаётся. Пушка и дальше (Э3+, порох) не тронуты — обычная цена. — Реализовано новой таблицей `GameSession.WOODEN_RANGED_RESOURCE_COST` (эпохи 1-2), `buildUnitCard` теперь выбирает переопределение цены и для `category === "ranged"`, не только для `"ship"`; добавлена запись для Э1 в саму `WOODEN_SHIP_RESOURCE_COST` (раньше там не было эпохи 1 — Галера просто ничем не переопределялась и брала дефолтную цену эпохи 1, еду). Клиентское зеркало таблицы цен (`main.ts`, только для подсказки в попапе постройки — списывает всё равно сервер) обновлено идентично. Проверено пробником — Галера/Катапульта списали ровно 1 Лес со склада (еда не тронута), Требушет тоже забрал Лес со склада (пищевая часть его цены в этом тесте была покрыта бесплатным доступом региона, а не складом — ожидаемый приоритет спенд-планировщика «доступ → склад → рынок», не баг).
17. **[ИСПРАВЛЕНО] Лес приравнен к торговому ресурсу.** Убрать из стратегических — торгуется в торговой сети как обычный торговый ресурс. — Изменена ровно одна строка данных: `wood` в `RESOURCES` (`web/src/map/types.ts`) — `category: "strategic"` → `"trade"`. Вся остальная логика (доступ к сети/«Торговец», удвоение добычи по технологиям, гейт добычи по технологии) уже была параметризована по `category` из этой же таблицы, так что переключение сработало «само» без отдельных правок кода — с побочными, но естественными следствиями: (а) сбор Леса Рабочим в регионе больше не требует «Горное дело» (гейт был только у стратегических, `resourceIsExtractable`) — вырубка топором (`chopForest`) гейта и не имела, тут выравнивание для регионного сбора; (б) удвоение добычи Леса теперь даёт «Гильдии» (торговые ×2), а не «Индустриализация» (стратегические ×2), как раньше. Проверено пробником — `resourceIsExtractable(0, "wood")` без «Горное дело» вернул `true`.

---

## 15. Катаклизмы, пути победы и Совет ООН (переработка, зафиксировано по прямому запросу — сначала список, потом коммит/пуш, реализация поэтапно ПОСЛЕ)

Список записан со слов пользователя как есть, без предварительного анализа реализуемости — уточнения и решение неоднозначностей делаются на своём шаге, не здесь (тот же принцип, что и у §14). Ничего из этого раздела не реализовано в коде.

### 15.1. Катаклизмы (детализация эффекта сброса «Учёного», см. таблицу 2.3.1)

Раньше таблица 2.3.1 описывала эффект «Учёного» как заглушку («вскрывает карту катаклизма, сам катаклизм не спроектирован»). Теперь катаклизм спроектирован — 6 видов, ниже.

- **Триггер и количество** — за КАЖДЫЙ экземпляр «Учёного» среди сброшенных карт (та же логика «по числу экземпляров», что и у остальных строк таблицы 2.3.1) разыгрывается число катаклизмов, равное **текущей максимальной эпохе игрока, который сбросил карты** (`playerEpoch(playerId)` — та же функция, что уже используется в `researchTech`/`availableResearchFor`). Чем выше эпоха игрока — тем больше катаклизмов происходит за один сброшенный «Учёный» (например Э3 → 3 катаклизма, Э6 → 6). Каждый катаклизм — независимый случайный выбор одного из 6 видов ниже, **повторы одного и того же вида в одной пачке разрешены и это намеренно** (прямое уточнение) — см. следующий пункт про масштабирование магнитуды.
- **Масштабирование магнитуды по повторам (прямое уточнение).** Если один и тот же вид катаклизма выпал в пачке несколько раз, эффект не просто повторяется независимо по разу на каждое выпадение — **магнитуда эффекта этого вида умножается на текущую эпоху игрока и на то, сколько раз именно этот вид выпал в этой пачке** («множится на номер эпохи по количеству этого катаклизма» — дословно). Пример логики: Э3, «Пандемия» выпала 2 раза из 3 бросков этой пачки → эффект «Пандемии» в этой пачке масштабируется на 3 (эпоха) × 2 (повторов) = 6, не дважды по «−1 население». **Как именно конкретное число превращается в эффект у каждого из 6 видов ниже (например «−1 население» → «−6 населения» одному городу, или 6 разных городов по −1, или что-то ещё) не уточнено — решить по каждому виду на своём шаге реализации**, ниже в описании каждого вида это отдельно не расписано, только сам принцип масштабирования.
- **Эффекты глобальные, не только для сбросившего игрока** — катаклизм не привязан к игроку-инициатору вообще, может задеть тайл/город/ресурс ЛЮБОГО игрока (или ничей, если тайл необитаем) — это стихийное бедствие всего мира, а не наказание конкретно сбросившему.
- **Рабочий-нейтрализатор (2.3.1, «Рабочий среди сброшенных отменяет эффект сброса только у карт ДЕЙСТВИЙ») распространяется и на катаклизмы «Учёного»** (прямое уточнение — подтверждено, «Учёный» такая же карта действия, как «Воин»/«Поселенец»/«Строитель»): если среди сброшенных карт есть «Рабочий», ни один «Учёный» в той же пачке катаклизмы не разыгрывает.

Шесть видов катаклизма:

1. **Таяние льдов.** Два независимых эффекта разом, не альтернатива (прямое уточнение — «и лёд тает, и пустыня тонет»): (а) случайный гекс полярной шапки тает — на его месте образуется море, либо открывается тундра, ранее скрытая подо льдом; (б) ОТДЕЛЬНО случайный гекс пустыни уходит под воду вместе со всем, что на нём находится (ресурс, постройки, юниты — если есть).
2. **Извержение вулкана.** Случайный гекс гор превращается в гекс вулкана — оверлей по аналогии с лесом (пятна лавы с кратером посередине). Ресурс на гексе снимается, гекс становится непроходимым. Если гор на карте уже не осталось (все либо стали вулканами, либо их не было) — случайные Холмы превращаются в новые Горы, которые в будущих катаклизмах этого вида тоже могут стать вулканом.
3. **Деградация.** Выбирается случайный гекс: если на нём есть лес — лес исчезает (без дальнейшей деградации рельефа в этот же катаклизм); если леса нет — рельеф деградирует на 1 ступень по цепочке Горы → Холмы → Равнина → Пустыня → Море (все ресурсы/постройки на затопленной клетке пропадают вместе с ней, как и у обычного опустынивания в 2.3.1/3.2.4).
4. **Пандемия.** Случайный город (любого игрока) теряет 1 населения (не ниже 1 — не убивает город сама по себе, тот же принцип нижней границы, что у «Население»/«Поселенец» в 2.3.1; с масштабированием по повторам — см. выше).
5. **Истощение ресурсов.** 1 случайный ресурс (конкретный экземпляр на карте, не весь тип целиком) исчезает (с масштабированием по повторам — см. выше).
6. **Землетрясение.** Случайный РЕГИОН испытывает землетрясение — считаются все регионы, включая морские и необитаемые (не только регионы с городами). **Здания рушатся, только если землетрясение задело регион СТОЛИЦЫ** (прямое уточнение — снимает неоднозначность «здания привязаны к игроку/столице, не к городу»: если задет регион не-столичного города или необитаемый/морской регион, постройки не трогаются вообще). Город в затронутом регионе (если есть, включая нестоличный) теряет 1 населения, если его размер больше 1 жителя — это условие не зависит от того, столица это или нет. **Анимация (прямой запрос)** — небольшая тряска: гексы затронутого региона на экране слегка сдвигаются влево-вправо (могут временно наезжать друг на друга), затем возвращаются на исходные места — чисто визуальный эффект, тот же класс, что и уже реализованная анимация дальних атак (`fxLayer`, 6.9), на состояние партии не влияет.

### 15.2. Пути победы (обзор — 5 условий, часть уже есть в коде под другими названиями)

По прямому запросу партия может закончиться пятью разными способами (не взаимоисключающими по разработке — реализуются независимо):

1. **ООН** — объединить мир под одним лидером через Совет ООН (см. 15.3 ниже).
2. **Военная** — захватить войной львиную долю территорий (порог «львиная доля» не задан числом — уточнить на своём шаге; вероятно доля от общего числа регионов/городов на карте).
3. **Космос** — покинуть планету (уже в коде и в ТЗ — Космодром, 3 компонента корабля, см. 4.4/9).
4. **Выживание** — пережить период катастроф в **60 циклов** (цикл = ход каждого игрока по кругу, т.е. полный круг ходов, а не отдельный ход одного игрока). **Подтверждено прямым уточнением — существующий `turnsRemaining` считает НЕПРАВИЛЬНО**: сейчас убывает на 1 за каждый ОТДЕЛЬНЫЙ ход любого игрока (см. §14 п.7/11.6 «Сводка по партии»), а должен убывать на 1 только за ПОЛНЫЙ цикл (когда `currentPlayerIndex` возвращается к 0, т.е. `onPlayingEndTurn()`/цикл в терминах уже существующего кода — тот же момент, что сбрасывает `productionUsedThisCycle`/`accessUsed`/`hexDefense` и т.д., см. 6.8/9). Переделать существующий счётчик под эту семантику (не заводить отдельный новый) — реализовано (см. §14 текущей сессии). **Уточнение по прямому запросу (см. 15.4) — это НЕ эксклюзивная победа одного игрока: как только `turnsRemaining` доходит до 0, ВСЕ ещё не выбывшие (`eliminatedPlayers`) на этот момент игроки побеждают вместе, общей победой («мы пережили это») — проигравших в этом случае нет, только уже выбывшие по ходу партии (потеря всех городов и т.п.) считаются проигравшими. Добровольный «режим вызова», а не гонка за единоличным первым местом, в отличие от остальных 4 путей победы.
5. **Большинство** — набрать большинство (большую часть) населения планеты под одним игроком (в клиенте уже отображается лидер по населению с % от общего, см. 11.6, — сейчас чисто информационно, победу не запускает).

### 15.3. Совет ООН — кандидаты, генсек, резолюции (переработка, заменяет прежнюю простую модель голосования из 4.4/14)

Прежний дизайн ООН (4.4: «раз в цикл голосование среди игроков, большинство в поддержку владельца завершает партию его победой») и уже реализованный в коде минимальный вариант (`useOon`/`voteOon`/`pendingUnVote` — голосование «за инициатора», большинство от ЧИСЛА игроков, не от населения) **заменяются** этой моделью — код после этой сессии реализует старую, более простую версию и потребует переработки под то, что ниже.

- **Кандидаты.** Игрок, первым построивший здание ООН, становится кандидатом №1. Игрок, вторым построивший здание ООН, становится кандидатом №2. Пока второе здание ООН не построено никем — кандидатом №2 автоматически считается игрок (кроме кандидата №1), у которого больше всего населения; при равном населении — у кого больше территорий (не уточнено — регионов или городов, уточнить на своём шаге); при равенстве и этого показателя — у кого больше войск (юнитов) среди оставшихся претендентов. Кандидат №2 при этом методе может смениться в любой момент, если у другого игрока изменились эти показатели (не уточнено явно, но следует из формулировки «пока никто не построил» — пересчитывается динамически).
- **Первые выборы.** Постройка ПЕРВОГО здания ООН (кандидатом №1) сразу же запускает первые выборы генерального секретаря ООН — между кандидатом №1 и (на тот момент ещё автоматическим) кандидатом №2. **Не уточнено на своём шаге**: как проходят сами выборы (голосуют ли все игроки, тем же весом населения, что и резолюции ниже — вероятно да, по аналогии), и что происходит с постом генсека дальше — переизбрание по таймеру/циклу, при смене кандидата №2, или пост бессрочный до конца партии/до потери кандидатом города.
- **Резолюции.** Генсек ООН выносит резолюции. **Выкладка (вынесение на голосование) каждой резолюции стоит генсеку 1 действие + 10 💰** (списывается при вынесении, независимо от исхода голосования — не уточнено на своём шаге, но по аналогии с остальными платными действиями в клиенте деньги/действие тратятся сразу, не по результату). Резолюция принимается, если голосов «за» набирается **≥60% от общего числа голосов**. **Вес голоса каждого игрока = размер его населения** (не «1 игрок — 1 голос», как в уже реализованной упрощённой версии).
- **10 видов резолюций** (эффект — на всю партию, пока резолюция не отменена/не переопределена следующей того же типа — не уточнено, но большинство эффектов ниже похожи на переключатели состояния мира, а не разовые события):
  1. **Открытая торговля.** Все торговые пути становятся доступны всем игрокам (не открывает границы — отдельно от соглашения «Открытые границы», это только доступ к чужим торговым сетям, см. 11.7/`tradeNetworkOf`).
  2. **Выборы мирового лидера.** **Подтверждено прямым уточнением — это и есть путь победы «ООН» из 15.2.** Голосование (голосовать за себя можно, воздержаться тоже можно), тот же порог и вес голоса, что у резолюций вообще (≥60% от суммарного населения-как-голосов, см. выше) — если набирается, играющий проголосовавший за себя(-её) побеждает партию как мировой лидер. Прямое следствие, отмеченное пользователем: раз голосовать за себя можно, игрок, контролирующий **60% населения планеты лично**, гарантированно побеждает этой резолюцией без чужих голосов вообще (сам факт владения этой долей населения уже даёт нужные 60% «за», даже если все остальные проголосуют «против» или воздержатся).
  3. **Запрет ядерного оружия.** Производить (активировать Ядерный арсенал на) новое ЯО больше нельзя, пока резолюция действует.
  4. **Нейтральные воды.** Перемещение по морю разрешено всем игрокам везде, независимо от дипломатических соглашений о границах — кроме входа непосредственно в чужие города.
  5. **Санкции на страну.** Выбранному игроку запрещена любая дипломатия — включая уже действующие соглашения (аннулируются).
  6. **Зелёная повестка.** Высадка леса (карта «Рост леса»/аналогичные механики посадки) — эффект удваивается.
  7. **Регуляция цен.** Жёсткая фиксация цены на бирже для выбранного ресурса (выбор ресурса + конкретная цена при принятии резолюции).
  8. **Сдерживание вооружений.** Лимит числа военных юнитов на игрока (запрет строить больше N — N задаётся при принятии). Уже существующие юниты сверх лимита не распускаются принудительно.
  9. **Помощь.** Перевод денег в размере N от КАЖДОЙ страны выбранной стране-получателю (N и получатель задаются при принятии).
  10. **Кредитование.** Каждая страна получает деньги пропорционально своему населению × заданный множитель (денежная эмиссия — деньги создаются, не переводятся от других игроков, в отличие от «Помощи»).

### 15.4. Предельные исследования «Учёного» (когда все ветки открыты, записано по прямому запросу)

Не реализовано. Как только КАЖДАЯ технология в КАЖДОЙ ветке уже когда-либо исследована КЕМ-ТО (та же коллективная модель открытости, что и в §14 п.5/`branchGroupDepth` — для каждой ветки `branchGroupDepth(branch) === branchTechOrder(branch).length`, дальше открывать нечего), карта «Учёный» не становится бесполезной: розыгрыш всё ещё стоит **предельную цену эпохи 6** (см. 4.3, сама эта цена по-прежнему не реализована — см. «Открытые вопросы» в 4.3/13), но вместо открытия технологии даёт игроку выбор **1 из 4** повторяемых эффектов ниже — можно разыгрывать сколько угодно раз подряд, пока условие «все ветки открыты» держится (оно уже необратимо, раз технологии не забываются).

Смысл, отмеченный пользователем: это инструмент борьбы с катастрофами §15.1 — восстановление после катаклизмов, когда сама исследовательская гонка уже закончилась.

1. **Рост населения.** Население ВО ВСЕХ городах игрока +1 бесплатно (без ресурсов/денег) — но не выше лимита **6** (новый предел размера города, не встречался в ТЗ раньше — уточнить на своём шаге, применяется ли этот лимит 6 вообще где-то ещё, кроме как здесь верхней границей для этого конкретного эффекта, или это общий предел роста города per se).
2. **Освоение пустынь.** В выбранном регионе — до 2 гексов: пустыня → равнина (реставрация плодородия). Если подходящих пустынь в регионе меньше двух (в т.ч. если там вместо суши уже море — например, после «Таяния льдов»/«Деградации», §15.1) — недостающие «слоты» насыпают остров на месте моря вместо равнины, но он сразу пустыня, не равнина (её ещё предстоит освоить отдельным применением этого же эффекта позже) — итог одного применения: до 2 гексов меняются суммарно, каждый либо пустыня→равнина, либо море→(новая) пустыня.
3. **Геологоразведка.** В выбранном регионе — 50% шанс открыть стратегический ресурс на 1 случайном подходящем гексе региона: нельзя поверх гекса, где уже есть ресурс, и региону нельзя иметь больше 4 ресурсов суммарно (после этого эффекта). Провал (50%) — действие/карта всё равно потрачены, эффекта на карте нет (не уточнено на своём шаге, но по аналогии с остальными «шанс не сработал» механиками в этом ТЗ, напр. защита от ЯО у Космонавтики).
4. **Посев пищи.** Выбрать гекс Равнины (без ресурса) и разместить на нём пищевой ресурс ОДНОГО из тех типов, что уже есть у игрока на складе (не любой пищевой ресурс из таблицы — только тот, что реально есть в `warehouse` сейчас) — не уточнено явно, списывается ли 1 единица этого ресурса со склада как «плата за посев» (вероятно да, раз выбор ограничен именно тем, что «уже есть на складе» — иначе привязка к складу была бы бессмысленна) — уточнить на своём шаге.

Прямая связь с путём победы «Выживание» (15.2 п.4): пока идёт партия после исчерпания всех веток, «Учёный» перестаёт быть про технологический прогресс и становится про сдерживание катастроф — добровольный режим на прохождение оставшихся циклов до 60, а не про обгон других игроков. Итог при достижении 60 циклов — общая победа всех выживших (см. правку в 15.2 п.4 выше), не персональная.
